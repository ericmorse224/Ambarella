'use strict';

var isMap = require('is-map');
var isSet = require('is-set');
var isWeakMap = require('is-weakmap');
var isWeakSet = require('is-weakset');

/** @type {import('.')***REMOVED*** */
module.exports = function whichCollection(/** @type {unknown***REMOVED*** */ value) {
	if (value && typeof value === 'object') {
		if (isMap(value)) {
			return 'Map';
		***REMOVED***
		if (isSet(value)) {
			return 'Set';
		***REMOVED***
		if (isWeakMap(value)) {
			return 'WeakMap';
		***REMOVED***
		if (isWeakSet(value)) {
			return 'WeakSet';
		***REMOVED***
	***REMOVED***
	return false;
***REMOVED***;

/*
  Copyright 2019 Google LLC

  Use of this source code is governed by an MIT-style
  license that can be found in the LICENSE file or at
  https://opensource.org/licenses/MIT.
*/

import upath from 'upath';
import {Compilation, WebpackError***REMOVED*** from 'webpack';

import {resolveWebpackURL***REMOVED*** from './resolve-webpack-url';

export function getScriptFilesForChunks(
  compilation: Compilation,
  chunkNames: Array<string>,
): Array<string> {
  const {chunks***REMOVED*** = compilation.getStats().toJson({chunks: true***REMOVED***);
  const {publicPath***REMOVED*** = compilation.options.output;
  const scriptFiles = new Set<string>();

  for (const chunkName of chunkNames) {
    const chunk = chunks!.find((chunk) => chunk.names?.includes(chunkName));
    if (chunk) {
      for (const file of chunk?.files ?? []) {
        // See https://github.com/GoogleChrome/workbox/issues/2161
        if (upath.extname(file) === '.js') {
          scriptFiles.add(resolveWebpackURL(publicPath as string, file));
        ***REMOVED***
      ***REMOVED***
    ***REMOVED*** else {
      compilation.warnings.push(
        new Error(
          `${chunkName***REMOVED*** was provided to ` +
            `importScriptsViaChunks, but didn't match any named chunks.`,
        ) as WebpackError,
      );
    ***REMOVED***
  ***REMOVED***

  if (scriptFiles.size === 0) {
    compilation.warnings.push(
      new Error(
        `There were no assets matching ` +
          `importScriptsViaChunks: [${chunkNames.join(' ')***REMOVED***].`,
      ) as WebpackError,
    );
  ***REMOVED***

  return Array.from(scriptFiles);
***REMOVED***

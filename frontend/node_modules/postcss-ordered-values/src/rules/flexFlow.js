'use strict';
// flex-flow: <flex-direction> || <flex-wrap>

const flexDirection = new Set([
  'row',
  'row-reverse',
  'column',
  'column-reverse',
]);

const flexWrap = new Set(['nowrap', 'wrap', 'wrap-reverse']);

/**
 * @param {import('postcss-value-parser').ParsedValue***REMOVED*** flexFlow
 * @return {string***REMOVED***
 */
module.exports = function normalizeFlexFlow(flexFlow) {
  let order = {
    direction: '',
    wrap: '',
  ***REMOVED***;

  flexFlow.walk(({ value ***REMOVED***) => {
    if (flexDirection.has(value.toLowerCase())) {
      order.direction = value;
      return;
    ***REMOVED***

    if (flexWrap.has(value.toLowerCase())) {
      order.wrap = value;
      return;
    ***REMOVED***
  ***REMOVED***);

  return `${order.direction***REMOVED*** ${order.wrap***REMOVED***`.trim();
***REMOVED***;

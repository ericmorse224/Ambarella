import test from 'tape';
import { elementType ***REMOVED*** from 'jsx-ast-utils';

import isInteractiveRole from '../../../src/util/isInteractiveRole';
import {
  genElementSymbol,
  genInteractiveRoleElements,
  genNonInteractiveRoleElements,
***REMOVED*** from '../../../__mocks__/genInteractives';

test('isInteractiveRole', (t) => {
  t.equal(
    isInteractiveRole(undefined, []),
    false,
    'identifies JSX Components (no tagName) as interactive role elements',
  );

  t.test('elements with a non-interactive role', (st) => {
    genNonInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;

      st.equal(
        isInteractiveRole(
          elementType(openingElement),
          attributes,
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` as an interactive role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.equal(
    isInteractiveRole('div', []),
    false,
    'does NOT identify elements without a role as interactive role elements',
  );

  t.test('elements with an interactive role', (st) => {
    genInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;

      st.equal(
        isInteractiveRole(
          elementType(openingElement),
          attributes,
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as an interactive role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.end();
***REMOVED***);

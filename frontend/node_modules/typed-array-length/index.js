'use strict';

// / <reference types="node" />

var callBind = require('call-bind');
var forEach = require('for-each');
var gOPD = require('gopd');
var isTypedArray = require('is-typed-array');
var typedArrays = require('possible-typed-array-names');
var gPO = require('reflect.getprototypeof/polyfill')();

/** @typedef {(value: import('.').TypedArray) => number***REMOVED*** TypedArrayLengthGetter */
/** @typedef {{ [k in `$${import('.').TypedArrayName***REMOVED***` | '__proto__']: k extends '__proto__' ? null : TypedArrayLengthGetter ***REMOVED******REMOVED*** Cache */

/** @type {Cache***REMOVED*** */
// @ts-expect-error TS doesn't seem to have a "will eventually satisfy" type
var getters = { __proto__: null ***REMOVED***;
var oDP = Object.defineProperty;
if (gOPD) {
	var getLength = /** @type {TypedArrayLengthGetter***REMOVED*** */ function (x) {
		return x.length;
	***REMOVED***;
	forEach(typedArrays, /** @type {(typedArray: import('.').TypedArrayName) => void***REMOVED*** */ function (typedArray) {
		var TA = global[typedArray];
		// In Safari 7, Typed Array constructors are typeof object
		if (typeof TA === 'function' || typeof TA === 'object') {
			var Proto = TA.prototype;
			// @ts-expect-error TS doesn't narrow types inside callbacks, which is weird
			var descriptor = gOPD(Proto, 'length');
			if (!descriptor) {
				var superProto = gPO(Proto);
				// @ts-expect-error TS doesn't narrow types inside callbacks, which is weird
				descriptor = gOPD(superProto, 'length');
			***REMOVED***
			// Opera 12.16 has a magic length data property on instances AND on Proto
			if (descriptor && descriptor.get) {
				// eslint-disable-next-line no-extra-parens
				getters[/** @type {`$${import('.').TypedArrayName***REMOVED***`***REMOVED*** */ ('$' + typedArray)] = callBind(descriptor.get);
			***REMOVED*** else if (oDP) {
				// this is likely an engine where instances have a magic length data property
				var arr = new global[typedArray](2);
				// @ts-expect-error TS doesn't narrow types inside callbacks, which is weird
				descriptor = gOPD(arr, 'length');
				if (descriptor && descriptor.configurable) {
					oDP(arr, 'length', { value: 3 ***REMOVED***);
				***REMOVED***
				if (arr.length === 2) {
				// eslint-disable-next-line no-extra-parens
					getters[/** @type {`$${import('.').TypedArrayName***REMOVED***`***REMOVED*** */ ('$' + typedArray)] = getLength;
				***REMOVED***
			***REMOVED***
		***REMOVED***
	***REMOVED***);
***REMOVED***

/** @type {TypedArrayLengthGetter***REMOVED*** */
var tryTypedArrays = function tryAllTypedArrays(value) {
	/** @type {number***REMOVED*** */ var foundLength;
	// @ts-expect-error not sure why this won't work
	forEach(getters, /** @type {(getter: TypedArrayLengthGetter) => void***REMOVED*** */ function (getter) {
		if (typeof foundLength !== 'number') {
			try {
				var length = getter(value);
				if (typeof length === 'number') {
					foundLength = length;
				***REMOVED***
			***REMOVED*** catch (e) {***REMOVED***
		***REMOVED***
	***REMOVED***);
	// @ts-expect-error TS can't guarantee the above callback is invoked sync
	return foundLength;
***REMOVED***;

/** @type {import('.')***REMOVED*** */
module.exports = function typedArrayLength(value) {
	if (!isTypedArray(value)) {
		return false;
	***REMOVED***
	return tryTypedArrays(value);
***REMOVED***;

'use strict';

var test = require('tape');
var qs = require('../');
var utils = require('../lib/utils');
var iconv = require('iconv-lite');
var SaferBuffer = require('safer-buffer').Buffer;
var hasSymbols = require('has-symbols');
var mockProperty = require('mock-property');
var emptyTestCases = require('./empty-keys-cases').emptyTestCases;
var hasBigInt = typeof BigInt === 'function';

test('stringify()', function (t) {
    t.test('stringifies a querystring object', function (st) {
        st.equal(qs.stringify({ a: 'b' ***REMOVED***), 'a=b');
        st.equal(qs.stringify({ a: 1 ***REMOVED***), 'a=1');
        st.equal(qs.stringify({ a: 1, b: 2 ***REMOVED***), 'a=1&b=2');
        st.equal(qs.stringify({ a: 'A_Z' ***REMOVED***), 'a=A_Z');
        st.equal(qs.stringify({ a: '€' ***REMOVED***), 'a=%E2%82%AC');
        st.equal(qs.stringify({ a: '' ***REMOVED***), 'a=%EE%80%80');
        st.equal(qs.stringify({ a: 'א' ***REMOVED***), 'a=%D7%90');
        st.equal(qs.stringify({ a: '𐐷' ***REMOVED***), 'a=%F0%90%90%B7');
        st.end();
    ***REMOVED***);

    t.test('stringifies falsy values', function (st) {
        st.equal(qs.stringify(undefined), '');
        st.equal(qs.stringify(null), '');
        st.equal(qs.stringify(null, { strictNullHandling: true ***REMOVED***), '');
        st.equal(qs.stringify(false), '');
        st.equal(qs.stringify(0), '');
        st.end();
    ***REMOVED***);

    t.test('stringifies symbols', { skip: !hasSymbols() ***REMOVED***, function (st) {
        st.equal(qs.stringify(Symbol.iterator), '');
        st.equal(qs.stringify([Symbol.iterator]), '0=Symbol%28Symbol.iterator%29');
        st.equal(qs.stringify({ a: Symbol.iterator ***REMOVED***), 'a=Symbol%28Symbol.iterator%29');
        st.equal(
            qs.stringify({ a: [Symbol.iterator] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***),
            'a[]=Symbol%28Symbol.iterator%29'
        );
        st.end();
    ***REMOVED***);

    t.test('stringifies bigints', { skip: !hasBigInt ***REMOVED***, function (st) {
        var three = BigInt(3);
        var encodeWithN = function (value, defaultEncoder, charset) {
            var result = defaultEncoder(value, defaultEncoder, charset);
            return typeof value === 'bigint' ? result + 'n' : result;
        ***REMOVED***;
        st.equal(qs.stringify(three), '');
        st.equal(qs.stringify([three]), '0=3');
        st.equal(qs.stringify([three], { encoder: encodeWithN ***REMOVED***), '0=3n');
        st.equal(qs.stringify({ a: three ***REMOVED***), 'a=3');
        st.equal(qs.stringify({ a: three ***REMOVED***, { encoder: encodeWithN ***REMOVED***), 'a=3n');
        st.equal(
            qs.stringify({ a: [three] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***),
            'a[]=3'
        );
        st.equal(
            qs.stringify({ a: [three] ***REMOVED***, { encodeValuesOnly: true, encoder: encodeWithN, arrayFormat: 'brackets' ***REMOVED***),
            'a[]=3n'
        );
        st.end();
    ***REMOVED***);

    t.test('encodes dot in key of object when encodeDotInKeys and allowDots is provided', function (st) {
        st.equal(
            qs.stringify(
                { 'name.obj': { first: 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: false, encodeDotInKeys: false ***REMOVED***
            ),
            'name.obj%5Bfirst%5D=John&name.obj%5Blast%5D=Doe',
            'with allowDots false and encodeDotInKeys false'
        );
        st.equal(
            qs.stringify(
                { 'name.obj': { first: 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeDotInKeys: false ***REMOVED***
            ),
            'name.obj.first=John&name.obj.last=Doe',
            'with allowDots true and encodeDotInKeys false'
        );
        st.equal(
            qs.stringify(
                { 'name.obj': { first: 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: false, encodeDotInKeys: true ***REMOVED***
            ),
            'name%252Eobj%5Bfirst%5D=John&name%252Eobj%5Blast%5D=Doe',
            'with allowDots false and encodeDotInKeys true'
        );
        st.equal(
            qs.stringify(
                { 'name.obj': { first: 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeDotInKeys: true ***REMOVED***
            ),
            'name%252Eobj.first=John&name%252Eobj.last=Doe',
            'with allowDots true and encodeDotInKeys true'
        );

        st.equal(
            qs.stringify(
                { 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: false, encodeDotInKeys: false ***REMOVED***
            ),
            'name.obj.subobject%5Bfirst.godly.name%5D=John&name.obj.subobject%5Blast%5D=Doe',
            'with allowDots false and encodeDotInKeys false'
        );
        st.equal(
            qs.stringify(
                { 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeDotInKeys: false ***REMOVED***
            ),
            'name.obj.subobject.first.godly.name=John&name.obj.subobject.last=Doe',
            'with allowDots false and encodeDotInKeys false'
        );
        st.equal(
            qs.stringify(
                { 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: false, encodeDotInKeys: true ***REMOVED***
            ),
            'name%252Eobj%252Esubobject%5Bfirst.godly.name%5D=John&name%252Eobj%252Esubobject%5Blast%5D=Doe',
            'with allowDots false and encodeDotInKeys true'
        );
        st.equal(
            qs.stringify(
                { 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeDotInKeys: true ***REMOVED***
            ),
            'name%252Eobj%252Esubobject.first%252Egodly%252Ename=John&name%252Eobj%252Esubobject.last=Doe',
            'with allowDots true and encodeDotInKeys true'
        );

        st.end();
    ***REMOVED***);

    t.test('should encode dot in key of object, and automatically set allowDots to `true` when encodeDotInKeys is true and allowDots in undefined', function (st) {
        st.equal(
            qs.stringify(
                { 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***,
                { encodeDotInKeys: true ***REMOVED***
            ),
            'name%252Eobj%252Esubobject.first%252Egodly%252Ename=John&name%252Eobj%252Esubobject.last=Doe',
            'with allowDots undefined and encodeDotInKeys true'
        );
        st.end();
    ***REMOVED***);

    t.test('should encode dot in key of object when encodeDotInKeys and allowDots is provided, and nothing else when encodeValuesOnly is provided', function (st) {
        st.equal(
            qs.stringify({ 'name.obj': { first: 'John', last: 'Doe' ***REMOVED*** ***REMOVED***, {
                encodeDotInKeys: true, allowDots: true, encodeValuesOnly: true
            ***REMOVED***),
            'name%2Eobj.first=John&name%2Eobj.last=Doe'
        );

        st.equal(
            qs.stringify({ 'name.obj.subobject': { 'first.godly.name': 'John', last: 'Doe' ***REMOVED*** ***REMOVED***, { allowDots: true, encodeDotInKeys: true, encodeValuesOnly: true ***REMOVED***),
            'name%2Eobj%2Esubobject.first%2Egodly%2Ename=John&name%2Eobj%2Esubobject.last=Doe'
        );

        st.end();
    ***REMOVED***);

    t.test('throws when `commaRoundTrip` is not a boolean', function (st) {
        st['throws'](
            function () { qs.stringify({***REMOVED***, { commaRoundTrip: 'not a boolean' ***REMOVED***); ***REMOVED***,
            TypeError,
            'throws when `commaRoundTrip` is not a boolean'
        );

        st.end();
    ***REMOVED***);

    t.test('throws when `encodeDotInKeys` is not a boolean', function (st) {
        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { encodeDotInKeys: 'foobar' ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { encodeDotInKeys: 0 ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { encodeDotInKeys: NaN ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { encodeDotInKeys: null ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st.end();
    ***REMOVED***);

    t.test('adds query prefix', function (st) {
        st.equal(qs.stringify({ a: 'b' ***REMOVED***, { addQueryPrefix: true ***REMOVED***), '?a=b');
        st.end();
    ***REMOVED***);

    t.test('with query prefix, outputs blank string given an empty object', function (st) {
        st.equal(qs.stringify({***REMOVED***, { addQueryPrefix: true ***REMOVED***), '');
        st.end();
    ***REMOVED***);

    t.test('stringifies nested falsy values', function (st) {
        st.equal(qs.stringify({ a: { b: { c: null ***REMOVED*** ***REMOVED*** ***REMOVED***), 'a%5Bb%5D%5Bc%5D=');
        st.equal(qs.stringify({ a: { b: { c: null ***REMOVED*** ***REMOVED*** ***REMOVED***, { strictNullHandling: true ***REMOVED***), 'a%5Bb%5D%5Bc%5D');
        st.equal(qs.stringify({ a: { b: { c: false ***REMOVED*** ***REMOVED*** ***REMOVED***), 'a%5Bb%5D%5Bc%5D=false');
        st.end();
    ***REMOVED***);

    t.test('stringifies a nested object', function (st) {
        st.equal(qs.stringify({ a: { b: 'c' ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=c');
        st.equal(qs.stringify({ a: { b: { c: { d: 'e' ***REMOVED*** ***REMOVED*** ***REMOVED*** ***REMOVED***), 'a%5Bb%5D%5Bc%5D%5Bd%5D=e');
        st.end();
    ***REMOVED***);

    t.test('`allowDots` option: stringifies a nested object with dots notation', function (st) {
        st.equal(qs.stringify({ a: { b: 'c' ***REMOVED*** ***REMOVED***, { allowDots: true ***REMOVED***), 'a.b=c');
        st.equal(qs.stringify({ a: { b: { c: { d: 'e' ***REMOVED*** ***REMOVED*** ***REMOVED*** ***REMOVED***, { allowDots: true ***REMOVED***), 'a.b.c.d=e');
        st.end();
    ***REMOVED***);

    t.test('stringifies an array value', function (st) {
        st.equal(
            qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***, { arrayFormat: 'indices' ***REMOVED***),
            'a%5B0%5D=b&a%5B1%5D=c&a%5B2%5D=d',
            'indices => indices'
        );
        st.equal(
            qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***, { arrayFormat: 'brackets' ***REMOVED***),
            'a%5B%5D=b&a%5B%5D=c&a%5B%5D=d',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***, { arrayFormat: 'comma' ***REMOVED***),
            'a=b%2Cc%2Cd',
            'comma => comma'
        );
        st.equal(
            qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***, { arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***),
            'a=b%2Cc%2Cd',
            'comma round trip => comma'
        );
        st.equal(
            qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***),
            'a%5B0%5D=b&a%5B1%5D=c&a%5B2%5D=d',
            'default => indices'
        );
        st.end();
    ***REMOVED***);

    t.test('`skipNulls` option', function (st) {
        st.equal(
            qs.stringify({ a: 'b', c: null ***REMOVED***, { skipNulls: true ***REMOVED***),
            'a=b',
            'omits nulls when asked'
        );

        st.equal(
            qs.stringify({ a: { b: 'c', d: null ***REMOVED*** ***REMOVED***, { skipNulls: true ***REMOVED***),
            'a%5Bb%5D=c',
            'omits nested nulls when asked'
        );

        st.end();
    ***REMOVED***);

    t.test('omits array indices when asked', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c', 'd'] ***REMOVED***, { indices: false ***REMOVED***), 'a=b&a=c&a=d');

        st.end();
    ***REMOVED***);

    t.test('omits object key/value pair when value is empty array', function (st) {
        st.equal(qs.stringify({ a: [], b: 'zz' ***REMOVED***), 'b=zz');

        st.end();
    ***REMOVED***);

    t.test('should not omit object key/value pair when value is empty array and when asked', function (st) {
        st.equal(qs.stringify({ a: [], b: 'zz' ***REMOVED***), 'b=zz');
        st.equal(qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: false ***REMOVED***), 'b=zz');
        st.equal(qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: true ***REMOVED***), 'a[]&b=zz');

        st.end();
    ***REMOVED***);

    t.test('should throw when allowEmptyArrays is not of type boolean', function (st) {
        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: 'foobar' ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: 0 ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: NaN ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st['throws'](
            function () { qs.stringify({ a: [], b: 'zz' ***REMOVED***, { allowEmptyArrays: null ***REMOVED***); ***REMOVED***,
            TypeError
        );

        st.end();
    ***REMOVED***);

    t.test('allowEmptyArrays + strictNullHandling', function (st) {
        st.equal(
            qs.stringify(
                { testEmptyArray: [] ***REMOVED***,
                { strictNullHandling: true, allowEmptyArrays: true ***REMOVED***
            ),
            'testEmptyArray[]'
        );

        st.end();
    ***REMOVED***);

    t.test('stringifies an array value with one item vs multiple items', function (st) {
        st.test('non-array item', function (s2t) {
            s2t.equal(qs.stringify({ a: 'c' ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a=c');
            s2t.equal(qs.stringify({ a: 'c' ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a=c');
            s2t.equal(qs.stringify({ a: 'c' ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=c');
            s2t.equal(qs.stringify({ a: 'c' ***REMOVED***, { encodeValuesOnly: true ***REMOVED***), 'a=c');

            s2t.end();
        ***REMOVED***);

        st.test('array with a single item', function (s2t) {
            s2t.equal(qs.stringify({ a: ['c'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[0]=c');
            s2t.equal(qs.stringify({ a: ['c'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[]=c');
            s2t.equal(qs.stringify({ a: ['c'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=c');
            s2t.equal(qs.stringify({ a: ['c'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***), 'a[]=c'); // so it parses back as an array
            s2t.equal(qs.stringify({ a: ['c'] ***REMOVED***, { encodeValuesOnly: true ***REMOVED***), 'a[0]=c');

            s2t.end();
        ***REMOVED***);

        st.test('array with multiple items', function (s2t) {
            s2t.equal(qs.stringify({ a: ['c', 'd'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[0]=c&a[1]=d');
            s2t.equal(qs.stringify({ a: ['c', 'd'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[]=c&a[]=d');
            s2t.equal(qs.stringify({ a: ['c', 'd'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=c,d');
            s2t.equal(qs.stringify({ a: ['c', 'd'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***), 'a=c,d');
            s2t.equal(qs.stringify({ a: ['c', 'd'] ***REMOVED***, { encodeValuesOnly: true ***REMOVED***), 'a[0]=c&a[1]=d');

            s2t.end();
        ***REMOVED***);

        st.test('array with multiple items with a comma inside', function (s2t) {
            s2t.equal(qs.stringify({ a: ['c,d', 'e'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=c%2Cd,e');
            s2t.equal(qs.stringify({ a: ['c,d', 'e'] ***REMOVED***, { arrayFormat: 'comma' ***REMOVED***), 'a=c%2Cd%2Ce');

            s2t.equal(qs.stringify({ a: ['c,d', 'e'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***), 'a=c%2Cd,e');
            s2t.equal(qs.stringify({ a: ['c,d', 'e'] ***REMOVED***, { arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***), 'a=c%2Cd%2Ce');

            s2t.end();
        ***REMOVED***);

        st.end();
    ***REMOVED***);

    t.test('stringifies a nested array value', function (st) {
        st.equal(qs.stringify({ a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[b][0]=c&a[b][1]=d');
        st.equal(qs.stringify({ a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[b][]=c&a[b][]=d');
        st.equal(qs.stringify({ a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a[b]=c,d');
        st.equal(qs.stringify({ a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true ***REMOVED***), 'a[b][0]=c&a[b][1]=d');
        st.end();
    ***REMOVED***);

    t.test('stringifies comma and empty array values', function (st) {
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: false, arrayFormat: 'indices' ***REMOVED***), 'a[0]=,&a[1]=&a[2]=c,d%');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: false, arrayFormat: 'brackets' ***REMOVED***), 'a[]=,&a[]=&a[]=c,d%');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: false, arrayFormat: 'comma' ***REMOVED***), 'a=,,,c,d%');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: false, arrayFormat: 'repeat' ***REMOVED***), 'a=,&a=&a=c,d%');

        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[0]=%2C&a[1]=&a[2]=c%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[]=%2C&a[]=&a[]=c%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=%2C,,c%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a=%2C&a=&a=c%2Cd%25');

        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'indices' ***REMOVED***), 'a%5B0%5D=%2C&a%5B1%5D=&a%5B2%5D=c%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'brackets' ***REMOVED***), 'a%5B%5D=%2C&a%5B%5D=&a%5B%5D=c%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'comma' ***REMOVED***), 'a=%2C%2C%2Cc%2Cd%25');
        st.equal(qs.stringify({ a: [',', '', 'c,d%'] ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'repeat' ***REMOVED***), 'a=%2C&a=&a=c%2Cd%25');

        st.end();
    ***REMOVED***);

    t.test('stringifies comma and empty non-array values', function (st) {
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: false, arrayFormat: 'indices' ***REMOVED***), 'a=,&b=&c=c,d%');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: false, arrayFormat: 'brackets' ***REMOVED***), 'a=,&b=&c=c,d%');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: false, arrayFormat: 'comma' ***REMOVED***), 'a=,&b=&c=c,d%');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: false, arrayFormat: 'repeat' ***REMOVED***), 'a=,&b=&c=c,d%');

        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');

        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'indices' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'brackets' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'comma' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');
        st.equal(qs.stringify({ a: ',', b: '', c: 'c,d%' ***REMOVED***, { encode: true, encodeValuesOnly: false, arrayFormat: 'repeat' ***REMOVED***), 'a=%2C&b=&c=c%2Cd%25');

        st.end();
    ***REMOVED***);

    t.test('stringifies a nested array value with dots notation', function (st) {
        st.equal(
            qs.stringify(
                { a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***
            ),
            'a.b[0]=c&a.b[1]=d',
            'indices: stringifies with dots + indices'
        );
        st.equal(
            qs.stringify(
                { a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***
            ),
            'a.b[]=c&a.b[]=d',
            'brackets: stringifies with dots + brackets'
        );
        st.equal(
            qs.stringify(
                { a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***
            ),
            'a.b=c,d',
            'comma: stringifies with dots + comma'
        );
        st.equal(
            qs.stringify(
                { a: { b: ['c', 'd'] ***REMOVED*** ***REMOVED***,
                { allowDots: true, encodeValuesOnly: true ***REMOVED***
            ),
            'a.b[0]=c&a.b[1]=d',
            'default: stringifies with dots + indices'
        );
        st.end();
    ***REMOVED***);

    t.test('stringifies an object inside an array', function (st) {
        st.equal(
            qs.stringify({ a: [{ b: 'c' ***REMOVED***] ***REMOVED***, { arrayFormat: 'indices', encodeValuesOnly: true ***REMOVED***),
            'a[0][b]=c',
            'indices => indices'
        );
        st.equal(
            qs.stringify({ a: [{ b: 'c' ***REMOVED***] ***REMOVED***, { arrayFormat: 'repeat', encodeValuesOnly: true ***REMOVED***),
            'a[b]=c',
            'repeat => repeat'
        );
        st.equal(
            qs.stringify({ a: [{ b: 'c' ***REMOVED***] ***REMOVED***, { arrayFormat: 'brackets', encodeValuesOnly: true ***REMOVED***),
            'a[][b]=c',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify({ a: [{ b: 'c' ***REMOVED***] ***REMOVED***, { encodeValuesOnly: true ***REMOVED***),
            'a[0][b]=c',
            'default => indices'
        );

        st.equal(
            qs.stringify({ a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***, { arrayFormat: 'indices', encodeValuesOnly: true ***REMOVED***),
            'a[0][b][c][0]=1',
            'indices => indices'
        );
        st.equal(
            qs.stringify({ a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***, { arrayFormat: 'repeat', encodeValuesOnly: true ***REMOVED***),
            'a[b][c]=1',
            'repeat => repeat'
        );
        st.equal(
            qs.stringify({ a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***, { arrayFormat: 'brackets', encodeValuesOnly: true ***REMOVED***),
            'a[][b][c][]=1',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify({ a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***, { encodeValuesOnly: true ***REMOVED***),
            'a[0][b][c][0]=1',
            'default => indices'
        );

        st.end();
    ***REMOVED***);

    t.test('stringifies an array with mixed objects and primitives', function (st) {
        st.equal(
            qs.stringify({ a: [{ b: 1 ***REMOVED***, 2, 3] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***),
            'a[0][b]=1&a[1]=2&a[2]=3',
            'indices => indices'
        );
        st.equal(
            qs.stringify({ a: [{ b: 1 ***REMOVED***, 2, 3] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***),
            'a[][b]=1&a[]=2&a[]=3',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify({ a: [{ b: 1 ***REMOVED***, 2, 3] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'comma' ***REMOVED***),
            '???',
            'brackets => brackets',
            { skip: 'TODO: figure out what this should do' ***REMOVED***
        );
        st.equal(
            qs.stringify({ a: [{ b: 1 ***REMOVED***, 2, 3] ***REMOVED***, { encodeValuesOnly: true ***REMOVED***),
            'a[0][b]=1&a[1]=2&a[2]=3',
            'default => indices'
        );

        st.end();
    ***REMOVED***);

    t.test('stringifies an object inside an array with dots notation', function (st) {
        st.equal(
            qs.stringify(
                { a: [{ b: 'c' ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false, arrayFormat: 'indices' ***REMOVED***
            ),
            'a[0].b=c',
            'indices => indices'
        );
        st.equal(
            qs.stringify(
                { a: [{ b: 'c' ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false, arrayFormat: 'brackets' ***REMOVED***
            ),
            'a[].b=c',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify(
                { a: [{ b: 'c' ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false ***REMOVED***
            ),
            'a[0].b=c',
            'default => indices'
        );

        st.equal(
            qs.stringify(
                { a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false, arrayFormat: 'indices' ***REMOVED***
            ),
            'a[0].b.c[0]=1',
            'indices => indices'
        );
        st.equal(
            qs.stringify(
                { a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false, arrayFormat: 'brackets' ***REMOVED***
            ),
            'a[].b.c[]=1',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify(
                { a: [{ b: { c: [1] ***REMOVED*** ***REMOVED***] ***REMOVED***,
                { allowDots: true, encode: false ***REMOVED***
            ),
            'a[0].b.c[0]=1',
            'default => indices'
        );

        st.end();
    ***REMOVED***);

    t.test('does not omit object keys when indices = false', function (st) {
        st.equal(qs.stringify({ a: [{ b: 'c' ***REMOVED***] ***REMOVED***, { indices: false ***REMOVED***), 'a%5Bb%5D=c');
        st.end();
    ***REMOVED***);

    t.test('uses indices notation for arrays when indices=true', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c'] ***REMOVED***, { indices: true ***REMOVED***), 'a%5B0%5D=b&a%5B1%5D=c');
        st.end();
    ***REMOVED***);

    t.test('uses indices notation for arrays when no arrayFormat is specified', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c'] ***REMOVED***), 'a%5B0%5D=b&a%5B1%5D=c');
        st.end();
    ***REMOVED***);

    t.test('uses indices notation for arrays when arrayFormat=indices', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c'] ***REMOVED***, { arrayFormat: 'indices' ***REMOVED***), 'a%5B0%5D=b&a%5B1%5D=c');
        st.end();
    ***REMOVED***);

    t.test('uses repeat notation for arrays when arrayFormat=repeat', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c'] ***REMOVED***, { arrayFormat: 'repeat' ***REMOVED***), 'a=b&a=c');
        st.end();
    ***REMOVED***);

    t.test('uses brackets notation for arrays when arrayFormat=brackets', function (st) {
        st.equal(qs.stringify({ a: ['b', 'c'] ***REMOVED***, { arrayFormat: 'brackets' ***REMOVED***), 'a%5B%5D=b&a%5B%5D=c');
        st.end();
    ***REMOVED***);

    t.test('stringifies a complicated object', function (st) {
        st.equal(qs.stringify({ a: { b: 'c', d: 'e' ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=c&a%5Bd%5D=e');
        st.end();
    ***REMOVED***);

    t.test('stringifies an empty value', function (st) {
        st.equal(qs.stringify({ a: '' ***REMOVED***), 'a=');
        st.equal(qs.stringify({ a: null ***REMOVED***, { strictNullHandling: true ***REMOVED***), 'a');

        st.equal(qs.stringify({ a: '', b: '' ***REMOVED***), 'a=&b=');
        st.equal(qs.stringify({ a: null, b: '' ***REMOVED***, { strictNullHandling: true ***REMOVED***), 'a&b=');

        st.equal(qs.stringify({ a: { b: '' ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=');
        st.equal(qs.stringify({ a: { b: null ***REMOVED*** ***REMOVED***, { strictNullHandling: true ***REMOVED***), 'a%5Bb%5D');
        st.equal(qs.stringify({ a: { b: null ***REMOVED*** ***REMOVED***, { strictNullHandling: false ***REMOVED***), 'a%5Bb%5D=');

        st.end();
    ***REMOVED***);

    t.test('stringifies an empty array in different arrayFormat', function (st) {
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false ***REMOVED***), 'b[0]=&c=c');
        // arrayFormat default
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'indices' ***REMOVED***), 'b[0]=&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'brackets' ***REMOVED***), 'b[]=&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'repeat' ***REMOVED***), 'b=&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'comma' ***REMOVED***), 'b=&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'comma', commaRoundTrip: true ***REMOVED***), 'b[]=&c=c');
        // with strictNullHandling
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'indices', strictNullHandling: true ***REMOVED***), 'b[0]&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'brackets', strictNullHandling: true ***REMOVED***), 'b[]&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'repeat', strictNullHandling: true ***REMOVED***), 'b&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'comma', strictNullHandling: true ***REMOVED***), 'b&c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'comma', strictNullHandling: true, commaRoundTrip: true ***REMOVED***), 'b[]&c=c');
        // with skipNulls
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'indices', skipNulls: true ***REMOVED***), 'c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'brackets', skipNulls: true ***REMOVED***), 'c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'repeat', skipNulls: true ***REMOVED***), 'c=c');
        st.equal(qs.stringify({ a: [], b: [null], c: 'c' ***REMOVED***, { encode: false, arrayFormat: 'comma', skipNulls: true ***REMOVED***), 'c=c');

        st.end();
    ***REMOVED***);

    t.test('stringifies a null object', { skip: !Object.create ***REMOVED***, function (st) {
        var obj = Object.create(null);
        obj.a = 'b';
        st.equal(qs.stringify(obj), 'a=b');
        st.end();
    ***REMOVED***);

    t.test('returns an empty string for invalid input', function (st) {
        st.equal(qs.stringify(undefined), '');
        st.equal(qs.stringify(false), '');
        st.equal(qs.stringify(null), '');
        st.equal(qs.stringify(''), '');
        st.end();
    ***REMOVED***);

    t.test('stringifies an object with a null object as a child', { skip: !Object.create ***REMOVED***, function (st) {
        var obj = { a: Object.create(null) ***REMOVED***;

        obj.a.b = 'c';
        st.equal(qs.stringify(obj), 'a%5Bb%5D=c');
        st.end();
    ***REMOVED***);

    t.test('drops keys with a value of undefined', function (st) {
        st.equal(qs.stringify({ a: undefined ***REMOVED***), '');

        st.equal(qs.stringify({ a: { b: undefined, c: null ***REMOVED*** ***REMOVED***, { strictNullHandling: true ***REMOVED***), 'a%5Bc%5D');
        st.equal(qs.stringify({ a: { b: undefined, c: null ***REMOVED*** ***REMOVED***, { strictNullHandling: false ***REMOVED***), 'a%5Bc%5D=');
        st.equal(qs.stringify({ a: { b: undefined, c: '' ***REMOVED*** ***REMOVED***), 'a%5Bc%5D=');
        st.end();
    ***REMOVED***);

    t.test('url encodes values', function (st) {
        st.equal(qs.stringify({ a: 'b c' ***REMOVED***), 'a=b%20c');
        st.end();
    ***REMOVED***);

    t.test('stringifies a date', function (st) {
        var now = new Date();
        var str = 'a=' + encodeURIComponent(now.toISOString());
        st.equal(qs.stringify({ a: now ***REMOVED***), str);
        st.end();
    ***REMOVED***);

    t.test('stringifies the weird object from qs', function (st) {
        st.equal(qs.stringify({ 'my weird field': '~q1!2"\'w$5&7/z8)?' ***REMOVED***), 'my%20weird%20field=~q1%212%22%27w%245%267%2Fz8%29%3F');
        st.end();
    ***REMOVED***);

    t.test('skips properties that are part of the object prototype', function (st) {
        st.intercept(Object.prototype, 'crash', { value: 'test' ***REMOVED***);

        st.equal(qs.stringify({ a: 'b' ***REMOVED***), 'a=b');
        st.equal(qs.stringify({ a: { b: 'c' ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=c');

        st.end();
    ***REMOVED***);

    t.test('stringifies boolean values', function (st) {
        st.equal(qs.stringify({ a: true ***REMOVED***), 'a=true');
        st.equal(qs.stringify({ a: { b: true ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=true');
        st.equal(qs.stringify({ b: false ***REMOVED***), 'b=false');
        st.equal(qs.stringify({ b: { c: false ***REMOVED*** ***REMOVED***), 'b%5Bc%5D=false');
        st.end();
    ***REMOVED***);

    t.test('stringifies buffer values', function (st) {
        st.equal(qs.stringify({ a: SaferBuffer.from('test') ***REMOVED***), 'a=test');
        st.equal(qs.stringify({ a: { b: SaferBuffer.from('test') ***REMOVED*** ***REMOVED***), 'a%5Bb%5D=test');
        st.end();
    ***REMOVED***);

    t.test('stringifies an object using an alternative delimiter', function (st) {
        st.equal(qs.stringify({ a: 'b', c: 'd' ***REMOVED***, { delimiter: ';' ***REMOVED***), 'a=b;c=d');
        st.end();
    ***REMOVED***);

    t.test('does not blow up when Buffer global is missing', function (st) {
        var restore = mockProperty(global, 'Buffer', { 'delete': true ***REMOVED***);

        var result = qs.stringify({ a: 'b', c: 'd' ***REMOVED***);

        restore();

        st.equal(result, 'a=b&c=d');
        st.end();
    ***REMOVED***);

    t.test('does not crash when parsing circular references', function (st) {
        var a = {***REMOVED***;
        a.b = a;

        st['throws'](
            function () { qs.stringify({ 'foo[bar]': 'baz', 'foo[baz]': a ***REMOVED***); ***REMOVED***,
            /RangeError: Cyclic object value/,
            'cyclic values throw'
        );

        var circular = {
            a: 'value'
        ***REMOVED***;
        circular.a = circular;
        st['throws'](
            function () { qs.stringify(circular); ***REMOVED***,
            /RangeError: Cyclic object value/,
            'cyclic values throw'
        );

        var arr = ['a'];
        st.doesNotThrow(
            function () { qs.stringify({ x: arr, y: arr ***REMOVED***); ***REMOVED***,
            'non-cyclic values do not throw'
        );

        st.end();
    ***REMOVED***);

    t.test('non-circular duplicated references can still work', function (st) {
        var hourOfDay = {
            'function': 'hour_of_day'
        ***REMOVED***;

        var p1 = {
            'function': 'gte',
            arguments: [hourOfDay, 0]
        ***REMOVED***;
        var p2 = {
            'function': 'lte',
            arguments: [hourOfDay, 23]
        ***REMOVED***;

        st.equal(
            qs.stringify({ filters: { $and: [p1, p2] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***),
            'filters[$and][0][function]=gte&filters[$and][0][arguments][0][function]=hour_of_day&filters[$and][0][arguments][1]=0&filters[$and][1][function]=lte&filters[$and][1][arguments][0][function]=hour_of_day&filters[$and][1][arguments][1]=23'
        );
        st.equal(
            qs.stringify({ filters: { $and: [p1, p2] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***),
            'filters[$and][][function]=gte&filters[$and][][arguments][][function]=hour_of_day&filters[$and][][arguments][]=0&filters[$and][][function]=lte&filters[$and][][arguments][][function]=hour_of_day&filters[$and][][arguments][]=23'
        );
        st.equal(
            qs.stringify({ filters: { $and: [p1, p2] ***REMOVED*** ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***),
            'filters[$and][function]=gte&filters[$and][arguments][function]=hour_of_day&filters[$and][arguments]=0&filters[$and][function]=lte&filters[$and][arguments][function]=hour_of_day&filters[$and][arguments]=23'
        );

        st.end();
    ***REMOVED***);

    t.test('selects properties when filter=array', function (st) {
        st.equal(qs.stringify({ a: 'b' ***REMOVED***, { filter: ['a'] ***REMOVED***), 'a=b');
        st.equal(qs.stringify({ a: 1 ***REMOVED***, { filter: [] ***REMOVED***), '');

        st.equal(
            qs.stringify(
                { a: { b: [1, 2, 3, 4], c: 'd' ***REMOVED***, c: 'f' ***REMOVED***,
                { filter: ['a', 'b', 0, 2], arrayFormat: 'indices' ***REMOVED***
            ),
            'a%5Bb%5D%5B0%5D=1&a%5Bb%5D%5B2%5D=3',
            'indices => indices'
        );
        st.equal(
            qs.stringify(
                { a: { b: [1, 2, 3, 4], c: 'd' ***REMOVED***, c: 'f' ***REMOVED***,
                { filter: ['a', 'b', 0, 2], arrayFormat: 'brackets' ***REMOVED***
            ),
            'a%5Bb%5D%5B%5D=1&a%5Bb%5D%5B%5D=3',
            'brackets => brackets'
        );
        st.equal(
            qs.stringify(
                { a: { b: [1, 2, 3, 4], c: 'd' ***REMOVED***, c: 'f' ***REMOVED***,
                { filter: ['a', 'b', 0, 2] ***REMOVED***
            ),
            'a%5Bb%5D%5B0%5D=1&a%5Bb%5D%5B2%5D=3',
            'default => indices'
        );

        st.end();
    ***REMOVED***);

    t.test('supports custom representations when filter=function', function (st) {
        var calls = 0;
        var obj = { a: 'b', c: 'd', e: { f: new Date(1257894000000) ***REMOVED*** ***REMOVED***;
        var filterFunc = function (prefix, value) {
            calls += 1;
            if (calls === 1) {
                st.equal(prefix, '', 'prefix is empty');
                st.equal(value, obj);
            ***REMOVED*** else if (prefix === 'c') {
                return void 0;
            ***REMOVED*** else if (value instanceof Date) {
                st.equal(prefix, 'e[f]');
                return value.getTime();
            ***REMOVED***
            return value;
        ***REMOVED***;

        st.equal(qs.stringify(obj, { filter: filterFunc ***REMOVED***), 'a=b&e%5Bf%5D=1257894000000');
        st.equal(calls, 5);
        st.end();
    ***REMOVED***);

    t.test('can disable uri encoding', function (st) {
        st.equal(qs.stringify({ a: 'b' ***REMOVED***, { encode: false ***REMOVED***), 'a=b');
        st.equal(qs.stringify({ a: { b: 'c' ***REMOVED*** ***REMOVED***, { encode: false ***REMOVED***), 'a[b]=c');
        st.equal(qs.stringify({ a: 'b', c: null ***REMOVED***, { strictNullHandling: true, encode: false ***REMOVED***), 'a=b&c');
        st.end();
    ***REMOVED***);

    t.test('can sort the keys', function (st) {
        var sort = function (a, b) {
            return a.localeCompare(b);
        ***REMOVED***;
        st.equal(qs.stringify({ a: 'c', z: 'y', b: 'f' ***REMOVED***, { sort: sort ***REMOVED***), 'a=c&b=f&z=y');
        st.equal(qs.stringify({ a: 'c', z: { j: 'a', i: 'b' ***REMOVED***, b: 'f' ***REMOVED***, { sort: sort ***REMOVED***), 'a=c&b=f&z%5Bi%5D=b&z%5Bj%5D=a');
        st.end();
    ***REMOVED***);

    t.test('can sort the keys at depth 3 or more too', function (st) {
        var sort = function (a, b) {
            return a.localeCompare(b);
        ***REMOVED***;
        st.equal(
            qs.stringify(
                { a: 'a', z: { zj: { zjb: 'zjb', zja: 'zja' ***REMOVED***, zi: { zib: 'zib', zia: 'zia' ***REMOVED*** ***REMOVED***, b: 'b' ***REMOVED***,
                { sort: sort, encode: false ***REMOVED***
            ),
            'a=a&b=b&z[zi][zia]=zia&z[zi][zib]=zib&z[zj][zja]=zja&z[zj][zjb]=zjb'
        );
        st.equal(
            qs.stringify(
                { a: 'a', z: { zj: { zjb: 'zjb', zja: 'zja' ***REMOVED***, zi: { zib: 'zib', zia: 'zia' ***REMOVED*** ***REMOVED***, b: 'b' ***REMOVED***,
                { sort: null, encode: false ***REMOVED***
            ),
            'a=a&z[zj][zjb]=zjb&z[zj][zja]=zja&z[zi][zib]=zib&z[zi][zia]=zia&b=b'
        );
        st.end();
    ***REMOVED***);

    t.test('can stringify with custom encoding', function (st) {
        st.equal(qs.stringify({ 県: '大阪府', '': '' ***REMOVED***, {
            encoder: function (str) {
                if (str.length === 0) {
                    return '';
                ***REMOVED***
                var buf = iconv.encode(str, 'shiftjis');
                var result = [];
                for (var i = 0; i < buf.length; ++i) {
                    result.push(buf.readUInt8(i).toString(16));
                ***REMOVED***
                return '%' + result.join('%');
            ***REMOVED***
        ***REMOVED***), '%8c%a7=%91%e5%8d%e3%95%7b&=');
        st.end();
    ***REMOVED***);

    t.test('receives the default encoder as a second argument', function (st) {
        st.plan(8);

        qs.stringify({ a: 1, b: new Date(), c: true, d: [1] ***REMOVED***, {
            encoder: function (str) {
                st.match(typeof str, /^(?:string|number|boolean)$/);
                return '';
            ***REMOVED***
        ***REMOVED***);

        st.end();
    ***REMOVED***);

    t.test('receives the default encoder as a second argument', function (st) {
        st.plan(2);

        qs.stringify({ a: 1 ***REMOVED***, {
            encoder: function (str, defaultEncoder) {
                st.equal(defaultEncoder, utils.encode);
            ***REMOVED***
        ***REMOVED***);

        st.end();
    ***REMOVED***);

    t.test('throws error with wrong encoder', function (st) {
        st['throws'](function () {
            qs.stringify({***REMOVED***, { encoder: 'string' ***REMOVED***);
        ***REMOVED***, new TypeError('Encoder has to be a function.'));
        st.end();
    ***REMOVED***);

    t.test('can use custom encoder for a buffer object', { skip: typeof Buffer === 'undefined' ***REMOVED***, function (st) {
        st.equal(qs.stringify({ a: SaferBuffer.from([1]) ***REMOVED***, {
            encoder: function (buffer) {
                if (typeof buffer === 'string') {
                    return buffer;
                ***REMOVED***
                return String.fromCharCode(buffer.readUInt8(0) + 97);
            ***REMOVED***
        ***REMOVED***), 'a=b');

        st.equal(qs.stringify({ a: SaferBuffer.from('a b') ***REMOVED***, {
            encoder: function (buffer) {
                return buffer;
            ***REMOVED***
        ***REMOVED***), 'a=a b');
        st.end();
    ***REMOVED***);

    t.test('serializeDate option', function (st) {
        var date = new Date();
        st.equal(
            qs.stringify({ a: date ***REMOVED***),
            'a=' + date.toISOString().replace(/:/g, '%3A'),
            'default is toISOString'
        );

        var mutatedDate = new Date();
        mutatedDate.toISOString = function () {
            throw new SyntaxError();
        ***REMOVED***;
        st['throws'](function () {
            mutatedDate.toISOString();
        ***REMOVED***, SyntaxError);
        st.equal(
            qs.stringify({ a: mutatedDate ***REMOVED***),
            'a=' + Date.prototype.toISOString.call(mutatedDate).replace(/:/g, '%3A'),
            'toISOString works even when method is not locally present'
        );

        var specificDate = new Date(6);
        st.equal(
            qs.stringify(
                { a: specificDate ***REMOVED***,
                { serializeDate: function (d) { return d.getTime() * 7; ***REMOVED*** ***REMOVED***
            ),
            'a=42',
            'custom serializeDate function called'
        );

        st.equal(
            qs.stringify(
                { a: [date] ***REMOVED***,
                {
                    serializeDate: function (d) { return d.getTime(); ***REMOVED***,
                    arrayFormat: 'comma'
                ***REMOVED***
            ),
            'a=' + date.getTime(),
            'works with arrayFormat comma'
        );
        st.equal(
            qs.stringify(
                { a: [date] ***REMOVED***,
                {
                    serializeDate: function (d) { return d.getTime(); ***REMOVED***,
                    arrayFormat: 'comma',
                    commaRoundTrip: true
                ***REMOVED***
            ),
            'a%5B%5D=' + date.getTime(),
            'works with arrayFormat comma'
        );

        st.end();
    ***REMOVED***);

    t.test('RFC 1738 serialization', function (st) {
        st.equal(qs.stringify({ a: 'b c' ***REMOVED***, { format: qs.formats.RFC1738 ***REMOVED***), 'a=b+c');
        st.equal(qs.stringify({ 'a b': 'c d' ***REMOVED***, { format: qs.formats.RFC1738 ***REMOVED***), 'a+b=c+d');
        st.equal(qs.stringify({ 'a b': SaferBuffer.from('a b') ***REMOVED***, { format: qs.formats.RFC1738 ***REMOVED***), 'a+b=a+b');

        st.equal(qs.stringify({ 'foo(ref)': 'bar' ***REMOVED***, { format: qs.formats.RFC1738 ***REMOVED***), 'foo(ref)=bar');

        st.end();
    ***REMOVED***);

    t.test('RFC 3986 spaces serialization', function (st) {
        st.equal(qs.stringify({ a: 'b c' ***REMOVED***, { format: qs.formats.RFC3986 ***REMOVED***), 'a=b%20c');
        st.equal(qs.stringify({ 'a b': 'c d' ***REMOVED***, { format: qs.formats.RFC3986 ***REMOVED***), 'a%20b=c%20d');
        st.equal(qs.stringify({ 'a b': SaferBuffer.from('a b') ***REMOVED***, { format: qs.formats.RFC3986 ***REMOVED***), 'a%20b=a%20b');

        st.end();
    ***REMOVED***);

    t.test('Backward compatibility to RFC 3986', function (st) {
        st.equal(qs.stringify({ a: 'b c' ***REMOVED***), 'a=b%20c');
        st.equal(qs.stringify({ 'a b': SaferBuffer.from('a b') ***REMOVED***), 'a%20b=a%20b');

        st.end();
    ***REMOVED***);

    t.test('Edge cases and unknown formats', function (st) {
        ['UFO1234', false, 1234, null, {***REMOVED***, []].forEach(function (format) {
            st['throws'](
                function () {
                    qs.stringify({ a: 'b c' ***REMOVED***, { format: format ***REMOVED***);
                ***REMOVED***,
                new TypeError('Unknown format option provided.')
            );
        ***REMOVED***);
        st.end();
    ***REMOVED***);

    t.test('encodeValuesOnly', function (st) {
        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e=f'], f: [['g'], ['h']] ***REMOVED***,
                { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***
            ),
            'a=b&c[0]=d&c[1]=e%3Df&f[0][0]=g&f[1][0]=h',
            'encodeValuesOnly + indices'
        );
        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e=f'], f: [['g'], ['h']] ***REMOVED***,
                { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***
            ),
            'a=b&c[]=d&c[]=e%3Df&f[][]=g&f[][]=h',
            'encodeValuesOnly + brackets'
        );
        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e=f'], f: [['g'], ['h']] ***REMOVED***,
                { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***
            ),
            'a=b&c=d&c=e%3Df&f=g&f=h',
            'encodeValuesOnly + repeat'
        );

        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e'], f: [['g'], ['h']] ***REMOVED***,
                { arrayFormat: 'indices' ***REMOVED***
            ),
            'a=b&c%5B0%5D=d&c%5B1%5D=e&f%5B0%5D%5B0%5D=g&f%5B1%5D%5B0%5D=h',
            'no encodeValuesOnly + indices'
        );
        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e'], f: [['g'], ['h']] ***REMOVED***,
                { arrayFormat: 'brackets' ***REMOVED***
            ),
            'a=b&c%5B%5D=d&c%5B%5D=e&f%5B%5D%5B%5D=g&f%5B%5D%5B%5D=h',
            'no encodeValuesOnly + brackets'
        );
        st.equal(
            qs.stringify(
                { a: 'b', c: ['d', 'e'], f: [['g'], ['h']] ***REMOVED***,
                { arrayFormat: 'repeat' ***REMOVED***
            ),
            'a=b&c=d&c=e&f=g&f=h',
            'no encodeValuesOnly + repeat'
        );

        st.end();
    ***REMOVED***);

    t.test('encodeValuesOnly - strictNullHandling', function (st) {
        st.equal(
            qs.stringify(
                { a: { b: null ***REMOVED*** ***REMOVED***,
                { encodeValuesOnly: true, strictNullHandling: true ***REMOVED***
            ),
            'a[b]'
        );
        st.end();
    ***REMOVED***);

    t.test('throws if an invalid charset is specified', function (st) {
        st['throws'](function () {
            qs.stringify({ a: 'b' ***REMOVED***, { charset: 'foobar' ***REMOVED***);
        ***REMOVED***, new TypeError('The charset option must be either utf-8, iso-8859-1, or undefined'));
        st.end();
    ***REMOVED***);

    t.test('respects a charset of iso-8859-1', function (st) {
        st.equal(qs.stringify({ æ: 'æ' ***REMOVED***, { charset: 'iso-8859-1' ***REMOVED***), '%E6=%E6');
        st.end();
    ***REMOVED***);

    t.test('encodes unrepresentable chars as numeric entities in iso-8859-1 mode', function (st) {
        st.equal(qs.stringify({ a: '☺' ***REMOVED***, { charset: 'iso-8859-1' ***REMOVED***), 'a=%26%239786%3B');
        st.end();
    ***REMOVED***);

    t.test('respects an explicit charset of utf-8 (the default)', function (st) {
        st.equal(qs.stringify({ a: 'æ' ***REMOVED***, { charset: 'utf-8' ***REMOVED***), 'a=%C3%A6');
        st.end();
    ***REMOVED***);

    t.test('`charsetSentinel` option', function (st) {
        st.equal(
            qs.stringify({ a: 'æ' ***REMOVED***, { charsetSentinel: true, charset: 'utf-8' ***REMOVED***),
            'utf8=%E2%9C%93&a=%C3%A6',
            'adds the right sentinel when instructed to and the charset is utf-8'
        );

        st.equal(
            qs.stringify({ a: 'æ' ***REMOVED***, { charsetSentinel: true, charset: 'iso-8859-1' ***REMOVED***),
            'utf8=%26%2310003%3B&a=%E6',
            'adds the right sentinel when instructed to and the charset is iso-8859-1'
        );

        st.end();
    ***REMOVED***);

    t.test('does not mutate the options argument', function (st) {
        var options = {***REMOVED***;
        qs.stringify({***REMOVED***, options);
        st.deepEqual(options, {***REMOVED***);
        st.end();
    ***REMOVED***);

    t.test('strictNullHandling works with custom filter', function (st) {
        var filter = function (prefix, value) {
            return value;
        ***REMOVED***;

        var options = { strictNullHandling: true, filter: filter ***REMOVED***;
        st.equal(qs.stringify({ key: null ***REMOVED***, options), 'key');
        st.end();
    ***REMOVED***);

    t.test('strictNullHandling works with null serializeDate', function (st) {
        var serializeDate = function () {
            return null;
        ***REMOVED***;
        var options = { strictNullHandling: true, serializeDate: serializeDate ***REMOVED***;
        var date = new Date();
        st.equal(qs.stringify({ key: date ***REMOVED***, options), 'key');
        st.end();
    ***REMOVED***);

    t.test('allows for encoding keys and values differently', function (st) {
        var encoder = function (str, defaultEncoder, charset, type) {
            if (type === 'key') {
                return defaultEncoder(str, defaultEncoder, charset, type).toLowerCase();
            ***REMOVED***
            if (type === 'value') {
                return defaultEncoder(str, defaultEncoder, charset, type).toUpperCase();
            ***REMOVED***
            throw 'this should never happen! type: ' + type;
        ***REMOVED***;

        st.deepEqual(qs.stringify({ KeY: 'vAlUe' ***REMOVED***, { encoder: encoder ***REMOVED***), 'key=VALUE');
        st.end();
    ***REMOVED***);

    t.test('objects inside arrays', function (st) {
        var obj = { a: { b: { c: 'd', e: 'f' ***REMOVED*** ***REMOVED*** ***REMOVED***;
        var withArray = { a: { b: [{ c: 'd', e: 'f' ***REMOVED***] ***REMOVED*** ***REMOVED***;

        st.equal(qs.stringify(obj, { encode: false ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'no array, no arrayFormat');
        st.equal(qs.stringify(obj, { encode: false, arrayFormat: 'brackets' ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'no array, bracket');
        st.equal(qs.stringify(obj, { encode: false, arrayFormat: 'indices' ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'no array, indices');
        st.equal(qs.stringify(obj, { encode: false, arrayFormat: 'repeat' ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'no array, repeat');
        st.equal(qs.stringify(obj, { encode: false, arrayFormat: 'comma' ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'no array, comma');

        st.equal(qs.stringify(withArray, { encode: false ***REMOVED***), 'a[b][0][c]=d&a[b][0][e]=f', 'array, no arrayFormat');
        st.equal(qs.stringify(withArray, { encode: false, arrayFormat: 'brackets' ***REMOVED***), 'a[b][][c]=d&a[b][][e]=f', 'array, bracket');
        st.equal(qs.stringify(withArray, { encode: false, arrayFormat: 'indices' ***REMOVED***), 'a[b][0][c]=d&a[b][0][e]=f', 'array, indices');
        st.equal(qs.stringify(withArray, { encode: false, arrayFormat: 'repeat' ***REMOVED***), 'a[b][c]=d&a[b][e]=f', 'array, repeat');
        st.equal(
            qs.stringify(withArray, { encode: false, arrayFormat: 'comma' ***REMOVED***),
            '???',
            'array, comma',
            { skip: 'TODO: figure out what this should do' ***REMOVED***
        );

        st.end();
    ***REMOVED***);

    t.test('stringifies sparse arrays', function (st) {
        /* eslint no-sparse-arrays: 0 */
        st.equal(qs.stringify({ a: [, '2', , , '1'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[1]=2&a[4]=1');
        st.equal(qs.stringify({ a: [, '2', , , '1'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[]=2&a[]=1');
        st.equal(qs.stringify({ a: [, '2', , , '1'] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a=2&a=1');

        st.equal(qs.stringify({ a: [, { b: [, , { c: '1' ***REMOVED***] ***REMOVED***] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[1][b][2][c]=1');
        st.equal(qs.stringify({ a: [, { b: [, , { c: '1' ***REMOVED***] ***REMOVED***] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[][b][][c]=1');
        st.equal(qs.stringify({ a: [, { b: [, , { c: '1' ***REMOVED***] ***REMOVED***] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a[b][c]=1');

        st.equal(qs.stringify({ a: [, [, , [, , , { c: '1' ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[1][2][3][c]=1');
        st.equal(qs.stringify({ a: [, [, , [, , , { c: '1' ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[][][][c]=1');
        st.equal(qs.stringify({ a: [, [, , [, , , { c: '1' ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a[c]=1');

        st.equal(qs.stringify({ a: [, [, , [, , , { c: [, '1'] ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'indices' ***REMOVED***), 'a[1][2][3][c][1]=1');
        st.equal(qs.stringify({ a: [, [, , [, , , { c: [, '1'] ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'brackets' ***REMOVED***), 'a[][][][c][]=1');
        st.equal(qs.stringify({ a: [, [, , [, , , { c: [, '1'] ***REMOVED***]]] ***REMOVED***, { encodeValuesOnly: true, arrayFormat: 'repeat' ***REMOVED***), 'a[c]=1');

        st.end();
    ***REMOVED***);

    t.test('encodes a very long string', function (st) {
        var chars = [];
        var expected = [];
        for (var i = 0; i < 5e3; i++) {
            chars.push(' ' + i);

            expected.push('%20' + i);
        ***REMOVED***

        var obj = {
            foo: chars.join('')
        ***REMOVED***;

        st.equal(
            qs.stringify(obj, { arrayFormat: 'bracket', charset: 'utf-8' ***REMOVED***),
            'foo=' + expected.join('')
        );

        st.end();
    ***REMOVED***);

    t.end();
***REMOVED***);

test('stringifies empty keys', function (t) {
    emptyTestCases.forEach(function (testCase) {
        t.test('stringifies an object with empty string key with ' + testCase.input, function (st) {
            st.deepEqual(
                qs.stringify(testCase.withEmptyKeys, { encode: false, arrayFormat: 'indices' ***REMOVED***),
                testCase.stringifyOutput.indices,
                'test case: ' + testCase.input + ', indices'
            );
            st.deepEqual(
                qs.stringify(testCase.withEmptyKeys, { encode: false, arrayFormat: 'brackets' ***REMOVED***),
                testCase.stringifyOutput.brackets,
                'test case: ' + testCase.input + ', brackets'
            );
            st.deepEqual(
                qs.stringify(testCase.withEmptyKeys, { encode: false, arrayFormat: 'repeat' ***REMOVED***),
                testCase.stringifyOutput.repeat,
                'test case: ' + testCase.input + ', repeat'
            );

            st.end();
        ***REMOVED***);
    ***REMOVED***);

    t.test('edge case with object/arrays', function (st) {
        st.deepEqual(qs.stringify({ '': { '': [2, 3] ***REMOVED*** ***REMOVED***, { encode: false ***REMOVED***), '[][0]=2&[][1]=3');
        st.deepEqual(qs.stringify({ '': { '': [2, 3], a: 2 ***REMOVED*** ***REMOVED***, { encode: false ***REMOVED***), '[][0]=2&[][1]=3&[a]=2');
        st.deepEqual(qs.stringify({ '': { '': [2, 3] ***REMOVED*** ***REMOVED***, { encode: false, arrayFormat: 'indices' ***REMOVED***), '[][0]=2&[][1]=3');
        st.deepEqual(qs.stringify({ '': { '': [2, 3], a: 2 ***REMOVED*** ***REMOVED***, { encode: false, arrayFormat: 'indices' ***REMOVED***), '[][0]=2&[][1]=3&[a]=2');

        st.end();
    ***REMOVED***);
***REMOVED***);

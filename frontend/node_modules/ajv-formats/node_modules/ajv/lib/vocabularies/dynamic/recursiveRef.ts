import type {CodeKeywordDefinition***REMOVED*** from "../../types"
import {dynamicRef***REMOVED*** from "./dynamicRef"

const def: CodeKeywordDefinition = {
  keyword: "$recursiveRef",
  schemaType: "string",
  code: (cxt) => dynamicRef(cxt, cxt.schema),
***REMOVED***

export default def

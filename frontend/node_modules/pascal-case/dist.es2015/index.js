import { __assign ***REMOVED*** from "tslib";
import { noCase ***REMOVED*** from "no-case";
export function pascalCaseTransform(input, index) {
    var firstChar = input.charAt(0);
    var lowerChars = input.substr(1).toLowerCase();
    if (index > 0 && firstChar >= "0" && firstChar <= "9") {
        return "_" + firstChar + lowerChars;
    ***REMOVED***
    return "" + firstChar.toUpperCase() + lowerChars;
***REMOVED***
export function pascalCaseTransformMerge(input) {
    return input.charAt(0).toUpperCase() + input.slice(1).toLowerCase();
***REMOVED***
export function pascalCase(input, options) {
    if (options === void 0) { options = {***REMOVED***; ***REMOVED***
    return noCase(input, __assign({ delimiter: "", transform: pascalCaseTransform ***REMOVED***, options));
***REMOVED***
//# sourceMappingURL=index.js.map
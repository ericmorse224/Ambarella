'use strict';

var flat = require('../implementation');
var callBind = require('call-bind');
var test = require('tape');
var hasStrictMode = require('has-strict-mode')();
var runTests = require('./tests');

test('as a function', function (t) {
	t.test('bad array/this value', { skip: !hasStrictMode ***REMOVED***, function (st) {
		/* eslint no-useless-call: 0 */
		st['throws'](function () { flat.call(undefined); ***REMOVED***, TypeError, 'undefined is not an object');
		st['throws'](function () { flat.call(null); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(callBind(flat), t);

	t.end();
***REMOVED***);

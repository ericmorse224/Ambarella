import stripAnsi from 'strip-ansi';
import charRegex from 'char-regex';

export default function stringLength(string, {countAnsiEscapeCodes = false***REMOVED*** = {***REMOVED***) {
	if (string === '') {
		return 0;
	***REMOVED***

	if (!countAnsiEscapeCodes) {
		string = stripAnsi(string);
	***REMOVED***

	if (string === '') {
		return 0;
	***REMOVED***

	return string.match(charRegex()).length;
***REMOVED***

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Ivan Kopeykin @vankop
*/

"use strict";

/** @typedef {string|(string|ConditionalMapping)[]***REMOVED*** DirectMapping */
/** @typedef {{[k: string]: MappingValue***REMOVED******REMOVED*** ConditionalMapping */
/** @typedef {ConditionalMapping|DirectMapping|null***REMOVED*** MappingValue */
/** @typedef {Record<string, MappingValue>|ConditionalMapping|DirectMapping***REMOVED*** ExportsField */
/** @typedef {Record<string, MappingValue>***REMOVED*** ImportsField */

/**
 * Processing exports/imports field
 * @callback FieldProcessor
 * @param {string***REMOVED*** request request
 * @param {Set<string>***REMOVED*** conditionNames condition names
 * @returns {[string[], string | null]***REMOVED*** resolved paths with used field
 */

/*
Example exports field:
{
  ".": "./main.js",
  "./feature": {
    "browser": "./feature-browser.js",
    "default": "./feature.js"
  ***REMOVED***
***REMOVED***
Terminology:

Enhanced-resolve name keys ("." and "./feature") as exports field keys.

If value is string or string[], mapping is called as a direct mapping
and value called as a direct export.

If value is key-value object, mapping is called as a conditional mapping
and value called as a conditional export.

Key in conditional mapping is called condition name.

Conditional mapping nested in another conditional mapping is called nested mapping.

----------

Example imports field:
{
  "#a": "./main.js",
  "#moment": {
    "browser": "./moment/index.js",
    "default": "moment"
  ***REMOVED***,
  "#moment/": {
    "browser": "./moment/",
    "default": "moment/"
  ***REMOVED***
***REMOVED***
Terminology:

Enhanced-resolve name keys ("#a" and "#moment/", "#moment") as imports field keys.

If value is string or string[], mapping is called as a direct mapping
and value called as a direct export.

If value is key-value object, mapping is called as a conditional mapping
and value called as a conditional export.

Key in conditional mapping is called condition name.

Conditional mapping nested in another conditional mapping is called nested mapping.

*/

const { parseIdentifier ***REMOVED*** = require("./identifier");
const slashCode = "/".charCodeAt(0);
const dotCode = ".".charCodeAt(0);
const hashCode = "#".charCodeAt(0);
const patternRegEx = /\*/g;

/**
 * @param {ExportsField***REMOVED*** exportsField the exports field
 * @returns {FieldProcessor***REMOVED*** process callback
 */
module.exports.processExportsField = function processExportsField(
	exportsField
) {
	return createFieldProcessor(
		buildExportsField(exportsField),
		request => (request.length === 0 ? "." : "./" + request),
		assertExportsFieldRequest,
		assertExportTarget
	);
***REMOVED***;

/**
 * @param {ImportsField***REMOVED*** importsField the exports field
 * @returns {FieldProcessor***REMOVED*** process callback
 */
module.exports.processImportsField = function processImportsField(
	importsField
) {
	return createFieldProcessor(
		importsField,
		request => "#" + request,
		assertImportsFieldRequest,
		assertImportTarget
	);
***REMOVED***;

/**
 * @param {ExportsField | ImportsField***REMOVED*** field root
 * @param {(s: string) => string***REMOVED*** normalizeRequest Normalize request, for `imports` field it adds `#`, for `exports` field it adds `.` or `./`
 * @param {(s: string) => string***REMOVED*** assertRequest assertRequest
 * @param {(s: string, f: boolean) => void***REMOVED*** assertTarget assertTarget
 * @returns {FieldProcessor***REMOVED*** field processor
 */
function createFieldProcessor(
	field,
	normalizeRequest,
	assertRequest,
	assertTarget
) {
	return function fieldProcessor(request, conditionNames) {
		request = assertRequest(request);

		const match = findMatch(normalizeRequest(request), field);

		if (match === null) return [[], null];

		const [mapping, remainingRequest, isSubpathMapping, isPattern, usedField] =
			match;

		/** @type {DirectMapping|null***REMOVED*** */
		let direct = null;

		if (isConditionalMapping(mapping)) {
			direct = conditionalMapping(
				/** @type {ConditionalMapping***REMOVED*** */ (mapping),
				conditionNames
			);

			// matching not found
			if (direct === null) return [[], null];
		***REMOVED*** else {
			direct = /** @type {DirectMapping***REMOVED*** */ (mapping);
		***REMOVED***

		return [
			directMapping(
				remainingRequest,
				isPattern,
				isSubpathMapping,
				direct,
				conditionNames,
				assertTarget
			),
			usedField
		];
	***REMOVED***;
***REMOVED***

/**
 * @param {string***REMOVED*** request request
 * @returns {string***REMOVED*** updated request
 */
function assertExportsFieldRequest(request) {
	if (request.charCodeAt(0) !== dotCode) {
		throw new Error('Request should be relative path and start with "."');
	***REMOVED***
	if (request.length === 1) return "";
	if (request.charCodeAt(1) !== slashCode) {
		throw new Error('Request should be relative path and start with "./"');
	***REMOVED***
	if (request.charCodeAt(request.length - 1) === slashCode) {
		throw new Error("Only requesting file allowed");
	***REMOVED***

	return request.slice(2);
***REMOVED***

/**
 * @param {string***REMOVED*** request request
 * @returns {string***REMOVED*** updated request
 */
function assertImportsFieldRequest(request) {
	if (request.charCodeAt(0) !== hashCode) {
		throw new Error('Request should start with "#"');
	***REMOVED***
	if (request.length === 1) {
		throw new Error("Request should have at least 2 characters");
	***REMOVED***
	if (request.charCodeAt(1) === slashCode) {
		throw new Error('Request should not start with "#/"');
	***REMOVED***
	if (request.charCodeAt(request.length - 1) === slashCode) {
		throw new Error("Only requesting file allowed");
	***REMOVED***

	return request.slice(1);
***REMOVED***

/**
 * @param {string***REMOVED*** exp export target
 * @param {boolean***REMOVED*** expectFolder is folder expected
 */
function assertExportTarget(exp, expectFolder) {
	const parsedIdentifier = parseIdentifier(exp);

	if (!parsedIdentifier) {
		return;
	***REMOVED***

	const [relativePath] = parsedIdentifier;
	const isFolder =
		relativePath.charCodeAt(relativePath.length - 1) === slashCode;

	if (isFolder !== expectFolder) {
		throw new Error(
			expectFolder
				? `Expecting folder to folder mapping. ${JSON.stringify(
						exp
				  )***REMOVED*** should end with "/"`
				: `Expecting file to file mapping. ${JSON.stringify(
						exp
				  )***REMOVED*** should not end with "/"`
		);
	***REMOVED***
***REMOVED***

/**
 * @param {string***REMOVED*** imp import target
 * @param {boolean***REMOVED*** expectFolder is folder expected
 */
function assertImportTarget(imp, expectFolder) {
	const parsedIdentifier = parseIdentifier(imp);

	if (!parsedIdentifier) {
		return;
	***REMOVED***

	const [relativePath] = parsedIdentifier;
	const isFolder =
		relativePath.charCodeAt(relativePath.length - 1) === slashCode;

	if (isFolder !== expectFolder) {
		throw new Error(
			expectFolder
				? `Expecting folder to folder mapping. ${JSON.stringify(
						imp
				  )***REMOVED*** should end with "/"`
				: `Expecting file to file mapping. ${JSON.stringify(
						imp
				  )***REMOVED*** should not end with "/"`
		);
	***REMOVED***
***REMOVED***

/**
 * @param {string***REMOVED*** a first string
 * @param {string***REMOVED*** b second string
 * @returns {number***REMOVED*** compare result
 */
function patternKeyCompare(a, b) {
	const aPatternIndex = a.indexOf("*");
	const bPatternIndex = b.indexOf("*");
	const baseLenA = aPatternIndex === -1 ? a.length : aPatternIndex + 1;
	const baseLenB = bPatternIndex === -1 ? b.length : bPatternIndex + 1;

	if (baseLenA > baseLenB) return -1;
	if (baseLenB > baseLenA) return 1;
	if (aPatternIndex === -1) return 1;
	if (bPatternIndex === -1) return -1;
	if (a.length > b.length) return -1;
	if (b.length > a.length) return 1;

	return 0;
***REMOVED***

/**
 * Trying to match request to field
 * @param {string***REMOVED*** request request
 * @param {ExportsField | ImportsField***REMOVED*** field exports or import field
 * @returns {[MappingValue, string, boolean, boolean, string]|null***REMOVED*** match or null, number is negative and one less when it's a folder mapping, number is request.length + 1 for direct mappings
 */
function findMatch(request, field) {
	if (
		Object.prototype.hasOwnProperty.call(field, request) &&
		!request.includes("*") &&
		!request.endsWith("/")
	) {
		const target = /** @type {{[k: string]: MappingValue***REMOVED******REMOVED*** */ (field)[request];

		return [target, "", false, false, request];
	***REMOVED***

	/** @type {string***REMOVED*** */
	let bestMatch = "";
	/** @type {string|undefined***REMOVED*** */
	let bestMatchSubpath;

	const keys = Object.getOwnPropertyNames(field);

	for (let i = 0; i < keys.length; i++) {
		const key = keys[i];
		const patternIndex = key.indexOf("*");

		if (patternIndex !== -1 && request.startsWith(key.slice(0, patternIndex))) {
			const patternTrailer = key.slice(patternIndex + 1);

			if (
				request.length >= key.length &&
				request.endsWith(patternTrailer) &&
				patternKeyCompare(bestMatch, key) === 1 &&
				key.lastIndexOf("*") === patternIndex
			) {
				bestMatch = key;
				bestMatchSubpath = request.slice(
					patternIndex,
					request.length - patternTrailer.length
				);
			***REMOVED***
		***REMOVED***
		// For legacy `./foo/`
		else if (
			key[key.length - 1] === "/" &&
			request.startsWith(key) &&
			patternKeyCompare(bestMatch, key) === 1
		) {
			bestMatch = key;
			bestMatchSubpath = request.slice(key.length);
		***REMOVED***
	***REMOVED***

	if (bestMatch === "") return null;

	const target = /** @type {{[k: string]: MappingValue***REMOVED******REMOVED*** */ (field)[bestMatch];
	const isSubpathMapping = bestMatch.endsWith("/");
	const isPattern = bestMatch.includes("*");

	return [
		target,
		/** @type {string***REMOVED*** */ (bestMatchSubpath),
		isSubpathMapping,
		isPattern,
		bestMatch
	];
***REMOVED***

/**
 * @param {ConditionalMapping|DirectMapping|null***REMOVED*** mapping mapping
 * @returns {boolean***REMOVED*** is conditional mapping
 */
function isConditionalMapping(mapping) {
	return (
		mapping !== null && typeof mapping === "object" && !Array.isArray(mapping)
	);
***REMOVED***

/**
 * @param {string|undefined***REMOVED*** remainingRequest remaining request when folder mapping, undefined for file mappings
 * @param {boolean***REMOVED*** isPattern true, if mapping is a pattern (contains "*")
 * @param {boolean***REMOVED*** isSubpathMapping true, for subpath mappings
 * @param {DirectMapping|null***REMOVED*** mappingTarget direct export
 * @param {Set<string>***REMOVED*** conditionNames condition names
 * @param {(d: string, f: boolean) => void***REMOVED*** assert asserting direct value
 * @returns {string[]***REMOVED*** mapping result
 */
function directMapping(
	remainingRequest,
	isPattern,
	isSubpathMapping,
	mappingTarget,
	conditionNames,
	assert
) {
	if (mappingTarget === null) return [];

	if (typeof mappingTarget === "string") {
		return [
			targetMapping(
				remainingRequest,
				isPattern,
				isSubpathMapping,
				mappingTarget,
				assert
			)
		];
	***REMOVED***

	/** @type {string[]***REMOVED*** */
	const targets = [];

	for (const exp of mappingTarget) {
		if (typeof exp === "string") {
			targets.push(
				targetMapping(
					remainingRequest,
					isPattern,
					isSubpathMapping,
					exp,
					assert
				)
			);
			continue;
		***REMOVED***

		const mapping = conditionalMapping(exp, conditionNames);
		if (!mapping) continue;
		const innerExports = directMapping(
			remainingRequest,
			isPattern,
			isSubpathMapping,
			mapping,
			conditionNames,
			assert
		);
		for (const innerExport of innerExports) {
			targets.push(innerExport);
		***REMOVED***
	***REMOVED***

	return targets;
***REMOVED***

/**
 * @param {string|undefined***REMOVED*** remainingRequest remaining request when folder mapping, undefined for file mappings
 * @param {boolean***REMOVED*** isPattern true, if mapping is a pattern (contains "*")
 * @param {boolean***REMOVED*** isSubpathMapping true, for subpath mappings
 * @param {string***REMOVED*** mappingTarget direct export
 * @param {(d: string, f: boolean) => void***REMOVED*** assert asserting direct value
 * @returns {string***REMOVED*** mapping result
 */
function targetMapping(
	remainingRequest,
	isPattern,
	isSubpathMapping,
	mappingTarget,
	assert
) {
	if (remainingRequest === undefined) {
		assert(mappingTarget, false);

		return mappingTarget;
	***REMOVED***

	if (isSubpathMapping) {
		assert(mappingTarget, true);

		return mappingTarget + remainingRequest;
	***REMOVED***

	assert(mappingTarget, false);

	let result = mappingTarget;

	if (isPattern) {
		result = result.replace(
			patternRegEx,
			remainingRequest.replace(/\$/g, "$$")
		);
	***REMOVED***

	return result;
***REMOVED***

/**
 * @param {ConditionalMapping***REMOVED*** conditionalMapping_ conditional mapping
 * @param {Set<string>***REMOVED*** conditionNames condition names
 * @returns {DirectMapping|null***REMOVED*** direct mapping if found
 */
function conditionalMapping(conditionalMapping_, conditionNames) {
	/** @type {[ConditionalMapping, string[], number][]***REMOVED*** */
	let lookup = [[conditionalMapping_, Object.keys(conditionalMapping_), 0]];

	loop: while (lookup.length > 0) {
		const [mapping, conditions, j] = lookup[lookup.length - 1];

		for (let i = j; i < conditions.length; i++) {
			const condition = conditions[i];

			if (condition === "default") {
				const innerMapping = mapping[condition];
				// is nested
				if (isConditionalMapping(innerMapping)) {
					const conditionalMapping = /** @type {ConditionalMapping***REMOVED*** */ (
						innerMapping
					);
					lookup[lookup.length - 1][2] = i + 1;
					lookup.push([conditionalMapping, Object.keys(conditionalMapping), 0]);
					continue loop;
				***REMOVED***

				return /** @type {DirectMapping***REMOVED*** */ (innerMapping);
			***REMOVED***

			if (conditionNames.has(condition)) {
				const innerMapping = mapping[condition];
				// is nested
				if (isConditionalMapping(innerMapping)) {
					const conditionalMapping = /** @type {ConditionalMapping***REMOVED*** */ (
						innerMapping
					);
					lookup[lookup.length - 1][2] = i + 1;
					lookup.push([conditionalMapping, Object.keys(conditionalMapping), 0]);
					continue loop;
				***REMOVED***

				return /** @type {DirectMapping***REMOVED*** */ (innerMapping);
			***REMOVED***
		***REMOVED***

		lookup.pop();
	***REMOVED***

	return null;
***REMOVED***

/**
 * @param {ExportsField***REMOVED*** field exports field
 * @returns {ExportsField***REMOVED*** normalized exports field
 */
function buildExportsField(field) {
	// handle syntax sugar, if exports field is direct mapping for "."
	if (typeof field === "string" || Array.isArray(field)) {
		return { ".": field ***REMOVED***;
	***REMOVED***

	const keys = Object.keys(field);

	for (let i = 0; i < keys.length; i++) {
		const key = keys[i];

		if (key.charCodeAt(0) !== dotCode) {
			// handle syntax sugar, if exports field is conditional mapping for "."
			if (i === 0) {
				while (i < keys.length) {
					const charCode = keys[i].charCodeAt(0);
					if (charCode === dotCode || charCode === slashCode) {
						throw new Error(
							`Exports field key should be relative path and start with "." (key: ${JSON.stringify(
								key
							)***REMOVED***)`
						);
					***REMOVED***
					i++;
				***REMOVED***

				return { ".": field ***REMOVED***;
			***REMOVED***

			throw new Error(
				`Exports field key should be relative path and start with "." (key: ${JSON.stringify(
					key
				)***REMOVED***)`
			);
		***REMOVED***

		if (key.length === 1) {
			continue;
		***REMOVED***

		if (key.charCodeAt(1) !== slashCode) {
			throw new Error(
				`Exports field key should be relative path and start with "./" (key: ${JSON.stringify(
					key
				)***REMOVED***)`
			);
		***REMOVED***
	***REMOVED***

	return field;
***REMOVED***

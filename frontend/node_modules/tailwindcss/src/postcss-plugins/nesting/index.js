import { nesting ***REMOVED*** from './plugin'

export default Object.assign(
  function (opts) {
    return {
      postcssPlugin: 'tailwindcss/nesting',
      Once(root, { result ***REMOVED***) {
        return nesting(opts)(root, result)
      ***REMOVED***,
    ***REMOVED***
  ***REMOVED***,
  { postcss: true ***REMOVED***
)

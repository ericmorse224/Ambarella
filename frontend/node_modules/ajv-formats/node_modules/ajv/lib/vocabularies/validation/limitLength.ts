import type {CodeKeywordDefinition, KeywordErrorDefinition***REMOVED*** from "../../types"
import type {KeywordCxt***REMOVED*** from "../../compile/validate"
import {_, str, operators***REMOVED*** from "../../compile/codegen"
import {useFunc***REMOVED*** from "../../compile/util"
import ucs2length from "../../runtime/ucs2length"

const error: KeywordErrorDefinition = {
  message({keyword, schemaCode***REMOVED***) {
    const comp = keyword === "maxLength" ? "more" : "fewer"
    return str`must NOT have ${comp***REMOVED*** than ${schemaCode***REMOVED*** characters`
  ***REMOVED***,
  params: ({schemaCode***REMOVED***) => _`{limit: ${schemaCode***REMOVED******REMOVED***`,
***REMOVED***

const def: CodeKeywordDefinition = {
  keyword: ["maxLength", "minLength"],
  type: "string",
  schemaType: "number",
  $data: true,
  error,
  code(cxt: KeywordCxt) {
    const {keyword, data, schemaCode, it***REMOVED*** = cxt
    const op = keyword === "maxLength" ? operators.GT : operators.LT
    const len =
      it.opts.unicode === false ? _`${data***REMOVED***.length` : _`${useFunc(cxt.gen, ucs2length)***REMOVED***(${data***REMOVED***)`
    cxt.fail$data(_`${len***REMOVED*** ${op***REMOVED*** ${schemaCode***REMOVED***`)
  ***REMOVED***,
***REMOVED***

export default def

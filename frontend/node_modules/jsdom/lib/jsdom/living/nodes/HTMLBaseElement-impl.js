"use strict";
const whatwgURL = require("whatwg-url");
const HTMLElementImpl = require("./HTMLElement-impl").implementation;
const { fallbackBaseURL ***REMOVED*** = require("../helpers/document-base-url");

class HTMLBaseElementImpl extends HTMLElementImpl {
  get href() {
    const document = this._ownerDocument;

    const url = this.hasAttributeNS(null, "href") ? this.getAttributeNS(null, "href") : "";
    const parsed = whatwgURL.parseURL(url, { baseURL: fallbackBaseURL(document) ***REMOVED***);

    if (parsed === null) {
      return url;
    ***REMOVED***

    return whatwgURL.serializeURL(parsed);
  ***REMOVED***

  set href(value) {
    this.setAttributeNS(null, "href", value);
  ***REMOVED***
***REMOVED***

module.exports = {
  implementation: HTMLBaseElementImpl
***REMOVED***;

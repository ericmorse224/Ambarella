/* eslint no-unused-vars: ["error", { "varsIgnorePattern": "^net|tls$" ***REMOVED***] */

'use strict';

const net = require('net');
const tls = require('tls');
const { randomFillSync ***REMOVED*** = require('crypto');

const PerMessageDeflate = require('./permessage-deflate');
const { EMPTY_BUFFER ***REMOVED*** = require('./constants');
const { isValidStatusCode ***REMOVED*** = require('./validation');
const { mask: applyMask, toBuffer ***REMOVED*** = require('./buffer-util');

const mask = Buffer.alloc(4);

/**
 * HyBi Sender implementation.
 */
class Sender {
  /**
   * Creates a Sender instance.
   *
   * @param {(net.Socket|tls.Socket)***REMOVED*** socket The connection socket
   * @param {Object***REMOVED*** [extensions] An object containing the negotiated extensions
   */
  constructor(socket, extensions) {
    this._extensions = extensions || {***REMOVED***;
    this._socket = socket;

    this._firstFragment = true;
    this._compress = false;

    this._bufferedBytes = 0;
    this._deflating = false;
    this._queue = [];
  ***REMOVED***

  /**
   * Frames a piece of data according to the HyBi WebSocket protocol.
   *
   * @param {Buffer***REMOVED*** data The data to frame
   * @param {Object***REMOVED*** options Options object
   * @param {Number***REMOVED*** options.opcode The opcode
   * @param {Boolean***REMOVED*** [options.readOnly=false] Specifies whether `data` can be
   *     modified
   * @param {Boolean***REMOVED*** [options.fin=false] Specifies whether or not to set the
   *     FIN bit
   * @param {Boolean***REMOVED*** [options.mask=false] Specifies whether or not to mask
   *     `data`
   * @param {Boolean***REMOVED*** [options.rsv1=false] Specifies whether or not to set the
   *     RSV1 bit
   * @return {Buffer[]***REMOVED*** The framed data as a list of `Buffer` instances
   * @public
   */
  static frame(data, options) {
    const merge = options.mask && options.readOnly;
    let offset = options.mask ? 6 : 2;
    let payloadLength = data.length;

    if (data.length >= 65536) {
      offset += 8;
      payloadLength = 127;
    ***REMOVED*** else if (data.length > 125) {
      offset += 2;
      payloadLength = 126;
    ***REMOVED***

    const target = Buffer.allocUnsafe(merge ? data.length + offset : offset);

    target[0] = options.fin ? options.opcode | 0x80 : options.opcode;
    if (options.rsv1) target[0] |= 0x40;

    target[1] = payloadLength;

    if (payloadLength === 126) {
      target.writeUInt16BE(data.length, 2);
    ***REMOVED*** else if (payloadLength === 127) {
      target.writeUInt32BE(0, 2);
      target.writeUInt32BE(data.length, 6);
    ***REMOVED***

    if (!options.mask) return [target, data];

    randomFillSync(mask, 0, 4);

    target[1] |= 0x80;
    target[offset - 4] = mask[0];
    target[offset - 3] = mask[1];
    target[offset - 2] = mask[2];
    target[offset - 1] = mask[3];

    if (merge) {
      applyMask(data, mask, target, offset, data.length);
      return [target];
    ***REMOVED***

    applyMask(data, mask, data, 0, data.length);
    return [target, data];
  ***REMOVED***

  /**
   * Sends a close message to the other peer.
   *
   * @param {Number***REMOVED*** [code] The status code component of the body
   * @param {String***REMOVED*** [data] The message component of the body
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask the message
   * @param {Function***REMOVED*** [cb] Callback
   * @public
   */
  close(code, data, mask, cb) {
    let buf;

    if (code === undefined) {
      buf = EMPTY_BUFFER;
    ***REMOVED*** else if (typeof code !== 'number' || !isValidStatusCode(code)) {
      throw new TypeError('First argument must be a valid error code number');
    ***REMOVED*** else if (data === undefined || data === '') {
      buf = Buffer.allocUnsafe(2);
      buf.writeUInt16BE(code, 0);
    ***REMOVED*** else {
      const length = Buffer.byteLength(data);

      if (length > 123) {
        throw new RangeError('The message must not be greater than 123 bytes');
      ***REMOVED***

      buf = Buffer.allocUnsafe(2 + length);
      buf.writeUInt16BE(code, 0);
      buf.write(data, 2);
    ***REMOVED***

    if (this._deflating) {
      this.enqueue([this.doClose, buf, mask, cb]);
    ***REMOVED*** else {
      this.doClose(buf, mask, cb);
    ***REMOVED***
  ***REMOVED***

  /**
   * Frames and sends a close message.
   *
   * @param {Buffer***REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask `data`
   * @param {Function***REMOVED*** [cb] Callback
   * @private
   */
  doClose(data, mask, cb) {
    this.sendFrame(
      Sender.frame(data, {
        fin: true,
        rsv1: false,
        opcode: 0x08,
        mask,
        readOnly: false
      ***REMOVED***),
      cb
    );
  ***REMOVED***

  /**
   * Sends a ping message to the other peer.
   *
   * @param {****REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask `data`
   * @param {Function***REMOVED*** [cb] Callback
   * @public
   */
  ping(data, mask, cb) {
    const buf = toBuffer(data);

    if (buf.length > 125) {
      throw new RangeError('The data size must not be greater than 125 bytes');
    ***REMOVED***

    if (this._deflating) {
      this.enqueue([this.doPing, buf, mask, toBuffer.readOnly, cb]);
    ***REMOVED*** else {
      this.doPing(buf, mask, toBuffer.readOnly, cb);
    ***REMOVED***
  ***REMOVED***

  /**
   * Frames and sends a ping message.
   *
   * @param {Buffer***REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask `data`
   * @param {Boolean***REMOVED*** [readOnly=false] Specifies whether `data` can be modified
   * @param {Function***REMOVED*** [cb] Callback
   * @private
   */
  doPing(data, mask, readOnly, cb) {
    this.sendFrame(
      Sender.frame(data, {
        fin: true,
        rsv1: false,
        opcode: 0x09,
        mask,
        readOnly
      ***REMOVED***),
      cb
    );
  ***REMOVED***

  /**
   * Sends a pong message to the other peer.
   *
   * @param {****REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask `data`
   * @param {Function***REMOVED*** [cb] Callback
   * @public
   */
  pong(data, mask, cb) {
    const buf = toBuffer(data);

    if (buf.length > 125) {
      throw new RangeError('The data size must not be greater than 125 bytes');
    ***REMOVED***

    if (this._deflating) {
      this.enqueue([this.doPong, buf, mask, toBuffer.readOnly, cb]);
    ***REMOVED*** else {
      this.doPong(buf, mask, toBuffer.readOnly, cb);
    ***REMOVED***
  ***REMOVED***

  /**
   * Frames and sends a pong message.
   *
   * @param {Buffer***REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [mask=false] Specifies whether or not to mask `data`
   * @param {Boolean***REMOVED*** [readOnly=false] Specifies whether `data` can be modified
   * @param {Function***REMOVED*** [cb] Callback
   * @private
   */
  doPong(data, mask, readOnly, cb) {
    this.sendFrame(
      Sender.frame(data, {
        fin: true,
        rsv1: false,
        opcode: 0x0a,
        mask,
        readOnly
      ***REMOVED***),
      cb
    );
  ***REMOVED***

  /**
   * Sends a data message to the other peer.
   *
   * @param {****REMOVED*** data The message to send
   * @param {Object***REMOVED*** options Options object
   * @param {Boolean***REMOVED*** [options.compress=false] Specifies whether or not to
   *     compress `data`
   * @param {Boolean***REMOVED*** [options.binary=false] Specifies whether `data` is binary
   *     or text
   * @param {Boolean***REMOVED*** [options.fin=false] Specifies whether the fragment is the
   *     last one
   * @param {Boolean***REMOVED*** [options.mask=false] Specifies whether or not to mask
   *     `data`
   * @param {Function***REMOVED*** [cb] Callback
   * @public
   */
  send(data, options, cb) {
    const buf = toBuffer(data);
    const perMessageDeflate = this._extensions[PerMessageDeflate.extensionName];
    let opcode = options.binary ? 2 : 1;
    let rsv1 = options.compress;

    if (this._firstFragment) {
      this._firstFragment = false;
      if (rsv1 && perMessageDeflate) {
        rsv1 = buf.length >= perMessageDeflate._threshold;
      ***REMOVED***
      this._compress = rsv1;
    ***REMOVED*** else {
      rsv1 = false;
      opcode = 0;
    ***REMOVED***

    if (options.fin) this._firstFragment = true;

    if (perMessageDeflate) {
      const opts = {
        fin: options.fin,
        rsv1,
        opcode,
        mask: options.mask,
        readOnly: toBuffer.readOnly
      ***REMOVED***;

      if (this._deflating) {
        this.enqueue([this.dispatch, buf, this._compress, opts, cb]);
      ***REMOVED*** else {
        this.dispatch(buf, this._compress, opts, cb);
      ***REMOVED***
    ***REMOVED*** else {
      this.sendFrame(
        Sender.frame(buf, {
          fin: options.fin,
          rsv1: false,
          opcode,
          mask: options.mask,
          readOnly: toBuffer.readOnly
        ***REMOVED***),
        cb
      );
    ***REMOVED***
  ***REMOVED***

  /**
   * Dispatches a data message.
   *
   * @param {Buffer***REMOVED*** data The message to send
   * @param {Boolean***REMOVED*** [compress=false] Specifies whether or not to compress
   *     `data`
   * @param {Object***REMOVED*** options Options object
   * @param {Number***REMOVED*** options.opcode The opcode
   * @param {Boolean***REMOVED*** [options.readOnly=false] Specifies whether `data` can be
   *     modified
   * @param {Boolean***REMOVED*** [options.fin=false] Specifies whether or not to set the
   *     FIN bit
   * @param {Boolean***REMOVED*** [options.mask=false] Specifies whether or not to mask
   *     `data`
   * @param {Boolean***REMOVED*** [options.rsv1=false] Specifies whether or not to set the
   *     RSV1 bit
   * @param {Function***REMOVED*** [cb] Callback
   * @private
   */
  dispatch(data, compress, options, cb) {
    if (!compress) {
      this.sendFrame(Sender.frame(data, options), cb);
      return;
    ***REMOVED***

    const perMessageDeflate = this._extensions[PerMessageDeflate.extensionName];

    this._bufferedBytes += data.length;
    this._deflating = true;
    perMessageDeflate.compress(data, options.fin, (_, buf) => {
      if (this._socket.destroyed) {
        const err = new Error(
          'The socket was closed while data was being compressed'
        );

        if (typeof cb === 'function') cb(err);

        for (let i = 0; i < this._queue.length; i++) {
          const callback = this._queue[i][4];

          if (typeof callback === 'function') callback(err);
        ***REMOVED***

        return;
      ***REMOVED***

      this._bufferedBytes -= data.length;
      this._deflating = false;
      options.readOnly = false;
      this.sendFrame(Sender.frame(buf, options), cb);
      this.dequeue();
    ***REMOVED***);
  ***REMOVED***

  /**
   * Executes queued send operations.
   *
   * @private
   */
  dequeue() {
    while (!this._deflating && this._queue.length) {
      const params = this._queue.shift();

      this._bufferedBytes -= params[1].length;
      Reflect.apply(params[0], this, params.slice(1));
    ***REMOVED***
  ***REMOVED***

  /**
   * Enqueues a send operation.
   *
   * @param {Array***REMOVED*** params Send operation parameters.
   * @private
   */
  enqueue(params) {
    this._bufferedBytes += params[1].length;
    this._queue.push(params);
  ***REMOVED***

  /**
   * Sends a frame.
   *
   * @param {Buffer[]***REMOVED*** list The frame to send
   * @param {Function***REMOVED*** [cb] Callback
   * @private
   */
  sendFrame(list, cb) {
    if (list.length === 2) {
      this._socket.cork();
      this._socket.write(list[0]);
      this._socket.write(list[1], cb);
      this._socket.uncork();
    ***REMOVED*** else {
      this._socket.write(list[0], cb);
    ***REMOVED***
  ***REMOVED***
***REMOVED***

module.exports = Sender;

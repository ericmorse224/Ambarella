import type {KeywordCxt***REMOVED*** from "../../compile/validate"
import {_, not, nil, Code, Name***REMOVED*** from "../../compile/codegen"

export function checkNullable(
  {gen, data, parentSchema***REMOVED***: KeywordCxt,
  cond: Code = nil
): [Name, Code] {
  const valid = gen.name("valid")
  if (parentSchema.nullable) {
    gen.let(valid, _`${data***REMOVED*** === null`)
    cond = not(valid)
  ***REMOVED*** else {
    gen.let(valid, false)
  ***REMOVED***
  return [valid, cond]
***REMOVED***

export function checkNullableObject(cxt: KeywordCxt, cond: Code): [Name, Code] {
  const [valid, cond_] = checkNullable(cxt, cond)
  return [valid, _`${cond_***REMOVED*** && typeof ${cxt.data***REMOVED*** == "object" && !Array.isArray(${cxt.data***REMOVED***)`]
***REMOVED***

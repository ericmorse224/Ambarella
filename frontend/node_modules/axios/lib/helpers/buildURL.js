'use strict';

var utils = require('./../utils');

function encode(val) {
  return encodeURIComponent(val).
    replace(/%3A/gi, ':').
    replace(/%24/g, '$').
    replace(/%2C/gi, ',').
    replace(/%20/g, '+').
    replace(/%5B/gi, '[').
    replace(/%5D/gi, ']');
***REMOVED***

/**
 * Build a URL by appending params to the end
 *
 * @param {string***REMOVED*** url The base of the url (e.g., http://www.google.com)
 * @param {object***REMOVED*** [params] The params to be appended
 * @returns {string***REMOVED*** The formatted url
 */
module.exports = function buildURL(url, params, paramsSerializer) {
  /*eslint no-param-reassign:0*/
  if (!params) {
    return url;
  ***REMOVED***

  var serializedParams;
  if (paramsSerializer) {
    serializedParams = paramsSerializer(params);
  ***REMOVED*** else if (utils.isURLSearchParams(params)) {
    serializedParams = params.toString();
  ***REMOVED*** else {
    var parts = [];

    utils.forEach(params, function serialize(val, key) {
      if (val === null || typeof val === 'undefined') {
        return;
      ***REMOVED***

      if (utils.isArray(val)) {
        key = key + '[]';
      ***REMOVED*** else {
        val = [val];
      ***REMOVED***

      utils.forEach(val, function parseValue(v) {
        if (utils.isDate(v)) {
          v = v.toISOString();
        ***REMOVED*** else if (utils.isObject(v)) {
          v = JSON.stringify(v);
        ***REMOVED***
        parts.push(encode(key) + '=' + encode(v));
      ***REMOVED***);
    ***REMOVED***);

    serializedParams = parts.join('&');
  ***REMOVED***

  if (serializedParams) {
    var hashmarkIndex = url.indexOf('#');
    if (hashmarkIndex !== -1) {
      url = url.slice(0, hashmarkIndex);
    ***REMOVED***

    url += (url.indexOf('?') === -1 ? '?' : '&') + serializedParams;
  ***REMOVED***

  return url;
***REMOVED***;

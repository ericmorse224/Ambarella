module.exports = {
  prefix(prop) {
    let match = prop.match(/^(-\w+-)/)
    if (match) {
      return match[0]
    ***REMOVED***

    return ''
  ***REMOVED***,

  unprefixed(prop) {
    return prop.replace(/^-\w+-/, '')
  ***REMOVED***
***REMOVED***

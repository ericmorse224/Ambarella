/**
 * @fileoverview Defines a storage for rules.
 * @author Nicholas C. Zakas
 * @author aladdin-add
 */

"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const builtInRules = require("../rules");

//------------------------------------------------------------------------------
// Helpers
//------------------------------------------------------------------------------

/**
 * Normalizes a rule module to the new-style API
 * @param {(Function|{create: Function***REMOVED***)***REMOVED*** rule A rule object, which can either be a function
 * ("old-style") or an object with a `create` method ("new-style")
 * @returns {{create: Function***REMOVED******REMOVED*** A new-style rule.
 */
function normalizeRule(rule) {
    return typeof rule === "function" ? Object.assign({ create: rule ***REMOVED***, rule) : rule;
***REMOVED***

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

/**
 * A storage for rules.
 */
class Rules {
    constructor() {
        this._rules = Object.create(null);
    ***REMOVED***

    /**
     * Registers a rule module for rule id in storage.
     * @param {string***REMOVED*** ruleId Rule id (file name).
     * @param {Function***REMOVED*** ruleModule Rule handler.
     * @returns {void***REMOVED***
     */
    define(ruleId, ruleModule) {
        this._rules[ruleId] = normalizeRule(ruleModule);
    ***REMOVED***

    /**
     * Access rule handler by id (file name).
     * @param {string***REMOVED*** ruleId Rule id (file name).
     * @returns {{create: Function, schema: JsonSchema[]***REMOVED******REMOVED***
     * A rule. This is normalized to always have the new-style shape with a `create` method.
     */
    get(ruleId) {
        if (typeof this._rules[ruleId] === "string") {
            this.define(ruleId, require(this._rules[ruleId]));
        ***REMOVED***
        if (this._rules[ruleId]) {
            return this._rules[ruleId];
        ***REMOVED***
        if (builtInRules.has(ruleId)) {
            return builtInRules.get(ruleId);
        ***REMOVED***

        return null;
    ***REMOVED***

    *[Symbol.iterator]() {
        yield* builtInRules;

        for (const ruleId of Object.keys(this._rules)) {
            yield [ruleId, this.get(ruleId)];
        ***REMOVED***
    ***REMOVED***
***REMOVED***

module.exports = Rules;

'use strict';
const parser = require('postcss-selector-parser');
const exists = require('../exists');
const isMixin = require('../isMixin');
const BasePlugin = require('../plugin');
const { FF_2 ***REMOVED*** = require('../dictionary/browsers');
const { SELECTOR ***REMOVED*** = require('../dictionary/identifiers');
const { RULE ***REMOVED*** = require('../dictionary/postcss');
const { BODY ***REMOVED*** = require('../dictionary/tags');

module.exports = class BodyEmpty extends BasePlugin {
  /** @param {import('postcss').Result***REMOVED*** result */
  constructor(result) {
    super([FF_2], [RULE], result);
  ***REMOVED***

  /**
   * @param {import('postcss').Rule***REMOVED*** rule
   * @return {void***REMOVED***
   */
  detect(rule) {
    if (isMixin(rule)) {
      return;
    ***REMOVED***
    parser(this.analyse(rule)).processSync(rule.selector);
  ***REMOVED***

  /**
   * @param {import('postcss').Rule***REMOVED*** rule
   * @return {parser.SyncProcessor<void>***REMOVED***
   */
  analyse(rule) {
    return (selectors) => {
      selectors.each((selector) => {
        if (
          exists(selector, 0, BODY) &&
          exists(selector, 1, ':empty') &&
          exists(selector, 2, ' ') &&
          selector.at(3)
        ) {
          this.push(rule, {
            identifier: SELECTOR,
            hack: selector.toString(),
          ***REMOVED***);
        ***REMOVED***
      ***REMOVED***);
    ***REMOVED***;
  ***REMOVED***
***REMOVED***;

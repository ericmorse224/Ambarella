// translate the various posix character classes into unicode properties
// this works across all unicode locales
// { <posix class>: [<translation>, /u flag required, negated]
const posixClasses = {
    '[:alnum:]': ['\\p{L***REMOVED***\\p{Nl***REMOVED***\\p{Nd***REMOVED***', true],
    '[:alpha:]': ['\\p{L***REMOVED***\\p{Nl***REMOVED***', true],
    '[:ascii:]': ['\\x' + '00-\\x' + '7f', false],
    '[:blank:]': ['\\p{Zs***REMOVED***\\t', true],
    '[:cntrl:]': ['\\p{Cc***REMOVED***', true],
    '[:digit:]': ['\\p{Nd***REMOVED***', true],
    '[:graph:]': ['\\p{Z***REMOVED***\\p{C***REMOVED***', true, true],
    '[:lower:]': ['\\p{Ll***REMOVED***', true],
    '[:print:]': ['\\p{C***REMOVED***', true],
    '[:punct:]': ['\\p{P***REMOVED***', true],
    '[:space:]': ['\\p{Z***REMOVED***\\t\\r\\n\\v\\f', true],
    '[:upper:]': ['\\p{Lu***REMOVED***', true],
    '[:word:]': ['\\p{L***REMOVED***\\p{Nl***REMOVED***\\p{Nd***REMOVED***\\p{Pc***REMOVED***', true],
    '[:xdigit:]': ['A-Fa-f0-9', false],
***REMOVED***;
// only need to escape a few things inside of brace expressions
// escapes: [ \ ] -
const braceEscape = (s) => s.replace(/[[\]\\-]/g, '\\$&');
// escape all regexp magic characters
const regexpEscape = (s) => s.replace(/[-[\]{***REMOVED***()*+?.,\\^$|#\s]/g, '\\$&');
// everything has already been escaped, we just have to join
const rangesToString = (ranges) => ranges.join('');
// takes a glob string at a posix brace expression, and returns
// an equivalent regular expression source, and boolean indicating
// whether the /u flag needs to be applied, and the number of chars
// consumed to parse the character class.
// This also removes out of order ranges, and returns ($.) if the
// entire class just no good.
export const parseClass = (glob, position) => {
    const pos = position;
    /* c8 ignore start */
    if (glob.charAt(pos) !== '[') {
        throw new Error('not in a brace expression');
    ***REMOVED***
    /* c8 ignore stop */
    const ranges = [];
    const negs = [];
    let i = pos + 1;
    let sawStart = false;
    let uflag = false;
    let escaping = false;
    let negate = false;
    let endPos = pos;
    let rangeStart = '';
    WHILE: while (i < glob.length) {
        const c = glob.charAt(i);
        if ((c === '!' || c === '^') && i === pos + 1) {
            negate = true;
            i++;
            continue;
        ***REMOVED***
        if (c === ']' && sawStart && !escaping) {
            endPos = i + 1;
            break;
        ***REMOVED***
        sawStart = true;
        if (c === '\\') {
            if (!escaping) {
                escaping = true;
                i++;
                continue;
            ***REMOVED***
            // escaped \ char, fall through and treat like normal char
        ***REMOVED***
        if (c === '[' && !escaping) {
            // either a posix class, a collation equivalent, or just a [
            for (const [cls, [unip, u, neg]] of Object.entries(posixClasses)) {
                if (glob.startsWith(cls, i)) {
                    // invalid, [a-[] is fine, but not [a-[:alpha]]
                    if (rangeStart) {
                        return ['$.', false, glob.length - pos, true];
                    ***REMOVED***
                    i += cls.length;
                    if (neg)
                        negs.push(unip);
                    else
                        ranges.push(unip);
                    uflag = uflag || u;
                    continue WHILE;
                ***REMOVED***
            ***REMOVED***
        ***REMOVED***
        // now it's just a normal character, effectively
        escaping = false;
        if (rangeStart) {
            // throw this range away if it's not valid, but others
            // can still match.
            if (c > rangeStart) {
                ranges.push(braceEscape(rangeStart) + '-' + braceEscape(c));
            ***REMOVED***
            else if (c === rangeStart) {
                ranges.push(braceEscape(c));
            ***REMOVED***
            rangeStart = '';
            i++;
            continue;
        ***REMOVED***
        // now might be the start of a range.
        // can be either c-d or c-] or c<more...>] or c] at this point
        if (glob.startsWith('-]', i + 1)) {
            ranges.push(braceEscape(c + '-'));
            i += 2;
            continue;
        ***REMOVED***
        if (glob.startsWith('-', i + 1)) {
            rangeStart = c;
            i += 2;
            continue;
        ***REMOVED***
        // not the start of a range, just a single character
        ranges.push(braceEscape(c));
        i++;
    ***REMOVED***
    if (endPos < i) {
        // didn't see the end of the class, not a valid class,
        // but might still be valid as a literal match.
        return ['', false, 0, false];
    ***REMOVED***
    // if we got no ranges and no negates, then we have a range that
    // cannot possibly match anything, and that poisons the whole glob
    if (!ranges.length && !negs.length) {
        return ['$.', false, glob.length - pos, true];
    ***REMOVED***
    // if we got one positive range, and it's a single character, then that's
    // not actually a magic pattern, it's just that one literal character.
    // we should not treat that as "magic", we should just return the literal
    // character. [_] is a perfectly valid way to escape glob magic chars.
    if (negs.length === 0 &&
        ranges.length === 1 &&
        /^\\?.$/.test(ranges[0]) &&
        !negate) {
        const r = ranges[0].length === 2 ? ranges[0].slice(-1) : ranges[0];
        return [regexpEscape(r), false, endPos - pos, false];
    ***REMOVED***
    const sranges = '[' + (negate ? '^' : '') + rangesToString(ranges) + ']';
    const snegs = '[' + (negate ? '' : '^') + rangesToString(negs) + ']';
    const comb = ranges.length && negs.length
        ? '(' + sranges + '|' + snegs + ')'
        : ranges.length
            ? sranges
            : snegs;
    return [comb, uflag, endPos - pos, true];
***REMOVED***;
//# sourceMappingURL=brace-expressions.js.map
'use strict';

var index = require('../');
var test = require('tape');
var runTests = require('./tests');

test('as a function', function (t) {
	t.test('ArrayBuffer support', { skip: typeof ArrayBuffer === 'undefined' ***REMOVED***, function (st) {
		st.test('bad array/this value', function (s2t) {
			s2t['throws'](function () { index(undefined); ***REMOVED***, TypeError, 'undefined is not an object');
			s2t['throws'](function () { index(null); ***REMOVED***, TypeError, 'null is not an object');
			s2t.end();
		***REMOVED***);

		runTests(index, st);

		st.end();
	***REMOVED***);

	t.test('no ArrayBuffer support', { skip: typeof ArrayBuffer !== 'undefined' ***REMOVED***, function (st) {
		st['throws'](
			function () { index({***REMOVED***); ***REMOVED***,
			SyntaxError,
			'ArrayBuffer is not supported'
		);
	***REMOVED***);

	t.end();
***REMOVED***);

/*
  Copyright 2018 Google LLC

  Use of this source code is governed by an MIT-style
  license that can be found in the LICENSE file or at
  https://opensource.org/licenses/MIT.
*/
import { logger ***REMOVED*** from 'workbox-core/_private/logger.js';
import '../_version.js';
/**
 * @param {string***REMOVED*** groupTitle
 * @param {Array<string>***REMOVED*** deletedURLs
 *
 * @private
 */
const logGroup = (groupTitle, deletedURLs) => {
    logger.groupCollapsed(groupTitle);
    for (const url of deletedURLs) {
        logger.log(url);
    ***REMOVED***
    logger.groupEnd();
***REMOVED***;
/**
 * @param {Array<string>***REMOVED*** deletedURLs
 *
 * @private
 * @memberof workbox-precaching
 */
export function printCleanupDetails(deletedURLs) {
    const deletionCount = deletedURLs.length;
    if (deletionCount > 0) {
        logger.groupCollapsed(`During precaching cleanup, ` +
            `${deletionCount***REMOVED*** cached ` +
            `request${deletionCount === 1 ? ' was' : 's were'***REMOVED*** deleted.`);
        logGroup('Deleted Cache Requests', deletedURLs);
        logger.groupEnd();
    ***REMOVED***
***REMOVED***

import '../_version.js';
/**
 * Removes any URL search parameters that should be ignored.
 *
 * @param {URL***REMOVED*** urlObject The original URL.
 * @param {Array<RegExp>***REMOVED*** ignoreURLParametersMatching RegExps to test against
 * each search parameter name. Matches mean that the search parameter should be
 * ignored.
 * @return {URL***REMOVED*** The URL with any ignored search parameters removed.
 *
 * @private
 * @memberof workbox-precaching
 */
export declare function removeIgnoredSearchParams(urlObject: URL, ignoreURLParametersMatching?: RegExp[]): URL;

import { getAbsoluteMappingEntries ***REMOVED*** from "../mapping-entry";
import { join ***REMOVED*** from "path";

describe("mapping-entry", () => {
  it("should change to absolute paths and sort in longest prefix order", () => {
    const result = getAbsoluteMappingEntries(
      "/absolute/base/url",
      {
        "*": ["/foo1", "/foo2"],
        "longest/pre/fix/*": ["/foo2/bar"],
        "pre/fix/*": ["/foo3"],
      ***REMOVED***,
      true
    );
    // assert.deepEqual(result, [
    //   {
    //     pattern: "longest/pre/fix/*",
    //     paths: [join("/absolute", "base", "url", "foo2", "bar")],
    //   ***REMOVED***,
    //   {
    //     pattern: "pre/fix/*",
    //     paths: [join("/absolute", "base", "url", "foo3")],
    //   ***REMOVED***,
    //   {
    //     pattern: "*",
    //     paths: [
    //       join("/absolute", "base", "url", "foo1"),
    //       join("/absolute", "base", "url", "foo2"),
    //     ],
    //   ***REMOVED***,
    // ]);
    expect(result).toEqual([
      {
        pattern: "longest/pre/fix/*",
        paths: [join("/absolute", "base", "url", "foo2", "bar")],
      ***REMOVED***,
      {
        pattern: "pre/fix/*",
        paths: [join("/absolute", "base", "url", "foo3")],
      ***REMOVED***,
      {
        pattern: "*",
        paths: [
          join("/absolute", "base", "url", "foo1"),
          join("/absolute", "base", "url", "foo2"),
        ],
      ***REMOVED***,
    ]);
  ***REMOVED***);

  it("should should add a match-all pattern when requested", () => {
    let result = getAbsoluteMappingEntries("/absolute/base/url", {***REMOVED***, true);
    // assert.deepEqual(result, [
    //   {
    //     pattern: "*",
    //     paths: [join("/absolute", "base", "url", "*")],
    //   ***REMOVED***,
    // ]);
    expect(result).toEqual([
      {
        pattern: "*",
        paths: [join("/absolute", "base", "url", "*")],
      ***REMOVED***,
    ]);

    result = getAbsoluteMappingEntries("/absolute/base/url", {***REMOVED***, false);
    // assert.deepEqual(result, []);
    expect(result).toEqual([]);
  ***REMOVED***);
***REMOVED***);

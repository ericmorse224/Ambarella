/*!
 * strip-comments <https://github.com/jonschlinkert/strip-comments>
 * Copyright (c) 2014-present, Jon Schlinkert.
 * Released under the MIT License.
 */

'use strict';

const compile = require('./lib/compile');
const parse = require('./lib/parse');

/**
 * Strip all code comments from the given `input`, including protected
 * comments that start with `!`, unless disabled by setting `options.keepProtected`
 * to true.
 *
 * ```js
 * const str = strip('const foo = "bar";// this is a comment\n /* me too *\/');
 * console.log(str);
 * // => 'const foo = "bar";'
 * ```
 * @name  strip
 * @param  {String***REMOVED*** `input` string from which to strip comments
 * @param  {Object***REMOVED*** `options` optional options, passed to [extract-comments][extract-comments]
 * @option {Boolean***REMOVED*** [options] `line` if `false` strip only block comments, default `true`
 * @option {Boolean***REMOVED*** [options] `block` if `false` strip only line comments, default `true`
 * @option {Boolean***REMOVED*** [options] `keepProtected` Keep ignored comments (e.g. `/*!` and `//!`)
 * @option {Boolean***REMOVED*** [options] `preserveNewlines` Preserve newlines after comments are stripped
 * @return {String***REMOVED*** modified input
 * @api public
 */

const strip = module.exports = (input, options) => {
  const opts = { ...options, block: true, line: true ***REMOVED***;
  return compile(parse(input, opts), opts);
***REMOVED***;

/**
 * Strip only block comments.
 *
 * ```js
 * const strip = require('..');
 * const str = strip.block('const foo = "bar";// this is a comment\n /* me too *\/');
 * console.log(str);
 * // => 'const foo = "bar";// this is a comment'
 * ```
 * @name  .block
 * @param  {String***REMOVED*** `input` string from which to strip comments
 * @param  {Object***REMOVED*** `options` pass `opts.keepProtected: true` to keep ignored comments (e.g. `/*!`)
 * @return {String***REMOVED*** modified string
 * @api public
 */

strip.block = (input, options) => {
  const opts = { ...options, block: true ***REMOVED***;
  return compile(parse(input, opts), opts);
***REMOVED***;

/**
 * Strip only line comments.
 *
 * ```js
 * const str = strip.line('const foo = "bar";// this is a comment\n /* me too *\/');
 * console.log(str);
 * // => 'const foo = "bar";\n/* me too *\/'
 * ```
 * @name  .line
 * @param  {String***REMOVED*** `input` string from which to strip comments
 * @param  {Object***REMOVED*** `options` pass `opts.keepProtected: true` to keep ignored comments (e.g. `//!`)
 * @return {String***REMOVED*** modified string
 * @api public
 */

strip.line = (input, options) => {
  const opts = { ...options, line: true ***REMOVED***;
  return compile(parse(input, opts), opts);
***REMOVED***;

/**
 * Strip the first comment from the given `input`. Or, if `opts.keepProtected` is true,
 * the first non-protected comment will be stripped.
 *
 * ```js
 * const output = strip.first(input, { keepProtected: true ***REMOVED***);
 * console.log(output);
 * // => '//! first comment\nfoo; '
 * ```
 * @name .first
 * @param {String***REMOVED*** `input`
 * @param {Object***REMOVED*** `options` pass `opts.keepProtected: true` to keep comments with `!`
 * @return {String***REMOVED***
 * @api public
 */

strip.first = (input, options) => {
  const opts = { ...options, block: true, line: true, first: true ***REMOVED***;
  return compile(parse(input, opts), opts);
***REMOVED***;

/**
 * Parses a string and returns a basic CST (Concrete Syntax Tree).
 *
 * ```js
 * const strip = require('..');
 * const str = strip.block('const foo = "bar";// this is a comment\n /* me too *\/');
 * console.log(str);
 * // => 'const foo = "bar";// this is a comment'
 * ```
 * @name  .block
 * @param  {String***REMOVED*** `input` string from which to strip comments
 * @param  {Object***REMOVED*** `options` pass `opts.keepProtected: true` to keep ignored comments (e.g. `/*!`)
 * @return {String***REMOVED*** modified string
 * @api public
 */

strip.parse = parse;

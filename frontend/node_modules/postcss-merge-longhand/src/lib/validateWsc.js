'use strict';
const colors = require('./colornames.js');

const widths = new Set(['thin', 'medium', 'thick']);
const styles = new Set([
  'none',
  'hidden',
  'dotted',
  'dashed',
  'solid',
  'double',
  'groove',
  'ridge',
  'inset',
  'outset',
]);

/**
 * @param {string***REMOVED*** value
 * @return {boolean***REMOVED***
 */
function isStyle(value) {
  return value !== undefined && styles.has(value.toLowerCase());
***REMOVED***

/**
 * @param {string***REMOVED*** value
 * @return {boolean***REMOVED***
 */
function isWidth(value) {
  return (
    (value && widths.has(value.toLowerCase())) ||
    /^(\d+(\.\d+)?|\.\d+)(\w+)?$/.test(value)
  );
***REMOVED***

/**
 * @param {string***REMOVED*** value
 * @return {boolean***REMOVED***
 */
function isColor(value) {
  if (!value) {
    return false;
  ***REMOVED***

  value = value.toLowerCase();

  if (/rgba?\(/.test(value)) {
    return true;
  ***REMOVED***

  if (/hsla?\(/.test(value)) {
    return true;
  ***REMOVED***

  if (/#([0-9a-z]{6***REMOVED***|[0-9a-z]{3***REMOVED***)/.test(value)) {
    return true;
  ***REMOVED***

  if (value === 'transparent') {
    return true;
  ***REMOVED***

  if (value === 'currentcolor') {
    return true;
  ***REMOVED***

  return colors.has(value);
***REMOVED***

/**
 * @param {[string, string, string]***REMOVED*** wscs
 * @return {boolean***REMOVED***
 */
function isValidWsc(wscs) {
  const validWidth = isWidth(wscs[0]);
  const validStyle = isStyle(wscs[1]);
  const validColor = isColor(wscs[2]);

  return (
    (validWidth && validStyle) ||
    (validWidth && validColor) ||
    (validStyle && validColor)
  );
***REMOVED***

module.exports = { isStyle, isWidth, isColor, isValidWsc ***REMOVED***;

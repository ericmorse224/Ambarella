import type {CodeKeywordDefinition, KeywordErrorDefinition***REMOVED*** from "../../types"
import type {KeywordCxt***REMOVED*** from "../../compile/validate"
import {_, str, operators***REMOVED*** from "../../compile/codegen"

const error: KeywordErrorDefinition = {
  message({keyword, schemaCode***REMOVED***) {
    const comp = keyword === "maxProperties" ? "more" : "fewer"
    return str`must NOT have ${comp***REMOVED*** than ${schemaCode***REMOVED*** properties`
  ***REMOVED***,
  params: ({schemaCode***REMOVED***) => _`{limit: ${schemaCode***REMOVED******REMOVED***`,
***REMOVED***

const def: CodeKeywordDefinition = {
  keyword: ["maxProperties", "minProperties"],
  type: "object",
  schemaType: "number",
  $data: true,
  error,
  code(cxt: KeywordCxt) {
    const {keyword, data, schemaCode***REMOVED*** = cxt
    const op = keyword === "maxProperties" ? operators.GT : operators.LT
    cxt.fail$data(_`Object.keys(${data***REMOVED***).length ${op***REMOVED*** ${schemaCode***REMOVED***`)
  ***REMOVED***,
***REMOVED***

export default def

const debug = require('../internal/debug')
const { MAX_LENGTH, MAX_SAFE_INTEGER ***REMOVED*** = require('../internal/constants')
const { safeRe: re, safeSrc: src, t ***REMOVED*** = require('../internal/re')

const parseOptions = require('../internal/parse-options')
const { compareIdentifiers ***REMOVED*** = require('../internal/identifiers')
class SemVer {
  constructor (version, options) {
    options = parseOptions(options)

    if (version instanceof SemVer) {
      if (version.loose === !!options.loose &&
        version.includePrerelease === !!options.includePrerelease) {
        return version
      ***REMOVED*** else {
        version = version.version
      ***REMOVED***
    ***REMOVED*** else if (typeof version !== 'string') {
      throw new TypeError(`Invalid version. Must be a string. Got type "${typeof version***REMOVED***".`)
    ***REMOVED***

    if (version.length > MAX_LENGTH) {
      throw new TypeError(
        `version is longer than ${MAX_LENGTH***REMOVED*** characters`
      )
    ***REMOVED***

    debug('SemVer', version, options)
    this.options = options
    this.loose = !!options.loose
    // this isn't actually relevant for versions, but keep it so that we
    // don't run into trouble passing this.options around.
    this.includePrerelease = !!options.includePrerelease

    const m = version.trim().match(options.loose ? re[t.LOOSE] : re[t.FULL])

    if (!m) {
      throw new TypeError(`Invalid Version: ${version***REMOVED***`)
    ***REMOVED***

    this.raw = version

    // these are actually numbers
    this.major = +m[1]
    this.minor = +m[2]
    this.patch = +m[3]

    if (this.major > MAX_SAFE_INTEGER || this.major < 0) {
      throw new TypeError('Invalid major version')
    ***REMOVED***

    if (this.minor > MAX_SAFE_INTEGER || this.minor < 0) {
      throw new TypeError('Invalid minor version')
    ***REMOVED***

    if (this.patch > MAX_SAFE_INTEGER || this.patch < 0) {
      throw new TypeError('Invalid patch version')
    ***REMOVED***

    // numberify any prerelease numeric ids
    if (!m[4]) {
      this.prerelease = []
    ***REMOVED*** else {
      this.prerelease = m[4].split('.').map((id) => {
        if (/^[0-9]+$/.test(id)) {
          const num = +id
          if (num >= 0 && num < MAX_SAFE_INTEGER) {
            return num
          ***REMOVED***
        ***REMOVED***
        return id
      ***REMOVED***)
    ***REMOVED***

    this.build = m[5] ? m[5].split('.') : []
    this.format()
  ***REMOVED***

  format () {
    this.version = `${this.major***REMOVED***.${this.minor***REMOVED***.${this.patch***REMOVED***`
    if (this.prerelease.length) {
      this.version += `-${this.prerelease.join('.')***REMOVED***`
    ***REMOVED***
    return this.version
  ***REMOVED***

  toString () {
    return this.version
  ***REMOVED***

  compare (other) {
    debug('SemVer.compare', this.version, this.options, other)
    if (!(other instanceof SemVer)) {
      if (typeof other === 'string' && other === this.version) {
        return 0
      ***REMOVED***
      other = new SemVer(other, this.options)
    ***REMOVED***

    if (other.version === this.version) {
      return 0
    ***REMOVED***

    return this.compareMain(other) || this.comparePre(other)
  ***REMOVED***

  compareMain (other) {
    if (!(other instanceof SemVer)) {
      other = new SemVer(other, this.options)
    ***REMOVED***

    return (
      compareIdentifiers(this.major, other.major) ||
      compareIdentifiers(this.minor, other.minor) ||
      compareIdentifiers(this.patch, other.patch)
    )
  ***REMOVED***

  comparePre (other) {
    if (!(other instanceof SemVer)) {
      other = new SemVer(other, this.options)
    ***REMOVED***

    // NOT having a prerelease is > having one
    if (this.prerelease.length && !other.prerelease.length) {
      return -1
    ***REMOVED*** else if (!this.prerelease.length && other.prerelease.length) {
      return 1
    ***REMOVED*** else if (!this.prerelease.length && !other.prerelease.length) {
      return 0
    ***REMOVED***

    let i = 0
    do {
      const a = this.prerelease[i]
      const b = other.prerelease[i]
      debug('prerelease compare', i, a, b)
      if (a === undefined && b === undefined) {
        return 0
      ***REMOVED*** else if (b === undefined) {
        return 1
      ***REMOVED*** else if (a === undefined) {
        return -1
      ***REMOVED*** else if (a === b) {
        continue
      ***REMOVED*** else {
        return compareIdentifiers(a, b)
      ***REMOVED***
    ***REMOVED*** while (++i)
  ***REMOVED***

  compareBuild (other) {
    if (!(other instanceof SemVer)) {
      other = new SemVer(other, this.options)
    ***REMOVED***

    let i = 0
    do {
      const a = this.build[i]
      const b = other.build[i]
      debug('build compare', i, a, b)
      if (a === undefined && b === undefined) {
        return 0
      ***REMOVED*** else if (b === undefined) {
        return 1
      ***REMOVED*** else if (a === undefined) {
        return -1
      ***REMOVED*** else if (a === b) {
        continue
      ***REMOVED*** else {
        return compareIdentifiers(a, b)
      ***REMOVED***
    ***REMOVED*** while (++i)
  ***REMOVED***

  // preminor will bump the version up to the next minor release, and immediately
  // down to pre-release. premajor and prepatch work the same way.
  inc (release, identifier, identifierBase) {
    if (release.startsWith('pre')) {
      if (!identifier && identifierBase === false) {
        throw new Error('invalid increment argument: identifier is empty')
      ***REMOVED***
      // Avoid an invalid semver results
      if (identifier) {
        const r = new RegExp(`^${this.options.loose ? src[t.PRERELEASELOOSE] : src[t.PRERELEASE]***REMOVED***$`)
        const match = `-${identifier***REMOVED***`.match(r)
        if (!match || match[1] !== identifier) {
          throw new Error(`invalid identifier: ${identifier***REMOVED***`)
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***

    switch (release) {
      case 'premajor':
        this.prerelease.length = 0
        this.patch = 0
        this.minor = 0
        this.major++
        this.inc('pre', identifier, identifierBase)
        break
      case 'preminor':
        this.prerelease.length = 0
        this.patch = 0
        this.minor++
        this.inc('pre', identifier, identifierBase)
        break
      case 'prepatch':
        // If this is already a prerelease, it will bump to the next version
        // drop any prereleases that might already exist, since they are not
        // relevant at this point.
        this.prerelease.length = 0
        this.inc('patch', identifier, identifierBase)
        this.inc('pre', identifier, identifierBase)
        break
      // If the input is a non-prerelease version, this acts the same as
      // prepatch.
      case 'prerelease':
        if (this.prerelease.length === 0) {
          this.inc('patch', identifier, identifierBase)
        ***REMOVED***
        this.inc('pre', identifier, identifierBase)
        break
      case 'release':
        if (this.prerelease.length === 0) {
          throw new Error(`version ${this.raw***REMOVED*** is not a prerelease`)
        ***REMOVED***
        this.prerelease.length = 0
        break

      case 'major':
        // If this is a pre-major version, bump up to the same major version.
        // Otherwise increment major.
        // 1.0.0-5 bumps to 1.0.0
        // 1.1.0 bumps to 2.0.0
        if (
          this.minor !== 0 ||
          this.patch !== 0 ||
          this.prerelease.length === 0
        ) {
          this.major++
        ***REMOVED***
        this.minor = 0
        this.patch = 0
        this.prerelease = []
        break
      case 'minor':
        // If this is a pre-minor version, bump up to the same minor version.
        // Otherwise increment minor.
        // 1.2.0-5 bumps to 1.2.0
        // 1.2.1 bumps to 1.3.0
        if (this.patch !== 0 || this.prerelease.length === 0) {
          this.minor++
        ***REMOVED***
        this.patch = 0
        this.prerelease = []
        break
      case 'patch':
        // If this is not a pre-release version, it will increment the patch.
        // If it is a pre-release it will bump up to the same patch version.
        // 1.2.0-5 patches to 1.2.0
        // 1.2.0 patches to 1.2.1
        if (this.prerelease.length === 0) {
          this.patch++
        ***REMOVED***
        this.prerelease = []
        break
      // This probably shouldn't be used publicly.
      // 1.0.0 'pre' would become 1.0.0-0 which is the wrong direction.
      case 'pre': {
        const base = Number(identifierBase) ? 1 : 0

        if (this.prerelease.length === 0) {
          this.prerelease = [base]
        ***REMOVED*** else {
          let i = this.prerelease.length
          while (--i >= 0) {
            if (typeof this.prerelease[i] === 'number') {
              this.prerelease[i]++
              i = -2
            ***REMOVED***
          ***REMOVED***
          if (i === -1) {
            // didn't increment anything
            if (identifier === this.prerelease.join('.') && identifierBase === false) {
              throw new Error('invalid increment argument: identifier already exists')
            ***REMOVED***
            this.prerelease.push(base)
          ***REMOVED***
        ***REMOVED***
        if (identifier) {
          // 1.2.0-beta.1 bumps to 1.2.0-beta.2,
          // 1.2.0-beta.fooblz or 1.2.0-beta bumps to 1.2.0-beta.0
          let prerelease = [identifier, base]
          if (identifierBase === false) {
            prerelease = [identifier]
          ***REMOVED***
          if (compareIdentifiers(this.prerelease[0], identifier) === 0) {
            if (isNaN(this.prerelease[1])) {
              this.prerelease = prerelease
            ***REMOVED***
          ***REMOVED*** else {
            this.prerelease = prerelease
          ***REMOVED***
        ***REMOVED***
        break
      ***REMOVED***
      default:
        throw new Error(`invalid increment argument: ${release***REMOVED***`)
    ***REMOVED***
    this.raw = this.format()
    if (this.build.length) {
      this.raw += `+${this.build.join('.')***REMOVED***`
    ***REMOVED***
    return this
  ***REMOVED***
***REMOVED***

module.exports = SemVer

'use strict'

const req = require('./req.js')

/**
 * Load Options
 *
 * @private
 * @method options
 *
 * @param  {Object***REMOVED*** config  PostCSS Config
 *
 * @return {Object***REMOVED*** options PostCSS Options
 */
const options = (config, file) => {
  if (config.parser && typeof config.parser === 'string') {
    try {
      config.parser = req(config.parser, file)
    ***REMOVED*** catch (err) {
      throw new Error(`Loading PostCSS Parser failed: ${err.message***REMOVED***\n\n(@${file***REMOVED***)`)
    ***REMOVED***
  ***REMOVED***

  if (config.syntax && typeof config.syntax === 'string') {
    try {
      config.syntax = req(config.syntax, file)
    ***REMOVED*** catch (err) {
      throw new Error(`Loading PostCSS Syntax failed: ${err.message***REMOVED***\n\n(@${file***REMOVED***)`)
    ***REMOVED***
  ***REMOVED***

  if (config.stringifier && typeof config.stringifier === 'string') {
    try {
      config.stringifier = req(config.stringifier, file)
    ***REMOVED*** catch (err) {
      throw new Error(`Loading PostCSS Stringifier failed: ${err.message***REMOVED***\n\n(@${file***REMOVED***)`)
    ***REMOVED***
  ***REMOVED***

  if (config.plugins) {
    delete config.plugins
  ***REMOVED***

  return config
***REMOVED***

module.exports = options

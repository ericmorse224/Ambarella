'use strict';
const { unit ***REMOVED*** = require('postcss-value-parser');

/**
 * @param {string***REMOVED*** value
 * @return {boolean***REMOVED***
 */
function hasUnit(value) {
  const parsedVal = unit(value);
  return parsedVal && parsedVal.unit !== '';
***REMOVED***

/**
 * @param {import('postcss-value-parser').ParsedValue***REMOVED*** columns
 * @return {import('postcss-value-parser').ParsedValue | string***REMOVED***
 */
module.exports = (columns) => {
  /** @type {string[]***REMOVED*** */
  const widths = [];
  /** @type {string[]***REMOVED*** */
  const other = [];
  columns.walk((node) => {
    const { type, value ***REMOVED*** = node;
    if (type === 'word') {
      if (hasUnit(value)) {
        widths.push(value);
      ***REMOVED*** else {
        other.push(value);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***);

  // only transform if declaration is not invalid or a single value
  if (other.length === 1 && widths.length === 1) {
    return `${widths[0].trimStart()***REMOVED*** ${other[0].trimStart()***REMOVED***`;
  ***REMOVED***

  return columns;
***REMOVED***;

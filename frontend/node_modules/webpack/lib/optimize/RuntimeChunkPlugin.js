/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

"use strict";

/** @typedef {import("../Compilation").EntryData***REMOVED*** EntryData */
/** @typedef {import("../Compiler")***REMOVED*** Compiler */
/** @typedef {import("../Entrypoint")***REMOVED*** Entrypoint */

const PLUGIN_NAME = "RuntimeChunkPlugin";

/** @typedef {(entrypoint: { name: string ***REMOVED***) => string***REMOVED*** RuntimeChunkFunction */

class RuntimeChunkPlugin {
	/**
	 * @param {{ name?: RuntimeChunkFunction ***REMOVED***=***REMOVED*** options options
	 */
	constructor(options) {
		this.options = {
			/** @type {RuntimeChunkFunction***REMOVED*** */
			name: entrypoint => `runtime~${entrypoint.name***REMOVED***`,
			...options
		***REMOVED***;
	***REMOVED***

	/**
	 * Apply the plugin
	 * @param {Compiler***REMOVED*** compiler the compiler instance
	 * @returns {void***REMOVED***
	 */
	apply(compiler) {
		compiler.hooks.thisCompilation.tap(PLUGIN_NAME, compilation => {
			compilation.hooks.addEntry.tap(PLUGIN_NAME, (_, { name: entryName ***REMOVED***) => {
				if (entryName === undefined) return;
				const data =
					/** @type {EntryData***REMOVED*** */
					(compilation.entries.get(entryName));
				if (data.options.runtime === undefined && !data.options.dependOn) {
					// Determine runtime chunk name
					let name =
						/** @type {string | RuntimeChunkFunction***REMOVED*** */
						(this.options.name);
					if (typeof name === "function") {
						name = name({ name: entryName ***REMOVED***);
					***REMOVED***
					data.options.runtime = name;
				***REMOVED***
			***REMOVED***);
		***REMOVED***);
	***REMOVED***
***REMOVED***

module.exports = RuntimeChunkPlugin;

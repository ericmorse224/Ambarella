'use strict';

const matchOperatorsRegex = /[|\\{***REMOVED***()[\]^$+*?.-]/g;

module.exports = string => {
	if (typeof string !== 'string') {
		throw new TypeError('Expected a string');
	***REMOVED***

	return string.replace(matchOperatorsRegex, '\\$&');
***REMOVED***;

import escapeClassName from './escapeClassName'
import escapeCommas from './escapeCommas'

export function asClass(name) {
  return escapeCommas(`.${escapeClassName(name)***REMOVED***`)
***REMOVED***

export default function nameClass(classPrefix, key) {
  return asClass(formatClass(classPrefix, key))
***REMOVED***

export function formatClass(classPrefix, key) {
  if (key === 'DEFAULT') {
    return classPrefix
  ***REMOVED***

  if (key === '-' || key === '-DEFAULT') {
    return `-${classPrefix***REMOVED***`
  ***REMOVED***

  if (key.startsWith('-')) {
    return `-${classPrefix***REMOVED***${key***REMOVED***`
  ***REMOVED***

  if (key.startsWith('/')) {
    return `${classPrefix***REMOVED***${key***REMOVED***`
  ***REMOVED***

  return `${classPrefix***REMOVED***-${key***REMOVED***`
***REMOVED***

'use strict';

var test = require('tape');
var inspect = require('object-inspect');
var is = require('object-is');
var forEach = require('for-each');
var v = require('es-value-fixtures');

var unboxPrimitive = require('..');

test('primitives', function (t) {
	forEach([null, undefined], function (nullValue) {
		t['throws'](
			// @ts-expect-error
			function () { unboxPrimitive(nullValue); ***REMOVED***,
			TypeError,
			inspect(nullValue) + ' is not a primitive'
		);
	***REMOVED***);

	// eslint-disable-next-line no-extra-parens
	forEach(/** @type {typeof v.nonNullPrimitives***REMOVED*** */ ([].concat(
		// @ts-expect-error TS sucks with concat
		v.nonNullPrimitives,
		v.zeroes,
		v.infinities,
		NaN
	)), function (primitive) {
		var obj = Object(primitive);
		t.ok(
			is(unboxPrimitive(obj), primitive),
			inspect(obj) + 'unboxes to ' + inspect(primitive)
		);
	***REMOVED***);

	t.end();
***REMOVED***);

test('objects', function (t) {
	// eslint-disable-next-line no-extra-parens
	forEach(/** @type {typeof v.objects***REMOVED*** */ (/** @type {unknown***REMOVED*** */ ([].concat(
		// @ts-expect-error TS sucks with concat
		v.objects,
		{***REMOVED***,
		[],
		function () {***REMOVED***,
		/a/g,
		new Date()
	))), function (object) {
		t['throws'](
			// @ts-expect-error
			function () { unboxPrimitive(object); ***REMOVED***,
			TypeError,
			inspect(object) + ' is not a primitive'
		);
	***REMOVED***);

	t.end();
***REMOVED***);

import type {DefinitionOptions***REMOVED*** from "./_types"
import type {SchemaObject, KeywordCxt, Name***REMOVED*** from "ajv"
import {_***REMOVED*** from "ajv/dist/compile/codegen"

const META_SCHEMA_ID = "http://json-schema.org/schema"

export function metaSchemaRef({defaultMeta***REMOVED***: DefinitionOptions = {***REMOVED***): SchemaObject {
  return defaultMeta === false ? {***REMOVED*** : {$ref: defaultMeta || META_SCHEMA_ID***REMOVED***
***REMOVED***

export function usePattern(
  {gen, it: {opts***REMOVED******REMOVED***: KeywordCxt,
  pattern: string,
  flags = opts.unicodeRegExp ? "u" : ""
): Name {
  const rx = new RegExp(pattern, flags)
  return gen.scopeValue("pattern", {
    key: rx.toString(),
    ref: rx,
    code: _`new RegExp(${pattern***REMOVED***, ${flags***REMOVED***)`,
  ***REMOVED***)
***REMOVED***

'use strict';

// This example shows how to understand if a default value is used or not.

// 1. const { parseArgs ***REMOVED*** = require('node:util'); // from node
// 2. const { parseArgs ***REMOVED*** = require('@pkgjs/parseargs'); // from package
const { parseArgs ***REMOVED*** = require('..'); // in repo

const options = {
  file: { short: 'f', type: 'string', default: 'FOO' ***REMOVED***,
***REMOVED***;

const { values, tokens ***REMOVED*** = parseArgs({ options, tokens: true ***REMOVED***);

const isFileDefault = !tokens.some((token) => token.kind === 'option' &&
 token.name === 'file'
);

console.log(values);
console.log(`Is the file option [${values.file***REMOVED***] the default value? ${isFileDefault***REMOVED***`);

// Try the following:
//    node is-default-value.js
//    node is-default-value.js -f FILE
//    node is-default-value.js --file FILE

import type {MacroKeywordDefinition***REMOVED*** from "ajv"

export default function getDef(): MacroKeywordDefinition {
  return {
    keyword: "prohibited",
    type: "object",
    schemaType: "array",
    macro: function (schema: string[]) {
      if (schema.length === 0) return true
      if (schema.length === 1) return {not: {required: schema***REMOVED******REMOVED***
      return {not: {anyOf: schema.map((p) => ({required: [p]***REMOVED***))***REMOVED******REMOVED***
    ***REMOVED***,
    metaSchema: {
      type: "array",
      items: {type: "string"***REMOVED***,
    ***REMOVED***,
  ***REMOVED***
***REMOVED***

module.exports = getDef

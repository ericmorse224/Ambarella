import type { SourceMapOptions ***REMOVED*** from "./index";
import type { Token ***REMOVED*** from "./parser/tokenizer";
import type { RootTransformerResult ***REMOVED*** from "./transformers/RootTransformer";
export interface RawSourceMap {
    version: number;
    file: string;
    sources: Array<string>;
    sourceRoot?: string;
    sourcesContent?: Array<string>;
    mappings: string;
    names: Array<string>;
***REMOVED***
/**
 * Generate a source map indicating that each line maps directly to the original line,
 * with the tokens in their new positions.
 */
export default function computeSourceMap({ code: generatedCode, mappings: rawMappings ***REMOVED***: RootTransformerResult, filePath: string, options: SourceMapOptions, source: string, tokens: Array<Token>): RawSourceMap;

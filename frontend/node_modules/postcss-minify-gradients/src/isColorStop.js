'use strict';
const { unit ***REMOVED*** = require('postcss-value-parser');
const { colord, extend ***REMOVED*** = require('colord');
const namesPlugin = require('colord/plugins/names');

extend([/** @type {any***REMOVED*** */ (namesPlugin)]);

/* Code derived from https://github.com/pigcan/is-color-stop */

const lengthUnits = new Set([
  'PX',
  'IN',
  'CM',
  'MM',
  'EM',
  'REM',
  'POINTS',
  'PC',
  'EX',
  'CH',
  'VW',
  'VH',
  'VMIN',
  'VMAX',
  '%',
]);

/**
 * @param {string***REMOVED*** input
 * @return {boolean***REMOVED***
 */
function isCSSLengthUnit(input) {
  return lengthUnits.has(input.toUpperCase());
***REMOVED***
/**
 * @param {string|undefined***REMOVED*** str
 * @return {boolean***REMOVED***
 */
function isStop(str) {
  if (str) {
    let stop = false;
    const node = unit(str);
    if (node) {
      const number = Number(node.number);
      if (number === 0 || (!isNaN(number) && isCSSLengthUnit(node.unit))) {
        stop = true;
      ***REMOVED***
    ***REMOVED*** else {
      stop = /^calc\(\S+\)$/g.test(str);
    ***REMOVED***
    return stop;
  ***REMOVED***
  return true;
***REMOVED***
/**
 * @param {string***REMOVED*** color
 * @param {string***REMOVED*** [stop]
 * @return {boolean***REMOVED***
 */
module.exports = function isColorStop(color, stop) {
  return colord(color).isValid() && isStop(stop);
***REMOVED***;

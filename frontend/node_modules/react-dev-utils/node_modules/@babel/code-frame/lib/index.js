'use strict';

Object.defineProperty(exports, '__esModule', { value: true ***REMOVED***);

var picocolors = require('picocolors');
var jsTokens = require('js-tokens');
var helperValidatorIdentifier = require('@babel/helper-validator-identifier');

function isColorSupported() {
  return (typeof process === "object" && (process.env.FORCE_COLOR === "0" || process.env.FORCE_COLOR === "false") ? false : picocolors.isColorSupported
  );
***REMOVED***
const compose = (f, g) => v => f(g(v));
function buildDefs(colors) {
  return {
    keyword: colors.cyan,
    capitalized: colors.yellow,
    jsxIdentifier: colors.yellow,
    punctuator: colors.yellow,
    number: colors.magenta,
    string: colors.green,
    regex: colors.magenta,
    comment: colors.gray,
    invalid: compose(compose(colors.white, colors.bgRed), colors.bold),
    gutter: colors.gray,
    marker: compose(colors.red, colors.bold),
    message: compose(colors.red, colors.bold),
    reset: colors.reset
  ***REMOVED***;
***REMOVED***
const defsOn = buildDefs(picocolors.createColors(true));
const defsOff = buildDefs(picocolors.createColors(false));
function getDefs(enabled) {
  return enabled ? defsOn : defsOff;
***REMOVED***

const sometimesKeywords = new Set(["as", "async", "from", "get", "of", "set"]);
const NEWLINE$1 = /\r\n|[\n\r\u2028\u2029]/;
const BRACKET = /^[()[\]{***REMOVED***]$/;
let tokenize;
{
  const JSX_TAG = /^[a-z][\w-]*$/i;
  const getTokenType = function (token, offset, text) {
    if (token.type === "name") {
      if (helperValidatorIdentifier.isKeyword(token.value) || helperValidatorIdentifier.isStrictReservedWord(token.value, true) || sometimesKeywords.has(token.value)) {
        return "keyword";
      ***REMOVED***
      if (JSX_TAG.test(token.value) && (text[offset - 1] === "<" || text.slice(offset - 2, offset) === "</")) {
        return "jsxIdentifier";
      ***REMOVED***
      if (token.value[0] !== token.value[0].toLowerCase()) {
        return "capitalized";
      ***REMOVED***
    ***REMOVED***
    if (token.type === "punctuator" && BRACKET.test(token.value)) {
      return "bracket";
    ***REMOVED***
    if (token.type === "invalid" && (token.value === "@" || token.value === "#")) {
      return "punctuator";
    ***REMOVED***
    return token.type;
  ***REMOVED***;
  tokenize = function* (text) {
    let match;
    while (match = jsTokens.default.exec(text)) {
      const token = jsTokens.matchToToken(match);
      yield {
        type: getTokenType(token, match.index, text),
        value: token.value
      ***REMOVED***;
    ***REMOVED***
  ***REMOVED***;
***REMOVED***
function highlight(text) {
  if (text === "") return "";
  const defs = getDefs(true);
  let highlighted = "";
  for (const {
    type,
    value
  ***REMOVED*** of tokenize(text)) {
    if (type in defs) {
      highlighted += value.split(NEWLINE$1).map(str => defs[type](str)).join("\n");
    ***REMOVED*** else {
      highlighted += value;
    ***REMOVED***
  ***REMOVED***
  return highlighted;
***REMOVED***

let deprecationWarningShown = false;
const NEWLINE = /\r\n|[\n\r\u2028\u2029]/;
function getMarkerLines(loc, source, opts) {
  const startLoc = Object.assign({
    column: 0,
    line: -1
  ***REMOVED***, loc.start);
  const endLoc = Object.assign({***REMOVED***, startLoc, loc.end);
  const {
    linesAbove = 2,
    linesBelow = 3
  ***REMOVED*** = opts || {***REMOVED***;
  const startLine = startLoc.line;
  const startColumn = startLoc.column;
  const endLine = endLoc.line;
  const endColumn = endLoc.column;
  let start = Math.max(startLine - (linesAbove + 1), 0);
  let end = Math.min(source.length, endLine + linesBelow);
  if (startLine === -1) {
    start = 0;
  ***REMOVED***
  if (endLine === -1) {
    end = source.length;
  ***REMOVED***
  const lineDiff = endLine - startLine;
  const markerLines = {***REMOVED***;
  if (lineDiff) {
    for (let i = 0; i <= lineDiff; i++) {
      const lineNumber = i + startLine;
      if (!startColumn) {
        markerLines[lineNumber] = true;
      ***REMOVED*** else if (i === 0) {
        const sourceLength = source[lineNumber - 1].length;
        markerLines[lineNumber] = [startColumn, sourceLength - startColumn + 1];
      ***REMOVED*** else if (i === lineDiff) {
        markerLines[lineNumber] = [0, endColumn];
      ***REMOVED*** else {
        const sourceLength = source[lineNumber - i].length;
        markerLines[lineNumber] = [0, sourceLength];
      ***REMOVED***
    ***REMOVED***
  ***REMOVED*** else {
    if (startColumn === endColumn) {
      if (startColumn) {
        markerLines[startLine] = [startColumn, 0];
      ***REMOVED*** else {
        markerLines[startLine] = true;
      ***REMOVED***
    ***REMOVED*** else {
      markerLines[startLine] = [startColumn, endColumn - startColumn];
    ***REMOVED***
  ***REMOVED***
  return {
    start,
    end,
    markerLines
  ***REMOVED***;
***REMOVED***
function codeFrameColumns(rawLines, loc, opts = {***REMOVED***) {
  const shouldHighlight = opts.forceColor || isColorSupported() && opts.highlightCode;
  const defs = getDefs(shouldHighlight);
  const lines = rawLines.split(NEWLINE);
  const {
    start,
    end,
    markerLines
  ***REMOVED*** = getMarkerLines(loc, lines, opts);
  const hasColumns = loc.start && typeof loc.start.column === "number";
  const numberMaxWidth = String(end).length;
  const highlightedLines = shouldHighlight ? highlight(rawLines) : rawLines;
  let frame = highlightedLines.split(NEWLINE, end).slice(start, end).map((line, index) => {
    const number = start + 1 + index;
    const paddedNumber = ` ${number***REMOVED***`.slice(-numberMaxWidth);
    const gutter = ` ${paddedNumber***REMOVED*** |`;
    const hasMarker = markerLines[number];
    const lastMarkerLine = !markerLines[number + 1];
    if (hasMarker) {
      let markerLine = "";
      if (Array.isArray(hasMarker)) {
        const markerSpacing = line.slice(0, Math.max(hasMarker[0] - 1, 0)).replace(/[^\t]/g, " ");
        const numberOfMarkers = hasMarker[1] || 1;
        markerLine = ["\n ", defs.gutter(gutter.replace(/\d/g, " ")), " ", markerSpacing, defs.marker("^").repeat(numberOfMarkers)].join("");
        if (lastMarkerLine && opts.message) {
          markerLine += " " + defs.message(opts.message);
        ***REMOVED***
      ***REMOVED***
      return [defs.marker(">"), defs.gutter(gutter), line.length > 0 ? ` ${line***REMOVED***` : "", markerLine].join("");
    ***REMOVED*** else {
      return ` ${defs.gutter(gutter)***REMOVED***${line.length > 0 ? ` ${line***REMOVED***` : ""***REMOVED***`;
    ***REMOVED***
  ***REMOVED***).join("\n");
  if (opts.message && !hasColumns) {
    frame = `${" ".repeat(numberMaxWidth + 1)***REMOVED***${opts.message***REMOVED***\n${frame***REMOVED***`;
  ***REMOVED***
  if (shouldHighlight) {
    return defs.reset(frame);
  ***REMOVED*** else {
    return frame;
  ***REMOVED***
***REMOVED***
function index (rawLines, lineNumber, colNumber, opts = {***REMOVED***) {
  if (!deprecationWarningShown) {
    deprecationWarningShown = true;
    const message = "Passing lineNumber and colNumber is deprecated to @babel/code-frame. Please use `codeFrameColumns`.";
    if (process.emitWarning) {
      process.emitWarning(message, "DeprecationWarning");
    ***REMOVED*** else {
      const deprecationError = new Error(message);
      deprecationError.name = "DeprecationWarning";
      console.warn(new Error(message));
    ***REMOVED***
  ***REMOVED***
  colNumber = Math.max(colNumber, 0);
  const location = {
    start: {
      column: colNumber,
      line: lineNumber
    ***REMOVED***
  ***REMOVED***;
  return codeFrameColumns(rawLines, location, opts);
***REMOVED***

exports.codeFrameColumns = codeFrameColumns;
exports.default = index;
exports.highlight = highlight;
//# sourceMappingURL=index.js.map

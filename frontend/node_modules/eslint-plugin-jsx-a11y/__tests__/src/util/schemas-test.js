import test from 'tape';

import { generateObjSchema, arraySchema, enumArraySchema ***REMOVED*** from '../../../src/util/schemas';

test('schemas', (t) => {
  t.test('should generate an object schema with correct properties', (st) => {
    const schema = generateObjSchema({
      foo: 'bar',
      baz: arraySchema,
    ***REMOVED***);
    const properties = schema.properties || {***REMOVED***;

    st.deepEqual(properties.foo, properties.foo, 'bar');
    st.deepEqual(properties.baz.type, 'array');

    st.end();
  ***REMOVED***);

  t.deepEqual(
    enumArraySchema(),
    {
      additionalItems: false,
      items: {
        enum: [],
        type: 'string',
      ***REMOVED***,
      minItems: 0,
      type: 'array',
      uniqueItems: true,
    ***REMOVED***,
    'enumArraySchema works with no arguments',
  );

  t.end();
***REMOVED***);

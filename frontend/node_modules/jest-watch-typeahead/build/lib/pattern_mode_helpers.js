import chalk from 'chalk';
import stripAnsi from 'strip-ansi';

const pluralize = (count, text) => count === 1 ? text : `${text***REMOVED***s`;

export const printPatternMatches = (count, entity, pipe, extraText = '') => {
  const pluralized = pluralize(count, entity);
  const result = count ? `\n\n Pattern matches ${count***REMOVED*** ${pluralized***REMOVED***` : `\n\n Pattern matches no ${pluralized***REMOVED***`;
  pipe.write(result + extraText);
***REMOVED***;
export const printStartTyping = (entity, pipe) => {
  pipe.write(`\n\n ${chalk.italic.yellow(`Start typing to filter by a ${entity***REMOVED*** regex pattern.`)***REMOVED***`);
***REMOVED***;
export const printMore = (entity, pipe, more) => {
  pipe.write(`\n   ${chalk.dim(`...and ${more***REMOVED*** more ${pluralize(more, entity)***REMOVED***`)***REMOVED***`);
***REMOVED***;
export const printTypeaheadItem = (item, pipe) => {
  pipe.write(`\n ${chalk.dim('\u203A')***REMOVED*** ${item***REMOVED***`);
***REMOVED***;
export const formatTypeaheadSelection = (item, index, activeIndex, prompt) => {
  if (index === activeIndex) {
    prompt.setPromptSelection(stripAnsi(item));
    return chalk.black.bgYellow(stripAnsi(item));
  ***REMOVED***

  return item;
***REMOVED***;
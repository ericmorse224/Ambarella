'use strict';

module.exports = function defFunc(ajv) {
  defFunc.definition = {
    type: 'object',
    macro: function (schema) {
      if (schema.length == 0) return true;
      if (schema.length == 1) return {required: schema***REMOVED***;
      var schemas = schema.map(function (prop) {
        return {required: [prop]***REMOVED***;
      ***REMOVED***);
      return {oneOf: schemas***REMOVED***;
    ***REMOVED***,
    metaSchema: {
      type: 'array',
      items: {
        type: 'string'
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***;

  ajv.addKeyword('oneRequired', defFunc.definition);
  return ajv;
***REMOVED***;

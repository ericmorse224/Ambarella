function pluginsFrom(plugins) {
  var flatPlugins = {
    level1Value: [],
    level1Property: [],
    level2Block: []
  ***REMOVED***;

  plugins = plugins || [];

  flatPlugins.level1Value = plugins
    .map(function(plugin) { return plugin.level1 && plugin.level1.value; ***REMOVED***)
    .filter(function(plugin) { return plugin != null; ***REMOVED***);

  flatPlugins.level1Property = plugins
    .map(function(plugin) { return plugin.level1 && plugin.level1.property; ***REMOVED***)
    .filter(function(plugin) { return plugin != null; ***REMOVED***);

  flatPlugins.level2Block = plugins
    .map(function(plugin) { return plugin.level2 && plugin.level2.block; ***REMOVED***)
    .filter(function(plugin) { return plugin != null; ***REMOVED***);

  return flatPlugins;
***REMOVED***

module.exports = pluginsFrom;

/**
 * @fileoverview JSLint XML reporter
 * @author Ian Christian Myers
 */
"use strict";

const xmlEscape = require("../xml-escape");

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

module.exports = function(results) {

    let output = "";

    output += "<?xml version=\"1.0\" encoding=\"utf-8\"?>";
    output += "<jslint>";

    results.forEach(result => {
        const messages = result.messages;

        output += `<file name="${result.filePath***REMOVED***">`;

        messages.forEach(message => {
            output += [
                `<issue line="${message.line***REMOVED***"`,
                `char="${message.column***REMOVED***"`,
                `evidence="${xmlEscape(message.source || "")***REMOVED***"`,
                `reason="${xmlEscape(message.message || "")***REMOVED***${message.ruleId ? ` (${message.ruleId***REMOVED***)` : ""***REMOVED***" />`
            ].join(" ");
        ***REMOVED***);

        output += "</file>";

    ***REMOVED***);

    output += "</jslint>";

    return output;
***REMOVED***;

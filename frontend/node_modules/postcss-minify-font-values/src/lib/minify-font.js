'use strict';
const { unit ***REMOVED*** = require('postcss-value-parser');
const keywords = require('./keywords');
const minifyFamily = require('./minify-family');
const minifyWeight = require('./minify-weight');

/**
 * @param {import('postcss-value-parser').Node[]***REMOVED*** nodes
 * @param {import('../index').Options***REMOVED*** opts
 * @return {import('postcss-value-parser').Node[]***REMOVED***
 */
module.exports = function (nodes, opts) {
  let i, max, node, family;
  let familyStart = NaN;
  let hasSize = false;

  for (i = 0, max = nodes.length; i < max; i += 1) {
    node = nodes[i];

    if (node.type === 'word') {
      if (hasSize) {
        continue;
      ***REMOVED***

      const value = node.value.toLowerCase();

      if (
        value === 'normal' ||
        value === 'inherit' ||
        value === 'initial' ||
        value === 'unset'
      ) {
        familyStart = i;
      ***REMOVED*** else if (keywords.style.has(value) || unit(value)) {
        familyStart = i;
      ***REMOVED*** else if (keywords.variant.has(value)) {
        familyStart = i;
      ***REMOVED*** else if (keywords.weight.has(value)) {
        node.value = minifyWeight(value);
        familyStart = i;
      ***REMOVED*** else if (keywords.stretch.has(value)) {
        familyStart = i;
      ***REMOVED*** else if (keywords.size.has(value) || unit(value)) {
        familyStart = i;
        hasSize = true;
      ***REMOVED***
    ***REMOVED*** else if (
      node.type === 'function' &&
      nodes[i + 1] &&
      nodes[i + 1].type === 'space'
    ) {
      familyStart = i;
    ***REMOVED*** else if (node.type === 'div' && node.value === '/') {
      familyStart = i + 1;
      break;
    ***REMOVED***
  ***REMOVED***

  familyStart += 2;

  family = minifyFamily(nodes.slice(familyStart), opts);

  return nodes.slice(0, familyStart).concat(family);
***REMOVED***;

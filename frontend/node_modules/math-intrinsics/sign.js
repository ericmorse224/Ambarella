'use strict';

var $isNaN = require('./isNaN');

/** @type {import('./sign')***REMOVED*** */
module.exports = function sign(number) {
	if ($isNaN(number) || number === 0) {
		return number;
	***REMOVED***
	return number < 0 ? -1 : +1;
***REMOVED***;

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

"use strict";

/**
 * @template T
 */
class Queue {
	/**
	 * @param {Iterable<T>=***REMOVED*** items The initial elements.
	 */
	constructor(items) {
		/**
		 * @private
		 * @type {Set<T>***REMOVED***
		 */
		this._set = new Set(items);
	***REMOVED***

	/**
	 * Returns the number of elements in this queue.
	 * @returns {number***REMOVED*** The number of elements in this queue.
	 */
	get length() {
		return this._set.size;
	***REMOVED***

	/**
	 * Appends the specified element to this queue.
	 * @param {T***REMOVED*** item The element to add.
	 * @returns {void***REMOVED***
	 */
	enqueue(item) {
		this._set.add(item);
	***REMOVED***

	/**
	 * Retrieves and removes the head of this queue.
	 * @returns {T | undefined***REMOVED*** The head of the queue of `undefined` if this queue is empty.
	 */
	dequeue() {
		const result = this._set[Symbol.iterator]().next();
		if (result.done) return;
		this._set.delete(result.value);
		return result.value;
	***REMOVED***
***REMOVED***

module.exports = Queue;

'use strict';

var test = require('tape');
var debug = require('object-inspect');
var forEach = require('for-each');

var isSet = require('..');

test('non-collections', function (t) {
	forEach([
		null,
		undefined,
		true,
		false,
		42,
		0,
		-0,
		NaN,
		Infinity,
		'',
		'foo',
		/a/g,
		[],
		{***REMOVED***,
		function () {***REMOVED***
	], function (nonCollection) {
		t.equal(isSet(nonCollection), false, debug(nonCollection) + ' is not a Set');
	***REMOVED***);

	t.end();
***REMOVED***);

test('Maps', { skip: typeof Map !== 'function' ***REMOVED***, function (t) {
	var m = new Map();
	t.equal(isSet(m), false, debug(m) + ' is not a Set');

	t.end();
***REMOVED***);

test('Sets', { skip: typeof Set !== 'function' ***REMOVED***, function (t) {
	var s = new Set();
	t.equal(isSet(s), true, debug(s) + ' is a Set');

	t.end();
***REMOVED***);

test('WeakMaps', { skip: typeof WeakMap !== 'function' ***REMOVED***, function (t) {
	var wm = new WeakMap();
	t.equal(isSet(wm), false, debug(wm) + ' is not a Set');

	t.end();
***REMOVED***);

test('WeakSets', { skip: typeof WeakSet !== 'function' ***REMOVED***, function (t) {
	var ws = new WeakSet();
	t.equal(isSet(ws), false, debug(ws) + ' is not a Set');

	t.end();
***REMOVED***);

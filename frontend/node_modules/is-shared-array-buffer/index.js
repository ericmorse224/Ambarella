'use strict';

var callBound = require('call-bound');

/** @type {undefined | ((thisArg: SharedArrayBuffer) => number)***REMOVED*** */
var $byteLength = callBound('SharedArrayBuffer.prototype.byteLength', true);

/** @type {import('.')***REMOVED*** */
module.exports = $byteLength
	? function isSharedArrayBuffer(obj) {
		if (!obj || typeof obj !== 'object') {
			return false;
		***REMOVED***
		try {
			// @ts-expect-error TS can't figure out this closed-over variable is non-nullable, and it's fine that `obj` might not be a SAB
			$byteLength(obj);
			return true;
		***REMOVED*** catch (e) {
			return false;
		***REMOVED***
	***REMOVED***
	: function isSharedArrayBuffer(_obj) { // eslint-disable-line no-unused-vars
		return false;
	***REMOVED***;

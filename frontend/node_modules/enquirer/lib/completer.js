'use strict';

const unique = arr => arr.filter((v, i) => arr.lastIndexOf(v) === i);
const compact = arr => unique(arr).filter(Boolean);

module.exports = (action, data = {***REMOVED***, value = '') => {
  let { past = [], present = '' ***REMOVED*** = data;
  let rest, prev;

  switch (action) {
    case 'prev':
    case 'undo':
      rest = past.slice(0, past.length - 1);
      prev = past[past.length - 1] || '';
      return {
        past: compact([value, ...rest]),
        present: prev
      ***REMOVED***;

    case 'next':
    case 'redo':
      rest = past.slice(1);
      prev = past[0] || '';
      return {
        past: compact([...rest, value]),
        present: prev
      ***REMOVED***;

    case 'save':
      return {
        past: compact([...past, value]),
        present: ''
      ***REMOVED***;

    case 'remove':
      prev = compact(past.filter(v => v !== value));
      present = '';

      if (prev.length) {
        present = prev.pop();
      ***REMOVED***

      return {
        past: prev,
        present
      ***REMOVED***;

    default: {
      throw new Error(`Invalid action: "${action***REMOVED***"`);
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

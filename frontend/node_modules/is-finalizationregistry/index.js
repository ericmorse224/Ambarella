'use strict';

var callBound = require('call-bound');

/** @type {undefined | ((value: ThisParameterType<typeof FinalizationRegistry.prototype.register>, ...args: Parameters<typeof FinalizationRegistry.prototype.register>) => ReturnType<typeof FinalizationRegistry.prototype.register>)***REMOVED*** */
var $register = callBound('FinalizationRegistry.prototype.register', true);

/** @type {import('.')***REMOVED*** */
module.exports = $register
	? function isFinalizationRegistry(value) {
		if (!value || typeof value !== 'object') {
			return false;
		***REMOVED***
		try {
			// @ts-expect-error TS can't figure out that it's always truthy here
			$register(value, {***REMOVED***, null);
			return true;
		***REMOVED*** catch (e) {
			return false;
		***REMOVED***
	***REMOVED***
	// @ts-ignore unused var
	: function isFinalizationRegistry(value) { // eslint-disable-line no-unused-vars
		return false;
	***REMOVED***;

'use strict';
/*! (c) Andrea Giammarchi - ISC */

const {deserialize***REMOVED*** = require('./deserialize.js');
const {serialize***REMOVED*** = require('./serialize.js');

const {parse: $parse, stringify: $stringify***REMOVED*** = JSON;
const options = {json: true, lossy: true***REMOVED***;

/**
 * Revive a previously stringified structured clone.
 * @param {string***REMOVED*** str previously stringified data as string.
 * @returns {any***REMOVED*** whatever was previously stringified as clone.
 */
const parse = str => deserialize($parse(str));
exports.parse = parse;

/**
 * Represent a structured clone value as string.
 * @param {any***REMOVED*** any some clone-able value to stringify.
 * @returns {string***REMOVED*** the value stringified.
 */
const stringify = any => $stringify(serialize(any, options));
exports.stringify = stringify;

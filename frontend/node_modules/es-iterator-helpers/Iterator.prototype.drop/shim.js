'use strict';

var define = require('define-properties');
var getPolyfill = require('./polyfill');

var $IteratorPrototype = require('../Iterator.prototype/implementation');

module.exports = function shimIteratorPrototypeDrop() {
	var polyfill = getPolyfill();

	define(
		$IteratorPrototype,
		{ drop: polyfill ***REMOVED***,
		{ drop: function () { return $IteratorPrototype.drop !== polyfill; ***REMOVED*** ***REMOVED***
	);

	return polyfill;
***REMOVED***;

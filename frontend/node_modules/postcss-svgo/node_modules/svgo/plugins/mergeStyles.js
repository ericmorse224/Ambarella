'use strict';

/**
 * @typedef {import('../lib/types').XastElement***REMOVED*** XastElement
 */

const { visitSkip, detachNodeFromParent ***REMOVED*** = require('../lib/xast.js');
const JSAPI = require('../lib/svgo/jsAPI.js');

exports.name = 'mergeStyles';
exports.type = 'visitor';
exports.active = true;
exports.description = 'merge multiple style elements into one';

/**
 * Merge multiple style elements into one.
 *
 * @author strarsis <strarsis@gmail.com>
 *
 * @type {import('../lib/types').Plugin<void>***REMOVED***
 */
exports.fn = () => {
  /**
   * @type {null | XastElement***REMOVED***
   */
  let firstStyleElement = null;
  let collectedStyles = '';
  let styleContentType = 'text';

  return {
    element: {
      enter: (node, parentNode) => {
        // skip <foreignObject> content
        if (node.name === 'foreignObject') {
          return visitSkip;
        ***REMOVED***

        // collect style elements
        if (node.name !== 'style') {
          return;
        ***REMOVED***

        // skip <style> with invalid type attribute
        if (
          node.attributes.type != null &&
          node.attributes.type !== '' &&
          node.attributes.type !== 'text/css'
        ) {
          return;
        ***REMOVED***

        // extract style element content
        let css = '';
        for (const child of node.children) {
          if (child.type === 'text') {
            css += child.value;
          ***REMOVED***
          if (child.type === 'cdata') {
            styleContentType = 'cdata';
            css += child.value;
          ***REMOVED***
        ***REMOVED***

        // remove empty style elements
        if (css.trim().length === 0) {
          detachNodeFromParent(node, parentNode);
          return;
        ***REMOVED***

        // collect css and wrap with media query if present in attribute
        if (node.attributes.media == null) {
          collectedStyles += css;
        ***REMOVED*** else {
          collectedStyles += `@media ${node.attributes.media***REMOVED***{${css***REMOVED******REMOVED***`;
          delete node.attributes.media;
        ***REMOVED***

        // combine collected styles in the first style element
        if (firstStyleElement == null) {
          firstStyleElement = node;
        ***REMOVED*** else {
          detachNodeFromParent(node, parentNode);
          firstStyleElement.children = [
            new JSAPI(
              { type: styleContentType, value: collectedStyles ***REMOVED***,
              firstStyleElement
            ),
          ];
        ***REMOVED***
      ***REMOVED***,
    ***REMOVED***,
  ***REMOVED***;
***REMOVED***;

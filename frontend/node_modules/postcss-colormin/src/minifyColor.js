'use strict';
const { colord, extend ***REMOVED*** = require('colord');
const namesPlugin = require('colord/plugins/names');
const minifierPlugin = require('colord/plugins/minify');

extend(/** @type {any[]***REMOVED*** */ ([namesPlugin, minifierPlugin]));

/**
 * Performs color value minification
 *
 * @param {string***REMOVED*** input - CSS value
 * @param {Record<string, boolean>***REMOVED*** options  object with colord.minify() options
 * @return {string***REMOVED***
 */
module.exports = function minifyColor(input, options = {***REMOVED***) {
  const instance = colord(input);

  if (instance.isValid()) {
    // Try to shorten the string if it is a valid CSS color value
    const minified = instance.minify(options);

    // Fall back to the original input if it's smaller or has equal length
    return minified.length < input.length ? minified : input.toLowerCase();
  ***REMOVED*** else {
    // Possibly malformed, so pass through
    return input;
  ***REMOVED***
***REMOVED***;

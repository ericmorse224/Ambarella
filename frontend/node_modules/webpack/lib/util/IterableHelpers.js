/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

"use strict";

/**
 * @template T
 * @param {Iterable<T>***REMOVED*** set a set
 * @returns {T | undefined***REMOVED*** last item
 */
const last = set => {
	let last;
	for (const item of set) last = item;
	return last;
***REMOVED***;

/**
 * @template T
 * @param {Iterable<T>***REMOVED*** iterable iterable
 * @param {(value: T) => boolean | null | undefined***REMOVED*** filter predicate
 * @returns {boolean***REMOVED*** true, if some items match the filter predicate
 */
const someInIterable = (iterable, filter) => {
	for (const item of iterable) {
		if (filter(item)) return true;
	***REMOVED***
	return false;
***REMOVED***;

/**
 * @template T
 * @param {Iterable<T>***REMOVED*** iterable an iterable
 * @returns {number***REMOVED*** count of items
 */
const countIterable = iterable => {
	let i = 0;
	for (const _ of iterable) i++;
	return i;
***REMOVED***;

module.exports.last = last;
module.exports.someInIterable = someInIterable;
module.exports.countIterable = countIterable;

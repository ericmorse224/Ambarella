import stringWidth from 'string-width';
import stripAnsi from 'strip-ansi';
import ansiStyles from 'ansi-styles';

const ESCAPES = new Set([
	'\u001B',
	'\u009B',
]);

const END_CODE = 39;
const ANSI_ESCAPE_BELL = '\u0007';
const ANSI_CSI = '[';
const ANSI_OSC = ']';
const ANSI_SGR_TERMINATOR = 'm';
const ANSI_ESCAPE_LINK = `${ANSI_OSC***REMOVED***8;;`;

const wrapAnsiCode = code => `${ESCAPES.values().next().value***REMOVED***${ANSI_CSI***REMOVED***${code***REMOVED***${ANSI_SGR_TERMINATOR***REMOVED***`;
const wrapAnsiHyperlink = uri => `${ESCAPES.values().next().value***REMOVED***${ANSI_ESCAPE_LINK***REMOVED***${uri***REMOVED***${ANSI_ESCAPE_BELL***REMOVED***`;

// Calculate the length of words split on ' ', ignoring
// the extra characters added by ansi escape codes
const wordLengths = string => string.split(' ').map(character => stringWidth(character));

// Wrap a long word across multiple rows
// Ansi escape codes do not count towards length
const wrapWord = (rows, word, columns) => {
	const characters = [...word];

	let isInsideEscape = false;
	let isInsideLinkEscape = false;
	let visible = stringWidth(stripAnsi(rows[rows.length - 1]));

	for (const [index, character] of characters.entries()) {
		const characterLength = stringWidth(character);

		if (visible + characterLength <= columns) {
			rows[rows.length - 1] += character;
		***REMOVED*** else {
			rows.push(character);
			visible = 0;
		***REMOVED***

		if (ESCAPES.has(character)) {
			isInsideEscape = true;
			isInsideLinkEscape = characters.slice(index + 1).join('').startsWith(ANSI_ESCAPE_LINK);
		***REMOVED***

		if (isInsideEscape) {
			if (isInsideLinkEscape) {
				if (character === ANSI_ESCAPE_BELL) {
					isInsideEscape = false;
					isInsideLinkEscape = false;
				***REMOVED***
			***REMOVED*** else if (character === ANSI_SGR_TERMINATOR) {
				isInsideEscape = false;
			***REMOVED***

			continue;
		***REMOVED***

		visible += characterLength;

		if (visible === columns && index < characters.length - 1) {
			rows.push('');
			visible = 0;
		***REMOVED***
	***REMOVED***

	// It's possible that the last row we copy over is only
	// ansi escape characters, handle this edge-case
	if (!visible && rows[rows.length - 1].length > 0 && rows.length > 1) {
		rows[rows.length - 2] += rows.pop();
	***REMOVED***
***REMOVED***;

// Trims spaces from a string ignoring invisible sequences
const stringVisibleTrimSpacesRight = string => {
	const words = string.split(' ');
	let last = words.length;

	while (last > 0) {
		if (stringWidth(words[last - 1]) > 0) {
			break;
		***REMOVED***

		last--;
	***REMOVED***

	if (last === words.length) {
		return string;
	***REMOVED***

	return words.slice(0, last).join(' ') + words.slice(last).join('');
***REMOVED***;

// The wrap-ansi module can be invoked in either 'hard' or 'soft' wrap mode
//
// 'hard' will never allow a string to take up more than columns characters
//
// 'soft' allows long words to expand past the column length
const exec = (string, columns, options = {***REMOVED***) => {
	if (options.trim !== false && string.trim() === '') {
		return '';
	***REMOVED***

	let returnValue = '';
	let escapeCode;
	let escapeUrl;

	const lengths = wordLengths(string);
	let rows = [''];

	for (const [index, word] of string.split(' ').entries()) {
		if (options.trim !== false) {
			rows[rows.length - 1] = rows[rows.length - 1].trimStart();
		***REMOVED***

		let rowLength = stringWidth(rows[rows.length - 1]);

		if (index !== 0) {
			if (rowLength >= columns && (options.wordWrap === false || options.trim === false)) {
				// If we start with a new word but the current row length equals the length of the columns, add a new row
				rows.push('');
				rowLength = 0;
			***REMOVED***

			if (rowLength > 0 || options.trim === false) {
				rows[rows.length - 1] += ' ';
				rowLength++;
			***REMOVED***
		***REMOVED***

		// In 'hard' wrap mode, the length of a line is never allowed to extend past 'columns'
		if (options.hard && lengths[index] > columns) {
			const remainingColumns = (columns - rowLength);
			const breaksStartingThisLine = 1 + Math.floor((lengths[index] - remainingColumns - 1) / columns);
			const breaksStartingNextLine = Math.floor((lengths[index] - 1) / columns);
			if (breaksStartingNextLine < breaksStartingThisLine) {
				rows.push('');
			***REMOVED***

			wrapWord(rows, word, columns);
			continue;
		***REMOVED***

		if (rowLength + lengths[index] > columns && rowLength > 0 && lengths[index] > 0) {
			if (options.wordWrap === false && rowLength < columns) {
				wrapWord(rows, word, columns);
				continue;
			***REMOVED***

			rows.push('');
		***REMOVED***

		if (rowLength + lengths[index] > columns && options.wordWrap === false) {
			wrapWord(rows, word, columns);
			continue;
		***REMOVED***

		rows[rows.length - 1] += word;
	***REMOVED***

	if (options.trim !== false) {
		rows = rows.map(row => stringVisibleTrimSpacesRight(row));
	***REMOVED***

	const pre = [...rows.join('\n')];

	for (const [index, character] of pre.entries()) {
		returnValue += character;

		if (ESCAPES.has(character)) {
			const {groups***REMOVED*** = new RegExp(`(?:\\${ANSI_CSI***REMOVED***(?<code>\\d+)m|\\${ANSI_ESCAPE_LINK***REMOVED***(?<uri>.*)${ANSI_ESCAPE_BELL***REMOVED***)`).exec(pre.slice(index).join('')) || {groups: {***REMOVED******REMOVED***;
			if (groups.code !== undefined) {
				const code = Number.parseFloat(groups.code);
				escapeCode = code === END_CODE ? undefined : code;
			***REMOVED*** else if (groups.uri !== undefined) {
				escapeUrl = groups.uri.length === 0 ? undefined : groups.uri;
			***REMOVED***
		***REMOVED***

		const code = ansiStyles.codes.get(Number(escapeCode));

		if (pre[index + 1] === '\n') {
			if (escapeUrl) {
				returnValue += wrapAnsiHyperlink('');
			***REMOVED***

			if (escapeCode && code) {
				returnValue += wrapAnsiCode(code);
			***REMOVED***
		***REMOVED*** else if (character === '\n') {
			if (escapeCode && code) {
				returnValue += wrapAnsiCode(escapeCode);
			***REMOVED***

			if (escapeUrl) {
				returnValue += wrapAnsiHyperlink(escapeUrl);
			***REMOVED***
		***REMOVED***
	***REMOVED***

	return returnValue;
***REMOVED***;

// For each newline, invoke the method separately
export default function wrapAnsi(string, columns, options) {
	return String(string)
		.normalize()
		.replace(/\r\n/g, '\n')
		.split('\n')
		.map(line => exec(line, columns, options))
		.join('\n');
***REMOVED***

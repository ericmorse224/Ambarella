'use strict';

var define = require('define-properties');
var getPolyfill = require('./polyfill');

var $IteratorPrototype = require('../Iterator.prototype/implementation');

module.exports = function shimIteratorPrototypeTake() {
	var polyfill = getPolyfill();

	define(
		$IteratorPrototype,
		{ take: polyfill ***REMOVED***,
		{ take: function () { return $IteratorPrototype.take !== polyfill; ***REMOVED*** ***REMOVED***
	);

	return polyfill;
***REMOVED***;

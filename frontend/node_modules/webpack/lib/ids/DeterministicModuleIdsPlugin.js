/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Florent Cailhol @ooflorent
*/

"use strict";

const {
	compareModulesByPreOrderIndexOrIdentifier
***REMOVED*** = require("../util/comparators");
const {
	getUsedModuleIdsAndModules,
	getFullModuleName,
	assignDeterministicIds
***REMOVED*** = require("./IdHelpers");

/** @typedef {import("../Compiler")***REMOVED*** Compiler */
/** @typedef {import("../Module")***REMOVED*** Module */

/**
 * @typedef {object***REMOVED*** DeterministicModuleIdsPluginOptions
 * @property {string=***REMOVED*** context context relative to which module identifiers are computed
 * @property {((module: Module) => boolean)=***REMOVED*** test selector function for modules
 * @property {number=***REMOVED*** maxLength maximum id length in digits (used as starting point)
 * @property {number=***REMOVED*** salt hash salt for ids
 * @property {boolean=***REMOVED*** fixedLength do not increase the maxLength to find an optimal id space size
 * @property {boolean=***REMOVED*** failOnConflict throw an error when id conflicts occur (instead of rehashing)
 */

const PLUGIN_NAME = "DeterministicModuleIdsPlugin";

class DeterministicModuleIdsPlugin {
	/**
	 * @param {DeterministicModuleIdsPluginOptions=***REMOVED*** options options
	 */
	constructor(options = {***REMOVED***) {
		this.options = options;
	***REMOVED***

	/**
	 * Apply the plugin
	 * @param {Compiler***REMOVED*** compiler the compiler instance
	 * @returns {void***REMOVED***
	 */
	apply(compiler) {
		compiler.hooks.compilation.tap(PLUGIN_NAME, compilation => {
			compilation.hooks.moduleIds.tap(PLUGIN_NAME, () => {
				const chunkGraph = compilation.chunkGraph;
				const context = this.options.context
					? this.options.context
					: compiler.context;
				const maxLength = this.options.maxLength || 3;
				const failOnConflict = this.options.failOnConflict || false;
				const fixedLength = this.options.fixedLength || false;
				const salt = this.options.salt || 0;
				let conflicts = 0;

				const [usedIds, modules] = getUsedModuleIdsAndModules(
					compilation,
					this.options.test
				);
				assignDeterministicIds(
					modules,
					module => getFullModuleName(module, context, compiler.root),
					failOnConflict
						? () => 0
						: compareModulesByPreOrderIndexOrIdentifier(
								compilation.moduleGraph
							),
					(module, id) => {
						const size = usedIds.size;
						usedIds.add(`${id***REMOVED***`);
						if (size === usedIds.size) {
							conflicts++;
							return false;
						***REMOVED***
						chunkGraph.setModuleId(module, id);
						return true;
					***REMOVED***,
					[10 ** maxLength],
					fixedLength ? 0 : 10,
					usedIds.size,
					salt
				);
				if (failOnConflict && conflicts)
					throw new Error(
						`Assigning deterministic module ids has lead to ${conflicts***REMOVED*** conflict${
							conflicts > 1 ? "s" : ""
						***REMOVED***.\nIncrease the 'maxLength' to increase the id space and make conflicts less likely (recommended when there are many conflicts or application is expected to grow), or add an 'salt' number to try another hash starting value in the same id space (recommended when there is only a single conflict).`
					);
			***REMOVED***);
		***REMOVED***);
	***REMOVED***
***REMOVED***

module.exports = DeterministicModuleIdsPlugin;

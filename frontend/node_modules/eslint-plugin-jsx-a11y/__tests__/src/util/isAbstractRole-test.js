import test from 'tape';
import { elementType ***REMOVED*** from 'jsx-ast-utils';

import isAbstractRole from '../../../src/util/isAbstractRole';
import {
  genElementSymbol,
  genAbstractRoleElements,
  genNonAbstractRoleElements,
***REMOVED*** from '../../../__mocks__/genInteractives';

test('isAbstractRole', (t) => {
  t.equal(
    isAbstractRole(undefined, []),
    false,
    'does NOT identify JSX Components (no tagName) as abstract role elements',
  );

  t.test('elements with an abstract role', (st) => {
    genAbstractRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;
      st.equal(
        isAbstractRole(
          elementType(openingElement),
          attributes,
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as an abstract role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('elements with a non-abstract role', (st) => {
    genNonAbstractRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;
      st.equal(
        isAbstractRole(
          elementType(openingElement),
          attributes,
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` as an abstract role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.end();
***REMOVED***);

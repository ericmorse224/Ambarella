import type {CodeKeywordDefinition, KeywordCxt***REMOVED*** from "ajv"
import {_***REMOVED*** from "ajv/dist/compile/codegen"

const TYPES = ["undefined", "string", "number", "object", "function", "boolean", "symbol"]

export default function getDef(): CodeKeywordDefinition {
  return {
    keyword: "typeof",
    schemaType: ["string", "array"],
    code(cxt: KeywordCxt) {
      const {data, schema, schemaValue***REMOVED*** = cxt
      cxt.fail(
        typeof schema == "string"
          ? _`typeof ${data***REMOVED*** != ${schema***REMOVED***`
          : _`${schemaValue***REMOVED***.indexOf(typeof ${data***REMOVED***) < 0`
      )
    ***REMOVED***,
    metaSchema: {
      anyOf: [
        {type: "string", enum: TYPES***REMOVED***,
        {type: "array", items: {type: "string", enum: TYPES***REMOVED******REMOVED***,
      ],
    ***REMOVED***,
  ***REMOVED***
***REMOVED***

module.exports = getDef

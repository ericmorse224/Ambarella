'use strict';
const BasePlugin = require('../plugin');
const { IE_8 ***REMOVED*** = require('../dictionary/browsers');
const { MEDIA_QUERY ***REMOVED*** = require('../dictionary/identifiers');
const { ATRULE ***REMOVED*** = require('../dictionary/postcss');

module.exports = class MediaSlash0 extends BasePlugin {
  /** @param {import('postcss').Result***REMOVED*** result */
  constructor(result) {
    super([IE_8], [ATRULE], result);
  ***REMOVED***
  /**
   * @param {import('postcss').AtRule***REMOVED*** rule
   * @return {void***REMOVED***
   */
  detect(rule) {
    const params = rule.params.trim();

    if (params.toLowerCase() === '\\0screen') {
      this.push(rule, {
        identifier: MEDIA_QUERY,
        hack: params,
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

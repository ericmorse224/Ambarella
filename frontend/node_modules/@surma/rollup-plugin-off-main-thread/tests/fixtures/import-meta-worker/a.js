let worker;

if (self.document) {
  worker = new Worker(import.meta.url);
***REMOVED***
if (self.postMessage) {
  setTimeout(() => {
    postMessage("a");
  ***REMOVED***, 500);
***REMOVED***

export { worker ***REMOVED***;

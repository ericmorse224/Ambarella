'use strict';

var index = require('../');
var callBind = require('call-bind');
var test = require('tape');
var runTests = require('./tests');

test('as a function', function (t) {
	t.test('bad array/this value', function (st) {
		st['throws'](callBind(index, null, undefined, function () {***REMOVED***), TypeError, 'undefined is not an object');
		st['throws'](callBind(index, null, null, function () {***REMOVED***), TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(index, t);

	t.end();
***REMOVED***);

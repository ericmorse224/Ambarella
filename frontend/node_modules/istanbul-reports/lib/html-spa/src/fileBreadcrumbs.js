const React = require('react');

module.exports = function FileBreadcrumbs({ fileFilter = '', setFileFilter ***REMOVED***) {
    const parts = fileFilter.split('/');
    const breadcrumbs = [
        {
            path: '',
            name: 'all files'
        ***REMOVED***,
        ...parts.map((part, i) => ({
            path: parts.slice(0, i + 1).join('/'),
            name: part
        ***REMOVED***))
    ];

    return breadcrumbs.map(({ path, name ***REMOVED***) =>
        path === fileFilter ? (
            name
        ) : (
            <>
                <a
                    href="javascript:void(0)"
                    onClick={() => setFileFilter(path)***REMOVED***
                >
                    {name***REMOVED***
                </a>
                /
            </>
        )
    );
***REMOVED***;

/* eslint no-console: "off" */

var asynckit = require('./')
  , async    = require('async')
  , assert   = require('assert')
  , expected = 0
  ;

var Benchmark = require('benchmark');
var suite = new Benchmark.Suite;

var source = [];
for (var z = 1; z < 100; z++)
{
  source.push(z);
  expected += z;
***REMOVED***

suite
// add tests

.add('async.map', function(deferred)
{
  var total = 0;

  async.map(source,
  function(i, cb)
  {
    setImmediate(function()
    {
      total += i;
      cb(null, total);
    ***REMOVED***);
  ***REMOVED***,
  function(err, result)
  {
    assert.ifError(err);
    assert.equal(result[result.length - 1], expected);
    deferred.resolve();
  ***REMOVED***);
***REMOVED***, {'defer': true***REMOVED***)


.add('asynckit.parallel', function(deferred)
{
  var total = 0;

  asynckit.parallel(source,
  function(i, cb)
  {
    setImmediate(function()
    {
      total += i;
      cb(null, total);
    ***REMOVED***);
  ***REMOVED***,
  function(err, result)
  {
    assert.ifError(err);
    assert.equal(result[result.length - 1], expected);
    deferred.resolve();
  ***REMOVED***);
***REMOVED***, {'defer': true***REMOVED***)


// add listeners
.on('cycle', function(ev)
{
  console.log(String(ev.target));
***REMOVED***)
.on('complete', function()
{
  console.log('Fastest is ' + this.filter('fastest').map('name'));
***REMOVED***)
// run async
.run({ 'async': true ***REMOVED***);

'use strict';

var test = require('tape');
var parse = require('../').parse;

function getEnv() {
	return 'xxx';
***REMOVED***

function getEnvObj() {
	return { op: '@@' ***REMOVED***;
***REMOVED***

test('functional env expansion', function (t) {
	t.plan(4);

	t.same(parse('a $XYZ c', getEnv), ['a', 'xxx', 'c']);
	t.same(parse('a $XYZ c', getEnvObj), ['a', { op: '@@' ***REMOVED***, 'c']);
	t.same(parse('a${XYZ***REMOVED***c', getEnvObj), ['a', { op: '@@' ***REMOVED***, 'c']);
	t.same(parse('"a $XYZ c"', getEnvObj), ['a ', { op: '@@' ***REMOVED***, ' c']);
***REMOVED***);

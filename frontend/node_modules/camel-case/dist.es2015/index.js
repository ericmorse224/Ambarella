import { __assign ***REMOVED*** from "tslib";
import { pascalCase, pascalCaseTransform, pascalCaseTransformMerge, ***REMOVED*** from "pascal-case";
export function camelCaseTransform(input, index) {
    if (index === 0)
        return input.toLowerCase();
    return pascalCaseTransform(input, index);
***REMOVED***
export function camelCaseTransformMerge(input, index) {
    if (index === 0)
        return input.toLowerCase();
    return pascalCaseTransformMerge(input);
***REMOVED***
export function camelCase(input, options) {
    if (options === void 0) { options = {***REMOVED***; ***REMOVED***
    return pascalCase(input, __assign({ transform: camelCaseTransform ***REMOVED***, options));
***REMOVED***
//# sourceMappingURL=index.js.map
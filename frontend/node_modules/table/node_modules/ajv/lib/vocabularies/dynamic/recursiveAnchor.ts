import type {CodeKeywordDefinition***REMOVED*** from "../../types"
import {dynamicAnchor***REMOVED*** from "./dynamicAnchor"
import {checkStrictMode***REMOVED*** from "../../compile/util"

const def: CodeKeywordDefinition = {
  keyword: "$recursiveAnchor",
  schemaType: "boolean",
  code(cxt) {
    if (cxt.schema) dynamicAnchor(cxt, "")
    else checkStrictMode(cxt.it, "$recursiveAnchor: false is ignored")
  ***REMOVED***,
***REMOVED***

export default def

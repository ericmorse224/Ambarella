"use strict";

exports.__esModule = true;
exports.default = void 0;

function one(h, node, parent) {
  const type = node && node.type;
  const fn = h.handlers[type];
  /* Fail on non-nodes. */

  if (!type) {
    throw new Error(`Expected node, got \`${node***REMOVED***\``);
  ***REMOVED***

  if (!fn) {
    throw new Error(`Node of type ${type***REMOVED*** is unknown`);
  ***REMOVED***

  return fn(h, node, parent);
***REMOVED***

var _default = one;
exports.default = _default;
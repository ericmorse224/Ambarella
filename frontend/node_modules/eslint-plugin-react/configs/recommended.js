'use strict';

const plugin = require('..');

const legacyConfig = plugin.configs.recommended;

module.exports = {
  plugins: { react: plugin ***REMOVED***,
  rules: legacyConfig.rules,
  languageOptions: { parserOptions: legacyConfig.parserOptions ***REMOVED***,
***REMOVED***;

Object.defineProperty(module.exports, 'languageOptions', { enumerable: false ***REMOVED***);

'use strict';
/**
 * @param {import('postcss').Rule***REMOVED*** rule
 * @param {string[]***REMOVED*** properties
 * @return {import('postcss').Declaration[]***REMOVED***
 */
module.exports = function getDecls(rule, properties) {
  return /** @type {import('postcss').Declaration[]***REMOVED*** */ (
    rule.nodes.filter(
      (node) =>
        node.type === 'decl' && properties.includes(node.prop.toLowerCase())
    )
  );
***REMOVED***;

import {Except***REMOVED*** from './except';

/**
Merge two types into a new type. Keys of the second type overrides keys of the first type.

@example
```
import {Merge***REMOVED*** from 'type-fest';

type Foo = {
	a: number;
	b: string;
***REMOVED***;

type Bar = {
	b: number;
***REMOVED***;

const ab: Merge<Foo, Bar> = {a: 1, b: 2***REMOVED***;
```
*/
export type Merge<FirstType, SecondType> = Except<FirstType, Extract<keyof FirstType, keyof SecondType>> & SecondType;

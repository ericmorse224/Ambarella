#!/usr/bin/env node

var fs = require('fs')
var updateDb = require('update-browserslist-db')

var browserslist = require('./')
var pkg = require('./package.json')

var args = process.argv.slice(2)

var USAGE =
  'Usage:\n' +
  '  npx browserslist\n' +
  '  npx browserslist "QUERIES"\n' +
  '  npx browserslist --json "QUERIES"\n' +
  '  npx browserslist --config="path/to/browserlist/file"\n' +
  '  npx browserslist --coverage "QUERIES"\n' +
  '  npx browserslist --coverage=US "QUERIES"\n' +
  '  npx browserslist --coverage=US,RU,global "QUERIES"\n' +
  '  npx browserslist --env="environment name defined in config"\n' +
  '  npx browserslist --stats="path/to/browserlist/stats/file"\n' +
  '  npx browserslist --mobile-to-desktop\n' +
  '  npx browserslist --ignore-unknown-versions\n'

function isArg(arg) {
  return args.some(function (str) {
    return str === arg || str.indexOf(arg + '=') === 0
  ***REMOVED***)
***REMOVED***

function error(msg) {
  process.stderr.write('browserslist: ' + msg + '\n')
  process.exit(1)
***REMOVED***

if (isArg('--help') || isArg('-h')) {
  process.stdout.write(pkg.description + '.\n\n' + USAGE + '\n')
***REMOVED*** else if (isArg('--version') || isArg('-v')) {
  process.stdout.write('browserslist ' + pkg.version + '\n')
***REMOVED*** else if (isArg('--update-db')) {
  /* c8 ignore next 8 */
  process.stdout.write(
    'The --update-db command is deprecated.\n' +
      'Please use npx update-browserslist-db@latest instead.\n'
  )
  process.stdout.write('Browserslist DB update will still be made.\n')
  updateDb(function (str) {
    process.stdout.write(str)
  ***REMOVED***)
***REMOVED*** else {
  var mode = 'browsers'
  var opts = {***REMOVED***
  var queries
  var areas

  for (var i = 0; i < args.length; i++) {
    if (args[i][0] !== '-') {
      queries = args[i].replace(/^["']|["']$/g, '')
      continue
    ***REMOVED***

    var arg = args[i].split('=')
    var name = arg[0]
    var value = arg[1]

    if (value) value = value.replace(/^["']|["']$/g, '')

    if (name === '--config' || name === '-b') {
      opts.config = value
    ***REMOVED*** else if (name === '--env' || name === '-e') {
      opts.env = value
    ***REMOVED*** else if (name === '--stats' || name === '-s') {
      opts.stats = value
    ***REMOVED*** else if (name === '--coverage' || name === '-c') {
      if (mode !== 'json') mode = 'coverage'
      if (value) {
        areas = value.split(',')
      ***REMOVED*** else {
        areas = ['global']
      ***REMOVED***
    ***REMOVED*** else if (name === '--json') {
      mode = 'json'
    ***REMOVED*** else if (name === '--mobile-to-desktop') {
      /* c8 ignore next */
      opts.mobileToDesktop = true
    ***REMOVED*** else if (name === '--ignore-unknown-versions') {
      /* c8 ignore next */
      opts.ignoreUnknownVersions = true
    ***REMOVED*** else {
      error('Unknown arguments ' + args[i] + '.\n\n' + USAGE)
    ***REMOVED***
  ***REMOVED***

  var browsers
  try {
    browsers = browserslist(queries, opts)
  ***REMOVED*** catch (e) {
    if (e.name === 'BrowserslistError') {
      error(e.message)
    ***REMOVED*** /* c8 ignore start */ else {
      throw e
    ***REMOVED*** /* c8 ignore end */
  ***REMOVED***

  var coverage
  if (mode === 'browsers') {
    browsers.forEach(function (browser) {
      process.stdout.write(browser + '\n')
    ***REMOVED***)
  ***REMOVED*** else if (areas) {
    coverage = areas.map(function (area) {
      var stats
      if (area !== 'global') {
        stats = area
      ***REMOVED*** else if (opts.stats) {
        stats = JSON.parse(fs.readFileSync(opts.stats))
      ***REMOVED***
      var result = browserslist.coverage(browsers, stats)
      var round = Math.round(result * 100) / 100.0

      return [area, round]
    ***REMOVED***)

    if (mode === 'coverage') {
      var prefix = 'These browsers account for '
      process.stdout.write(prefix)
      coverage.forEach(function (data, index) {
        var area = data[0]
        var round = data[1]
        var end = 'globally'
        if (area && area !== 'global') {
          end = 'in the ' + area.toUpperCase()
        ***REMOVED*** else if (opts.stats) {
          end = 'in custom statistics'
        ***REMOVED***

        if (index !== 0) {
          process.stdout.write(prefix.replace(/./g, ' '))
        ***REMOVED***

        process.stdout.write(round + '% of all users ' + end + '\n')
      ***REMOVED***)
    ***REMOVED***
  ***REMOVED***

  if (mode === 'json') {
    var data = { browsers: browsers ***REMOVED***
    if (coverage) {
      data.coverage = coverage.reduce(function (object, j) {
        object[j[0]] = j[1]
        return object
      ***REMOVED***, {***REMOVED***)
    ***REMOVED***
    process.stdout.write(JSON.stringify(data, null, '  ') + '\n')
  ***REMOVED***
***REMOVED***

function debug(logLevel, ...messages) {
    if (logLevel === 'debug')
        console.log(...messages);
***REMOVED***
function warn(logLevel, warning) {
    if (logLevel === 'debug' || logLevel === 'warn') {
        console.warn(warning);
    ***REMOVED***
***REMOVED***

export { debug, warn ***REMOVED***;

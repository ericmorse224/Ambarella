/**
 * @fileoverview Enforce scope prop is only used on <th> elements.
 * @author Ethan Cohen
 */

// -----------------------------------------------------------------------------
// Requirements
// -----------------------------------------------------------------------------

import { RuleTester ***REMOVED*** from 'eslint';
import parserOptionsMapper from '../../__util__/parserOptionsMapper';
import parsers from '../../__util__/helpers/parsers';
import rule from '../../../src/rules/scope';

// -----------------------------------------------------------------------------
// Tests
// -----------------------------------------------------------------------------

const ruleTester = new RuleTester();

const expectedError = {
  message: 'The scope prop can only be used on <th> elements.',
  type: 'JSXAttribute',
***REMOVED***;

const componentsSettings = {
  'jsx-a11y': {
    components: {
      Foo: 'div',
      TableHeader: 'th',
    ***REMOVED***,
  ***REMOVED***,
***REMOVED***;

ruleTester.run('scope', rule, {
  valid: parsers.all([].concat(
    { code: '<div />;' ***REMOVED***,
    { code: '<div foo />;' ***REMOVED***,
    { code: '<th scope />' ***REMOVED***,
    { code: '<th scope="row" />' ***REMOVED***,
    { code: '<th scope={foo***REMOVED*** />' ***REMOVED***,
    { code: '<th scope={"col"***REMOVED*** {...props***REMOVED*** />' ***REMOVED***,
    { code: '<Foo scope="bar" {...props***REMOVED*** />' ***REMOVED***,
    { code: '<TableHeader scope="row" />', settings: componentsSettings ***REMOVED***,
  )).map(parserOptionsMapper),
  invalid: parsers.all([].concat(
    { code: '<div scope />', errors: [expectedError] ***REMOVED***,
    { code: '<Foo scope="bar" />', settings: componentsSettings, errors: [expectedError] ***REMOVED***,
  )).map(parserOptionsMapper),
***REMOVED***);

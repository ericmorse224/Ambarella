import uri, { URIComponents, URIComponent, Options, options ***REMOVED*** from '..'
import { expectDeprecated, expectType ***REMOVED*** from 'tsd'

const parsed = uri.parse('foo')
expectType<URIComponents>(parsed)
const parsed2 = uri.parse('foo', {
  domainHost: true,
  scheme: 'https',
  unicodeSupport: false
***REMOVED***)
expectType<URIComponents>(parsed2)

expectType<URIComponent>({***REMOVED*** as URIComponents)
expectDeprecated({***REMOVED*** as URIComponents)

expectType<Options>({***REMOVED*** as options)
expectDeprecated({***REMOVED*** as options)

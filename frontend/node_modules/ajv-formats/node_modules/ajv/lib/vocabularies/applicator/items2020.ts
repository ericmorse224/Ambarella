import type {
  CodeKeywordDefinition,
  KeywordErrorDefinition,
  ErrorObject,
  AnySchema,
***REMOVED*** from "../../types"
import type {KeywordCxt***REMOVED*** from "../../compile/validate"
import {_, str***REMOVED*** from "../../compile/codegen"
import {alwaysValidSchema***REMOVED*** from "../../compile/util"
import {validateArray***REMOVED*** from "../code"
import {validateAdditionalItems***REMOVED*** from "./additionalItems"

export type ItemsError = ErrorObject<"items", {limit: number***REMOVED***, AnySchema>

const error: KeywordErrorDefinition = {
  message: ({params: {len***REMOVED******REMOVED***) => str`must NOT have more than ${len***REMOVED*** items`,
  params: ({params: {len***REMOVED******REMOVED***) => _`{limit: ${len***REMOVED******REMOVED***`,
***REMOVED***

const def: CodeKeywordDefinition = {
  keyword: "items",
  type: "array",
  schemaType: ["object", "boolean"],
  before: "uniqueItems",
  error,
  code(cxt: KeywordCxt) {
    const {schema, parentSchema, it***REMOVED*** = cxt
    const {prefixItems***REMOVED*** = parentSchema
    it.items = true
    if (alwaysValidSchema(it, schema)) return
    if (prefixItems) validateAdditionalItems(cxt, prefixItems)
    else cxt.ok(validateArray(cxt))
  ***REMOVED***,
***REMOVED***

export default def

import { spawn as nodeSpawn, ***REMOVED*** from 'child_process';
import crossSpawn from 'cross-spawn';
import { onExit ***REMOVED*** from 'signal-exit';
import { proxySignals ***REMOVED*** from './proxy-signals.js';
import { watchdog ***REMOVED*** from './watchdog.js';
/* c8 ignore start */
const spawn = process?.platform === 'win32' ? crossSpawn : nodeSpawn;
/**
 * Normalizes the arguments passed to `foregroundChild`.
 *
 * Exposed for testing.
 *
 * @internal
 */
export const normalizeFgArgs = (fgArgs) => {
    let [program, args = [], spawnOpts = {***REMOVED***, cleanup = () => { ***REMOVED***] = fgArgs;
    if (typeof args === 'function') {
        cleanup = args;
        spawnOpts = {***REMOVED***;
        args = [];
    ***REMOVED***
    else if (!!args && typeof args === 'object' && !Array.isArray(args)) {
        if (typeof spawnOpts === 'function')
            cleanup = spawnOpts;
        spawnOpts = args;
        args = [];
    ***REMOVED***
    else if (typeof spawnOpts === 'function') {
        cleanup = spawnOpts;
        spawnOpts = {***REMOVED***;
    ***REMOVED***
    if (Array.isArray(program)) {
        const [pp, ...pa] = program;
        program = pp;
        args = pa;
    ***REMOVED***
    return [program, args, { ...spawnOpts ***REMOVED***, cleanup];
***REMOVED***;
export function foregroundChild(...fgArgs) {
    const [program, args, spawnOpts, cleanup] = normalizeFgArgs(fgArgs);
    spawnOpts.stdio = [0, 1, 2];
    if (process.send) {
        spawnOpts.stdio.push('ipc');
    ***REMOVED***
    const child = spawn(program, args, spawnOpts);
    const childHangup = () => {
        try {
            child.kill('SIGHUP');
            /* c8 ignore start */
        ***REMOVED***
        catch (_) {
            // SIGHUP is weird on windows
            child.kill('SIGTERM');
        ***REMOVED***
        /* c8 ignore stop */
    ***REMOVED***;
    const removeOnExit = onExit(childHangup);
    proxySignals(child);
    const dog = watchdog(child);
    let done = false;
    child.on('close', async (code, signal) => {
        /* c8 ignore start */
        if (done)
            return;
        /* c8 ignore stop */
        done = true;
        const result = cleanup(code, signal, {
            watchdogPid: dog.pid,
        ***REMOVED***);
        const res = isPromise(result) ? await result : result;
        removeOnExit();
        if (res === false)
            return;
        else if (typeof res === 'string') {
            signal = res;
            code = null;
        ***REMOVED***
        else if (typeof res === 'number') {
            code = res;
            signal = null;
        ***REMOVED***
        if (signal) {
            // If there is nothing else keeping the event loop alive,
            // then there's a race between a graceful exit and getting
            // the signal to this process.  Put this timeout here to
            // make sure we're still alive to get the signal, and thus
            // exit with the intended signal code.
            /* istanbul ignore next */
            setTimeout(() => { ***REMOVED***, 2000);
            try {
                process.kill(process.pid, signal);
                /* c8 ignore start */
            ***REMOVED***
            catch (_) {
                process.kill(process.pid, 'SIGTERM');
            ***REMOVED***
            /* c8 ignore stop */
        ***REMOVED***
        else {
            process.exit(code || 0);
        ***REMOVED***
    ***REMOVED***);
    if (process.send) {
        process.removeAllListeners('message');
        child.on('message', (message, sendHandle) => {
            process.send?.(message, sendHandle);
        ***REMOVED***);
        process.on('message', (message, sendHandle) => {
            child.send(message, sendHandle);
        ***REMOVED***);
    ***REMOVED***
    return child;
***REMOVED***
const isPromise = (o) => !!o && typeof o === 'object' && typeof o.then === 'function';
//# sourceMappingURL=index.js.map
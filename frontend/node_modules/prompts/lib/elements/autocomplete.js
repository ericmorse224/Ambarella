'use strict';

const color = require('kleur');
const Prompt = require('./prompt');
const { erase, cursor ***REMOVED*** = require('sisteransi');
const { style, clear, figures, wrap, entriesToDisplay ***REMOVED*** = require('../util');

const getVal = (arr, i) => arr[i] && (arr[i].value || arr[i].title || arr[i]);
const getTitle = (arr, i) => arr[i] && (arr[i].title || arr[i].value || arr[i]);
const getIndex = (arr, valOrTitle) => {
  const index = arr.findIndex(el => el.value === valOrTitle || el.title === valOrTitle);
  return index > -1 ? index : undefined;
***REMOVED***;

/**
 * TextPrompt Base Element
 * @param {Object***REMOVED*** opts Options
 * @param {String***REMOVED*** opts.message Message
 * @param {Array***REMOVED*** opts.choices Array of auto-complete choices objects
 * @param {Function***REMOVED*** [opts.suggest] Filter function. Defaults to sort by title
 * @param {Number***REMOVED*** [opts.limit=10] Max number of results to show
 * @param {Number***REMOVED*** [opts.cursor=0] Cursor start position
 * @param {String***REMOVED*** [opts.style='default'] Render style
 * @param {String***REMOVED*** [opts.fallback] Fallback message - initial to default value
 * @param {String***REMOVED*** [opts.initial] Index of the default value
 * @param {Boolean***REMOVED*** [opts.clearFirst] The first ESCAPE keypress will clear the input
 * @param {Stream***REMOVED*** [opts.stdin] The Readable stream to listen to
 * @param {Stream***REMOVED*** [opts.stdout] The Writable stream to write readline data to
 * @param {String***REMOVED*** [opts.noMatches] The no matches found label
 */
class AutocompletePrompt extends Prompt {
  constructor(opts={***REMOVED***) {
    super(opts);
    this.msg = opts.message;
    this.suggest = opts.suggest;
    this.choices = opts.choices;
    this.initial = typeof opts.initial === 'number'
      ? opts.initial
      : getIndex(opts.choices, opts.initial);
    this.select = this.initial || opts.cursor || 0;
    this.i18n = { noMatches: opts.noMatches || 'no matches found' ***REMOVED***;
    this.fallback = opts.fallback || this.initial;
    this.clearFirst = opts.clearFirst || false;
    this.suggestions = [];
    this.input = '';
    this.limit = opts.limit || 10;
    this.cursor = 0;
    this.transform = style.render(opts.style);
    this.scale = this.transform.scale;
    this.render = this.render.bind(this);
    this.complete = this.complete.bind(this);
    this.clear = clear('', this.out.columns);
    this.complete(this.render);
    this.render();
  ***REMOVED***

  set fallback(fb) {
    this._fb = Number.isSafeInteger(parseInt(fb)) ? parseInt(fb) : fb;
  ***REMOVED***

  get fallback() {
    let choice;
    if (typeof this._fb === 'number')
      choice = this.choices[this._fb];
    else if (typeof this._fb === 'string')
      choice = { title: this._fb ***REMOVED***;
    return choice || this._fb || { title: this.i18n.noMatches ***REMOVED***;
  ***REMOVED***

  moveSelect(i) {
    this.select = i;
    if (this.suggestions.length > 0)
      this.value = getVal(this.suggestions, i);
    else this.value = this.fallback.value;
    this.fire();
  ***REMOVED***

  async complete(cb) {
    const p = (this.completing = this.suggest(this.input, this.choices));
    const suggestions = await p;

    if (this.completing !== p) return;
    this.suggestions = suggestions
      .map((s, i, arr) => ({ title: getTitle(arr, i), value: getVal(arr, i), description: s.description ***REMOVED***));
    this.completing = false;
    const l = Math.max(suggestions.length - 1, 0);
    this.moveSelect(Math.min(l, this.select));

    cb && cb();
  ***REMOVED***

  reset() {
    this.input = '';
    this.complete(() => {
      this.moveSelect(this.initial !== void 0 ? this.initial : 0);
      this.render();
    ***REMOVED***);
    this.render();
  ***REMOVED***

  exit() {
    if (this.clearFirst && this.input.length > 0) {
      this.reset();
    ***REMOVED*** else {
      this.done = this.exited = true; 
      this.aborted = false;
      this.fire();
      this.render();
      this.out.write('\n');
      this.close();
    ***REMOVED***
  ***REMOVED***

  abort() {
    this.done = this.aborted = true;
    this.exited = false;
    this.fire();
    this.render();
    this.out.write('\n');
    this.close();
  ***REMOVED***

  submit() {
    this.done = true;
    this.aborted = this.exited = false;
    this.fire();
    this.render();
    this.out.write('\n');
    this.close();
  ***REMOVED***

  _(c, key) {
    let s1 = this.input.slice(0, this.cursor);
    let s2 = this.input.slice(this.cursor);
    this.input = `${s1***REMOVED***${c***REMOVED***${s2***REMOVED***`;
    this.cursor = s1.length+1;
    this.complete(this.render);
    this.render();
  ***REMOVED***

  delete() {
    if (this.cursor === 0) return this.bell();
    let s1 = this.input.slice(0, this.cursor-1);
    let s2 = this.input.slice(this.cursor);
    this.input = `${s1***REMOVED***${s2***REMOVED***`;
    this.complete(this.render);
    this.cursor = this.cursor-1;
    this.render();
  ***REMOVED***

  deleteForward() {
    if(this.cursor*this.scale >= this.rendered.length) return this.bell();
    let s1 = this.input.slice(0, this.cursor);
    let s2 = this.input.slice(this.cursor+1);
    this.input = `${s1***REMOVED***${s2***REMOVED***`;
    this.complete(this.render);
    this.render();
  ***REMOVED***

  first() {
    this.moveSelect(0);
    this.render();
  ***REMOVED***

  last() {
    this.moveSelect(this.suggestions.length - 1);
    this.render();
  ***REMOVED***

  up() {
    if (this.select === 0) {
      this.moveSelect(this.suggestions.length - 1);
    ***REMOVED*** else {
      this.moveSelect(this.select - 1);
    ***REMOVED***
    this.render();
  ***REMOVED***

  down() {
    if (this.select === this.suggestions.length - 1) {
      this.moveSelect(0);
    ***REMOVED*** else {
      this.moveSelect(this.select + 1);
    ***REMOVED***
    this.render();
  ***REMOVED***

  next() {
    if (this.select === this.suggestions.length - 1) {
      this.moveSelect(0);
    ***REMOVED*** else this.moveSelect(this.select + 1);
    this.render();
  ***REMOVED***

  nextPage() {
    this.moveSelect(Math.min(this.select + this.limit, this.suggestions.length - 1));
    this.render();
  ***REMOVED***

  prevPage() {
    this.moveSelect(Math.max(this.select - this.limit, 0));
    this.render();
  ***REMOVED***

  left() {
    if (this.cursor <= 0) return this.bell();
    this.cursor = this.cursor-1;
    this.render();
  ***REMOVED***

  right() {
    if (this.cursor*this.scale >= this.rendered.length) return this.bell();
    this.cursor = this.cursor+1;
    this.render();
  ***REMOVED***

  renderOption(v, hovered, isStart, isEnd) {
    let desc;
    let prefix = isStart ? figures.arrowUp : isEnd ? figures.arrowDown : ' ';
    let title = hovered ? color.cyan().underline(v.title) : v.title;
    prefix = (hovered ? color.cyan(figures.pointer) + ' ' : '  ') + prefix;
    if (v.description) {
      desc = ` - ${v.description***REMOVED***`;
      if (prefix.length + title.length + desc.length >= this.out.columns
        || v.description.split(/\r?\n/).length > 1) {
        desc = '\n' + wrap(v.description, { margin: 3, width: this.out.columns ***REMOVED***)
      ***REMOVED***
    ***REMOVED***
    return prefix + ' ' + title + color.gray(desc || '');
  ***REMOVED***

  render() {
    if (this.closed) return;
    if (this.firstRender) this.out.write(cursor.hide);
    else this.out.write(clear(this.outputText, this.out.columns));
    super.render();

    let { startIndex, endIndex ***REMOVED*** = entriesToDisplay(this.select, this.choices.length, this.limit);

    this.outputText = [
      style.symbol(this.done, this.aborted, this.exited),
      color.bold(this.msg),
      style.delimiter(this.completing),
      this.done && this.suggestions[this.select]
        ? this.suggestions[this.select].title
        : this.rendered = this.transform.render(this.input)
    ].join(' ');

    if (!this.done) {
      const suggestions = this.suggestions
        .slice(startIndex, endIndex)
        .map((item, i) =>  this.renderOption(item,
          this.select === i + startIndex,
          i === 0 && startIndex > 0,
          i + startIndex === endIndex - 1 && endIndex < this.choices.length))
        .join('\n');
      this.outputText += `\n` + (suggestions || color.gray(this.fallback.title));
    ***REMOVED***

    this.out.write(erase.line + cursor.to(0) + this.outputText);
  ***REMOVED***
***REMOVED***

module.exports = AutocompletePrompt;

'use strict';

class Node {
  constructor(node) {
    this.type = node.type;
    if (node.value) this.value = node.value;
    if (node.match) this.match = node.match;
    this.newline = node.newline || '';
  ***REMOVED***
  get protected() {
    return Boolean(this.match) && this.match[1] === '!';
  ***REMOVED***
***REMOVED***

class Block extends Node {
  constructor(node) {
    super(node);
    this.nodes = node.nodes || [];
  ***REMOVED***
  push(node) {
    this.nodes.push(node);
  ***REMOVED***
  get protected() {
    return this.nodes.length > 0 && this.nodes[0].protected === true;
  ***REMOVED***
***REMOVED***

module.exports = { Node, Block ***REMOVED***;

import type { CreateNodeContext ***REMOVED*** from '../doc/createNode';
import type { CollectionItem ***REMOVED*** from '../parse/cst';
import type { Schema ***REMOVED*** from '../schema/Schema';
import type { StringifyContext ***REMOVED*** from '../stringify/stringify';
import { addPairToJSMap ***REMOVED*** from './addPairToJSMap';
import { NODE_TYPE ***REMOVED*** from './identity';
import type { Node ***REMOVED*** from './Node';
import type { ToJSContext ***REMOVED*** from './toJS';
export declare function createPair(key: unknown, value: unknown, ctx: CreateNodeContext): Pair<Node, Node>;
export declare class Pair<K = unknown, V = unknown> {
    readonly [NODE_TYPE]: symbol;
    /** Always Node or null when parsed, but can be set to anything. */
    key: K;
    /** Always Node or null when parsed, but can be set to anything. */
    value: V | null;
    /** The CST token that was composed into this pair.  */
    srcToken?: CollectionItem;
    constructor(key: K, value?: V | null);
    clone(schema?: Schema): Pair<K, V>;
    toJSON(_?: unknown, ctx?: ToJSContext): ReturnType<typeof addPairToJSMap>;
    toString(ctx?: StringifyContext, onComment?: () => void, onChompKeep?: () => void): string;
***REMOVED***

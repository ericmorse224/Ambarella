const { d, n ***REMOVED*** = require('../../options');

/**
 * Normalizes the options for the loader.
 * @param {import('../types').ReactRefreshLoaderOptions***REMOVED*** options Non-normalized loader options.
 * @returns {import('../types').NormalizedLoaderOptions***REMOVED*** Normalized loader options.
 */
const normalizeOptions = (options) => {
  d(options, 'const', false);

  n(options, 'esModule', (esModule) => {
    if (typeof esModule === 'boolean' || typeof esModule === 'undefined') {
      return esModule;
    ***REMOVED***

    d(esModule, 'include');
    d(esModule, 'exclude');

    return esModule;
  ***REMOVED***);

  return options;
***REMOVED***;

module.exports = normalizeOptions;

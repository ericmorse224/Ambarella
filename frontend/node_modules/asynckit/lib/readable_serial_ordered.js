var serialOrdered = require('../serialOrdered.js');

// API
module.exports = ReadableSerialOrdered;
// expose sort helpers
module.exports.ascending  = serialOrdered.ascending;
module.exports.descending = serialOrdered.descending;

/**
 * Streaming wrapper to `asynckit.serialOrdered`
 *
 * @param   {array|object***REMOVED*** list - array or object (named list) to iterate over
 * @param   {function***REMOVED*** iterator - iterator to run
 * @param   {function***REMOVED*** sortMethod - custom sort function
 * @param   {function***REMOVED*** callback - invoked when all elements processed
 * @returns {stream.Readable#***REMOVED***
 */
function ReadableSerialOrdered(list, iterator, sortMethod, callback)
{
  if (!(this instanceof ReadableSerialOrdered))
  {
    return new ReadableSerialOrdered(list, iterator, sortMethod, callback);
  ***REMOVED***

  // turn on object mode
  ReadableSerialOrdered.super_.call(this, {objectMode: true***REMOVED***);

  this._start(serialOrdered, list, iterator, sortMethod, callback);
***REMOVED***

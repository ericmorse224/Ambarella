/*
  Copyright 2018 Google LLC

  Use of this source code is governed by an MIT-style
  license that can be found in the LICENSE file or at
  https://opensource.org/licenses/MIT.
*/
import { logger ***REMOVED*** from 'workbox-core/_private/logger.js';
import '../_version.js';
/**
 * @param {string***REMOVED*** groupTitle
 * @param {Array<string>***REMOVED*** urls
 *
 * @private
 */
function _nestedGroup(groupTitle, urls) {
    if (urls.length === 0) {
        return;
    ***REMOVED***
    logger.groupCollapsed(groupTitle);
    for (const url of urls) {
        logger.log(url);
    ***REMOVED***
    logger.groupEnd();
***REMOVED***
/**
 * @param {Array<string>***REMOVED*** urlsToPrecache
 * @param {Array<string>***REMOVED*** urlsAlreadyPrecached
 *
 * @private
 * @memberof workbox-precaching
 */
export function printInstallDetails(urlsToPrecache, urlsAlreadyPrecached) {
    const precachedCount = urlsToPrecache.length;
    const alreadyPrecachedCount = urlsAlreadyPrecached.length;
    if (precachedCount || alreadyPrecachedCount) {
        let message = `Precaching ${precachedCount***REMOVED*** file${precachedCount === 1 ? '' : 's'***REMOVED***.`;
        if (alreadyPrecachedCount > 0) {
            message +=
                ` ${alreadyPrecachedCount***REMOVED*** ` +
                    `file${alreadyPrecachedCount === 1 ? ' is' : 's are'***REMOVED*** already cached.`;
        ***REMOVED***
        logger.groupCollapsed(message);
        _nestedGroup(`View newly precached URLs.`, urlsToPrecache);
        _nestedGroup(`View previously precached URLs.`, urlsAlreadyPrecached);
        logger.groupEnd();
    ***REMOVED***
***REMOVED***

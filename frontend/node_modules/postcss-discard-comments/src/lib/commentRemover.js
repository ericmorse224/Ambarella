'use strict';

/** @param {import('../index.js').Options***REMOVED*** options */
function CommentRemover(options) {
  this.options = options;
***REMOVED***
/**
 * @param {string***REMOVED*** comment
 * @return {boolean | undefined***REMOVED***
 */
CommentRemover.prototype.canRemove = function (comment) {
  const remove = this.options.remove;

  if (remove) {
    return remove(comment);
  ***REMOVED*** else {
    const isImportant = comment.indexOf('!') === 0;

    if (!isImportant) {
      return true;
    ***REMOVED***

    if (this.options.removeAll || this._hasFirst) {
      return true;
    ***REMOVED*** else if (this.options.removeAllButFirst && !this._hasFirst) {
      this._hasFirst = true;
      return false;
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

module.exports = CommentRemover;

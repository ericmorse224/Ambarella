"use strict";

module.exports = function (it) {
	const { path, message ***REMOVED*** = it;

	return `
Failed to read JSON file at ${path***REMOVED***:

${message***REMOVED***
`.trimStart();
***REMOVED***;

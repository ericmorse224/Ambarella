"use strict";

/**
 * A reference to the global object
 *
 * @type {object***REMOVED*** globalObject
 */
var globalObject;

/* istanbul ignore else */
if (typeof global !== "undefined") {
    // Node
    globalObject = global;
***REMOVED*** else if (typeof window !== "undefined") {
    // Browser
    globalObject = window;
***REMOVED*** else {
    // WebWorker
    globalObject = self;
***REMOVED***

module.exports = globalObject;

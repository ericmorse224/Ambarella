const React = require('react');

module.exports = function FlattenButton({ setIsFlat, isFlat ***REMOVED***) {
    return (
        <div className="toggle">
            <div className="toggle__label">Files:</div>
            <div className="toggle__options">
                <button
                    onClick={() => setIsFlat(!isFlat)***REMOVED***
                    className={
                        'toggle__option ' + (!isFlat ? 'is-toggled' : '')
                    ***REMOVED***
                >
                    Tree
                </button>
                <button
                    onClick={() => setIsFlat(!isFlat)***REMOVED***
                    className={'toggle__option ' + (isFlat ? 'is-toggled' : '')***REMOVED***
                >
                    Flat
                </button>
            </div>{' '***REMOVED***
        </div>
    );
***REMOVED***;

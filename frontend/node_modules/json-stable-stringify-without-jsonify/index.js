module.exports = function (obj, opts) {
    if (!opts) opts = {***REMOVED***;
    if (typeof opts === 'function') opts = { cmp: opts ***REMOVED***;
    var space = opts.space || '';
    if (typeof space === 'number') space = Array(space+1).join(' ');
    var cycles = (typeof opts.cycles === 'boolean') ? opts.cycles : false;
    var replacer = opts.replacer || function(key, value) { return value; ***REMOVED***;

    var cmp = opts.cmp && (function (f) {
        return function (node) {
            return function (a, b) {
                var aobj = { key: a, value: node[a] ***REMOVED***;
                var bobj = { key: b, value: node[b] ***REMOVED***;
                return f(aobj, bobj);
            ***REMOVED***;
        ***REMOVED***;
    ***REMOVED***)(opts.cmp);

    var seen = [];
    return (function stringify (parent, key, node, level) {
        var indent = space ? ('\n' + new Array(level + 1).join(space)) : '';
        var colonSeparator = space ? ': ' : ':';

        if (node && node.toJSON && typeof node.toJSON === 'function') {
            node = node.toJSON();
        ***REMOVED***

        node = replacer.call(parent, key, node);

        if (node === undefined) {
            return;
        ***REMOVED***
        if (typeof node !== 'object' || node === null) {
            return JSON.stringify(node);
        ***REMOVED***
        if (isArray(node)) {
            var out = [];
            for (var i = 0; i < node.length; i++) {
                var item = stringify(node, i, node[i], level+1) || JSON.stringify(null);
                out.push(indent + space + item);
            ***REMOVED***
            return '[' + out.join(',') + indent + ']';
        ***REMOVED***
        else {
            if (seen.indexOf(node) !== -1) {
                if (cycles) return JSON.stringify('__cycle__');
                throw new TypeError('Converting circular structure to JSON');
            ***REMOVED***
            else seen.push(node);

            var keys = objectKeys(node).sort(cmp && cmp(node));
            var out = [];
            for (var i = 0; i < keys.length; i++) {
                var key = keys[i];
                var value = stringify(node, key, node[key], level+1);

                if(!value) continue;

                var keyValue = JSON.stringify(key)
                    + colonSeparator
                    + value;
                ;
                out.push(indent + space + keyValue);
            ***REMOVED***
            seen.splice(seen.indexOf(node), 1);
            return '{' + out.join(',') + indent + '***REMOVED***';
        ***REMOVED***
    ***REMOVED***)({ '': obj ***REMOVED***, '', obj, 0);
***REMOVED***;

var isArray = Array.isArray || function (x) {
    return {***REMOVED***.toString.call(x) === '[object Array]';
***REMOVED***;

var objectKeys = Object.keys || function (obj) {
    var has = Object.prototype.hasOwnProperty || function () { return true ***REMOVED***;
    var keys = [];
    for (var key in obj) {
        if (has.call(obj, key)) keys.push(key);
    ***REMOVED***
    return keys;
***REMOVED***;

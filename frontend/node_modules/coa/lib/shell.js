module.exports = { escape, unescape ***REMOVED***;

function unescape(w) {
    w = w.charAt(0) === '"'
        ? w.replace(/^"|([^\\])"$/g, '$1')
        : w.replace(/\\ /g, ' ');

    return w.replace(/\\("|'|\$|`|\\)/g, '$1');
***REMOVED***

function escape(w) {
    w = w.replace(/(["'$`\\])/g,'\\$1');
    return w.match(/\s+/) ? `"${w***REMOVED***"` : w;
***REMOVED***

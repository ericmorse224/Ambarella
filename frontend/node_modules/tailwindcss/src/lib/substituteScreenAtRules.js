import { normalizeScreens ***REMOVED*** from '../util/normalizeScreens'
import buildMediaQuery from '../util/buildMediaQuery'

export default function ({ tailwindConfig: { theme ***REMOVED*** ***REMOVED***) {
  return function (css) {
    css.walkAtRules('screen', (atRule) => {
      let screen = atRule.params
      let screens = normalizeScreens(theme.screens)
      let screenDefinition = screens.find(({ name ***REMOVED***) => name === screen)

      if (!screenDefinition) {
        throw atRule.error(`No \`${screen***REMOVED***\` screen found.`)
      ***REMOVED***

      atRule.name = 'media'
      atRule.params = buildMediaQuery(screenDefinition)
    ***REMOVED***)
  ***REMOVED***
***REMOVED***

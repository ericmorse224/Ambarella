/**
 * Gets entry point of a supported socket integration.
 * @param {'wds' | 'whm' | 'wps' | string***REMOVED*** integrationType A valid socket integration type or a path to a module.
 * @returns {string | undefined***REMOVED*** Path to the resolved integration entry point.
 */
function getIntegrationEntry(integrationType) {
  let resolvedEntry;
  switch (integrationType) {
    case 'whm': {
      resolvedEntry = 'webpack-hot-middleware/client';
      break;
    ***REMOVED***
    case 'wps': {
      resolvedEntry = 'webpack-plugin-serve/client';
      break;
    ***REMOVED***
  ***REMOVED***

  return resolvedEntry;
***REMOVED***

module.exports = getIntegrationEntry;

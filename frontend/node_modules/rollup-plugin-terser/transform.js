const { minify ***REMOVED*** = require("terser");

const transform = (code, optionsString) => {
  const options = eval(`(${optionsString***REMOVED***)`);
  return minify(code, options).then(result => ({ result, nameCache: options.nameCache ***REMOVED***));
***REMOVED***;

exports.transform = transform;

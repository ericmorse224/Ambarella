'use strict';

exports.__esModule = true;

const { getScope ***REMOVED*** = require('./contextCompat');

/** @type {import('./declaredScope').default***REMOVED*** */
exports.default = function declaredScope(context, name, node) {
  const references = (node ? getScope(context, node) : context.getScope()).references;
  const reference = references.find((x) => x.identifier.name === name);
  if (!reference || !reference.resolved) { return undefined; ***REMOVED***
  return reference.resolved.scope.type;
***REMOVED***;

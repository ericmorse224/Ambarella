


import {isDeclaration***REMOVED*** from "./parser/tokenizer";
import {ContextualKeyword***REMOVED*** from "./parser/tokenizer/keywords";
import {TokenType as tt***REMOVED*** from "./parser/tokenizer/types";

import getImportExportSpecifierInfo from "./util/getImportExportSpecifierInfo";
import {getNonTypeIdentifiers***REMOVED*** from "./util/getNonTypeIdentifiers";
















/**
 * Class responsible for preprocessing and bookkeeping import and export declarations within the
 * file.
 *
 * TypeScript uses a simpler mechanism that does not use functions like interopRequireDefault and
 * interopRequireWildcard, so we also allow that mode for compatibility.
 */
export default class CJSImportProcessor {
   __init() {this.nonTypeIdentifiers = new Set()***REMOVED***
   __init2() {this.importInfoByPath = new Map()***REMOVED***
   __init3() {this.importsToReplace = new Map()***REMOVED***
   __init4() {this.identifierReplacements = new Map()***REMOVED***
   __init5() {this.exportBindingsByLocalName = new Map()***REMOVED***

  constructor(
     nameManager,
     tokens,
     enableLegacyTypeScriptModuleInterop,
     options,
     isTypeScriptTransformEnabled,
     keepUnusedImports,
     helperManager,
  ) {;this.nameManager = nameManager;this.tokens = tokens;this.enableLegacyTypeScriptModuleInterop = enableLegacyTypeScriptModuleInterop;this.options = options;this.isTypeScriptTransformEnabled = isTypeScriptTransformEnabled;this.keepUnusedImports = keepUnusedImports;this.helperManager = helperManager;CJSImportProcessor.prototype.__init.call(this);CJSImportProcessor.prototype.__init2.call(this);CJSImportProcessor.prototype.__init3.call(this);CJSImportProcessor.prototype.__init4.call(this);CJSImportProcessor.prototype.__init5.call(this);***REMOVED***

  preprocessTokens() {
    for (let i = 0; i < this.tokens.tokens.length; i++) {
      if (
        this.tokens.matches1AtIndex(i, tt._import) &&
        !this.tokens.matches3AtIndex(i, tt._import, tt.name, tt.eq)
      ) {
        this.preprocessImportAtIndex(i);
      ***REMOVED***
      if (
        this.tokens.matches1AtIndex(i, tt._export) &&
        !this.tokens.matches2AtIndex(i, tt._export, tt.eq)
      ) {
        this.preprocessExportAtIndex(i);
      ***REMOVED***
    ***REMOVED***
    this.generateImportReplacements();
  ***REMOVED***

  /**
   * In TypeScript, import statements that only import types should be removed.
   * This includes `import {***REMOVED*** from 'foo';`, but not `import 'foo';`.
   */
  pruneTypeOnlyImports() {
    this.nonTypeIdentifiers = getNonTypeIdentifiers(this.tokens, this.options);
    for (const [path, importInfo] of this.importInfoByPath.entries()) {
      if (
        importInfo.hasBareImport ||
        importInfo.hasStarExport ||
        importInfo.exportStarNames.length > 0 ||
        importInfo.namedExports.length > 0
      ) {
        continue;
      ***REMOVED***
      const names = [
        ...importInfo.defaultNames,
        ...importInfo.wildcardNames,
        ...importInfo.namedImports.map(({localName***REMOVED***) => localName),
      ];
      if (names.every((name) => this.shouldAutomaticallyElideImportedName(name))) {
        this.importsToReplace.set(path, "");
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  shouldAutomaticallyElideImportedName(name) {
    return (
      this.isTypeScriptTransformEnabled &&
      !this.keepUnusedImports &&
      !this.nonTypeIdentifiers.has(name)
    );
  ***REMOVED***

   generateImportReplacements() {
    for (const [path, importInfo] of this.importInfoByPath.entries()) {
      const {
        defaultNames,
        wildcardNames,
        namedImports,
        namedExports,
        exportStarNames,
        hasStarExport,
      ***REMOVED*** = importInfo;

      if (
        defaultNames.length === 0 &&
        wildcardNames.length === 0 &&
        namedImports.length === 0 &&
        namedExports.length === 0 &&
        exportStarNames.length === 0 &&
        !hasStarExport
      ) {
        // Import is never used, so don't even assign a name.
        this.importsToReplace.set(path, `require('${path***REMOVED***');`);
        continue;
      ***REMOVED***

      const primaryImportName = this.getFreeIdentifierForPath(path);
      let secondaryImportName;
      if (this.enableLegacyTypeScriptModuleInterop) {
        secondaryImportName = primaryImportName;
      ***REMOVED*** else {
        secondaryImportName =
          wildcardNames.length > 0 ? wildcardNames[0] : this.getFreeIdentifierForPath(path);
      ***REMOVED***
      let requireCode = `var ${primaryImportName***REMOVED*** = require('${path***REMOVED***');`;
      if (wildcardNames.length > 0) {
        for (const wildcardName of wildcardNames) {
          const moduleExpr = this.enableLegacyTypeScriptModuleInterop
            ? primaryImportName
            : `${this.helperManager.getHelperName("interopRequireWildcard")***REMOVED***(${primaryImportName***REMOVED***)`;
          requireCode += ` var ${wildcardName***REMOVED*** = ${moduleExpr***REMOVED***;`;
        ***REMOVED***
      ***REMOVED*** else if (exportStarNames.length > 0 && secondaryImportName !== primaryImportName) {
        requireCode += ` var ${secondaryImportName***REMOVED*** = ${this.helperManager.getHelperName(
          "interopRequireWildcard",
        )***REMOVED***(${primaryImportName***REMOVED***);`;
      ***REMOVED*** else if (defaultNames.length > 0 && secondaryImportName !== primaryImportName) {
        requireCode += ` var ${secondaryImportName***REMOVED*** = ${this.helperManager.getHelperName(
          "interopRequireDefault",
        )***REMOVED***(${primaryImportName***REMOVED***);`;
      ***REMOVED***

      for (const {importedName, localName***REMOVED*** of namedExports) {
        requireCode += ` ${this.helperManager.getHelperName(
          "createNamedExportFrom",
        )***REMOVED***(${primaryImportName***REMOVED***, '${localName***REMOVED***', '${importedName***REMOVED***');`;
      ***REMOVED***
      for (const exportStarName of exportStarNames) {
        requireCode += ` exports.${exportStarName***REMOVED*** = ${secondaryImportName***REMOVED***;`;
      ***REMOVED***
      if (hasStarExport) {
        requireCode += ` ${this.helperManager.getHelperName(
          "createStarExport",
        )***REMOVED***(${primaryImportName***REMOVED***);`;
      ***REMOVED***

      this.importsToReplace.set(path, requireCode);

      for (const defaultName of defaultNames) {
        this.identifierReplacements.set(defaultName, `${secondaryImportName***REMOVED***.default`);
      ***REMOVED***
      for (const {importedName, localName***REMOVED*** of namedImports) {
        this.identifierReplacements.set(localName, `${primaryImportName***REMOVED***.${importedName***REMOVED***`);
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  getFreeIdentifierForPath(path) {
    const components = path.split("/");
    const lastComponent = components[components.length - 1];
    const baseName = lastComponent.replace(/\W/g, "");
    return this.nameManager.claimFreeName(`_${baseName***REMOVED***`);
  ***REMOVED***

   preprocessImportAtIndex(index) {
    const defaultNames = [];
    const wildcardNames = [];
    const namedImports = [];

    index++;
    if (
      (this.tokens.matchesContextualAtIndex(index, ContextualKeyword._type) ||
        this.tokens.matches1AtIndex(index, tt._typeof)) &&
      !this.tokens.matches1AtIndex(index + 1, tt.comma) &&
      !this.tokens.matchesContextualAtIndex(index + 1, ContextualKeyword._from)
    ) {
      // import type declaration, so no need to process anything.
      return;
    ***REMOVED***

    if (this.tokens.matches1AtIndex(index, tt.parenL)) {
      // Dynamic import, so nothing to do
      return;
    ***REMOVED***

    if (this.tokens.matches1AtIndex(index, tt.name)) {
      defaultNames.push(this.tokens.identifierNameAtIndex(index));
      index++;
      if (this.tokens.matches1AtIndex(index, tt.comma)) {
        index++;
      ***REMOVED***
    ***REMOVED***

    if (this.tokens.matches1AtIndex(index, tt.star)) {
      // * as
      index += 2;
      wildcardNames.push(this.tokens.identifierNameAtIndex(index));
      index++;
    ***REMOVED***

    if (this.tokens.matches1AtIndex(index, tt.braceL)) {
      const result = this.getNamedImports(index + 1);
      index = result.newIndex;

      for (const namedImport of result.namedImports) {
        // Treat {default as X***REMOVED*** as a default import to ensure usage of require interop helper
        if (namedImport.importedName === "default") {
          defaultNames.push(namedImport.localName);
        ***REMOVED*** else {
          namedImports.push(namedImport);
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***

    if (this.tokens.matchesContextualAtIndex(index, ContextualKeyword._from)) {
      index++;
    ***REMOVED***

    if (!this.tokens.matches1AtIndex(index, tt.string)) {
      throw new Error("Expected string token at the end of import statement.");
    ***REMOVED***
    const path = this.tokens.stringValueAtIndex(index);
    const importInfo = this.getImportInfo(path);
    importInfo.defaultNames.push(...defaultNames);
    importInfo.wildcardNames.push(...wildcardNames);
    importInfo.namedImports.push(...namedImports);
    if (defaultNames.length === 0 && wildcardNames.length === 0 && namedImports.length === 0) {
      importInfo.hasBareImport = true;
    ***REMOVED***
  ***REMOVED***

   preprocessExportAtIndex(index) {
    if (
      this.tokens.matches2AtIndex(index, tt._export, tt._var) ||
      this.tokens.matches2AtIndex(index, tt._export, tt._let) ||
      this.tokens.matches2AtIndex(index, tt._export, tt._const)
    ) {
      this.preprocessVarExportAtIndex(index);
    ***REMOVED*** else if (
      this.tokens.matches2AtIndex(index, tt._export, tt._function) ||
      this.tokens.matches2AtIndex(index, tt._export, tt._class)
    ) {
      const exportName = this.tokens.identifierNameAtIndex(index + 2);
      this.addExportBinding(exportName, exportName);
    ***REMOVED*** else if (this.tokens.matches3AtIndex(index, tt._export, tt.name, tt._function)) {
      const exportName = this.tokens.identifierNameAtIndex(index + 3);
      this.addExportBinding(exportName, exportName);
    ***REMOVED*** else if (this.tokens.matches2AtIndex(index, tt._export, tt.braceL)) {
      this.preprocessNamedExportAtIndex(index);
    ***REMOVED*** else if (this.tokens.matches2AtIndex(index, tt._export, tt.star)) {
      this.preprocessExportStarAtIndex(index);
    ***REMOVED***
  ***REMOVED***

   preprocessVarExportAtIndex(index) {
    let depth = 0;
    // Handle cases like `export let {x***REMOVED*** = y;`, starting at the open-brace in that case.
    for (let i = index + 2; ; i++) {
      if (
        this.tokens.matches1AtIndex(i, tt.braceL) ||
        this.tokens.matches1AtIndex(i, tt.dollarBraceL) ||
        this.tokens.matches1AtIndex(i, tt.bracketL)
      ) {
        depth++;
      ***REMOVED*** else if (
        this.tokens.matches1AtIndex(i, tt.braceR) ||
        this.tokens.matches1AtIndex(i, tt.bracketR)
      ) {
        depth--;
      ***REMOVED*** else if (depth === 0 && !this.tokens.matches1AtIndex(i, tt.name)) {
        break;
      ***REMOVED*** else if (this.tokens.matches1AtIndex(1, tt.eq)) {
        const endIndex = this.tokens.currentToken().rhsEndIndex;
        if (endIndex == null) {
          throw new Error("Expected = token with an end index.");
        ***REMOVED***
        i = endIndex - 1;
      ***REMOVED*** else {
        const token = this.tokens.tokens[i];
        if (isDeclaration(token)) {
          const exportName = this.tokens.identifierNameAtIndex(i);
          this.identifierReplacements.set(exportName, `exports.${exportName***REMOVED***`);
        ***REMOVED***
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***

  /**
   * Walk this export statement just in case it's an export...from statement.
   * If it is, combine it into the import info for that path. Otherwise, just
   * bail out; it'll be handled later.
   */
   preprocessNamedExportAtIndex(index) {
    // export {
    index += 2;
    const {newIndex, namedImports***REMOVED*** = this.getNamedImports(index);
    index = newIndex;

    if (this.tokens.matchesContextualAtIndex(index, ContextualKeyword._from)) {
      index++;
    ***REMOVED*** else {
      // Reinterpret "a as b" to be local/exported rather than imported/local.
      for (const {importedName: localName, localName: exportedName***REMOVED*** of namedImports) {
        this.addExportBinding(localName, exportedName);
      ***REMOVED***
      return;
    ***REMOVED***

    if (!this.tokens.matches1AtIndex(index, tt.string)) {
      throw new Error("Expected string token at the end of import statement.");
    ***REMOVED***
    const path = this.tokens.stringValueAtIndex(index);
    const importInfo = this.getImportInfo(path);
    importInfo.namedExports.push(...namedImports);
  ***REMOVED***

   preprocessExportStarAtIndex(index) {
    let exportedName = null;
    if (this.tokens.matches3AtIndex(index, tt._export, tt.star, tt._as)) {
      // export * as
      index += 3;
      exportedName = this.tokens.identifierNameAtIndex(index);
      // foo from
      index += 2;
    ***REMOVED*** else {
      // export * from
      index += 3;
    ***REMOVED***
    if (!this.tokens.matches1AtIndex(index, tt.string)) {
      throw new Error("Expected string token at the end of star export statement.");
    ***REMOVED***
    const path = this.tokens.stringValueAtIndex(index);
    const importInfo = this.getImportInfo(path);
    if (exportedName !== null) {
      importInfo.exportStarNames.push(exportedName);
    ***REMOVED*** else {
      importInfo.hasStarExport = true;
    ***REMOVED***
  ***REMOVED***

   getNamedImports(index) {
    const namedImports = [];
    while (true) {
      if (this.tokens.matches1AtIndex(index, tt.braceR)) {
        index++;
        break;
      ***REMOVED***

      const specifierInfo = getImportExportSpecifierInfo(this.tokens, index);
      index = specifierInfo.endIndex;
      if (!specifierInfo.isType) {
        namedImports.push({
          importedName: specifierInfo.leftName,
          localName: specifierInfo.rightName,
        ***REMOVED***);
      ***REMOVED***

      if (this.tokens.matches2AtIndex(index, tt.comma, tt.braceR)) {
        index += 2;
        break;
      ***REMOVED*** else if (this.tokens.matches1AtIndex(index, tt.braceR)) {
        index++;
        break;
      ***REMOVED*** else if (this.tokens.matches1AtIndex(index, tt.comma)) {
        index++;
      ***REMOVED*** else {
        throw new Error(`Unexpected token: ${JSON.stringify(this.tokens.tokens[index])***REMOVED***`);
      ***REMOVED***
    ***REMOVED***
    return {newIndex: index, namedImports***REMOVED***;
  ***REMOVED***

  /**
   * Get a mutable import info object for this path, creating one if it doesn't
   * exist yet.
   */
   getImportInfo(path) {
    const existingInfo = this.importInfoByPath.get(path);
    if (existingInfo) {
      return existingInfo;
    ***REMOVED***
    const newInfo = {
      defaultNames: [],
      wildcardNames: [],
      namedImports: [],
      namedExports: [],
      hasBareImport: false,
      exportStarNames: [],
      hasStarExport: false,
    ***REMOVED***;
    this.importInfoByPath.set(path, newInfo);
    return newInfo;
  ***REMOVED***

   addExportBinding(localName, exportedName) {
    if (!this.exportBindingsByLocalName.has(localName)) {
      this.exportBindingsByLocalName.set(localName, []);
    ***REMOVED***
    this.exportBindingsByLocalName.get(localName).push(exportedName);
  ***REMOVED***

  /**
   * Return the code to use for the import for this path, or the empty string if
   * the code has already been "claimed" by a previous import.
   */
  claimImportCode(importPath) {
    const result = this.importsToReplace.get(importPath);
    this.importsToReplace.set(importPath, "");
    return result || "";
  ***REMOVED***

  getIdentifierReplacement(identifierName) {
    return this.identifierReplacements.get(identifierName) || null;
  ***REMOVED***

  /**
   * Return a string like `exports.foo = exports.bar`.
   */
  resolveExportBinding(assignedName) {
    const exportedNames = this.exportBindingsByLocalName.get(assignedName);
    if (!exportedNames || exportedNames.length === 0) {
      return null;
    ***REMOVED***
    return exportedNames.map((exportedName) => `exports.${exportedName***REMOVED***`).join(" = ");
  ***REMOVED***

  /**
   * Return all imported/exported names where we might be interested in whether usages of those
   * names are shadowed.
   */
  getGlobalNames() {
    return new Set([
      ...this.identifierReplacements.keys(),
      ...this.exportBindingsByLocalName.keys(),
    ]);
  ***REMOVED***
***REMOVED***

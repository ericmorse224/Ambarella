'use strict';
const { stringify ***REMOVED*** = require('postcss-value-parser');

/**
 * @param {import('postcss-value-parser').Node[][]***REMOVED*** values
 * @return {string***REMOVED***
 */
module.exports = function getValue(values) {
  return stringify(flatten(values));
***REMOVED***;
/**
 * @param {import('postcss-value-parser').Node[][]***REMOVED*** values
 * @return {import('postcss-value-parser').Node[]***REMOVED***
 */
function flatten(values) {
  /** @type {import('postcss-value-parser').Node[]***REMOVED*** */
  const nodes = [];
  for (const [index, arg] of values.entries()) {
    arg.forEach((val, idx) => {
      if (
        idx === arg.length - 1 &&
        index === values.length - 1 &&
        val.type === 'space'
      ) {
        return;
      ***REMOVED***
      nodes.push(val);
    ***REMOVED***);

    if (index !== values.length - 1) {
      nodes[nodes.length - 1].type = 'div';
      nodes[nodes.length - 1].value = ',';
    ***REMOVED***
  ***REMOVED***
  return nodes;
***REMOVED***

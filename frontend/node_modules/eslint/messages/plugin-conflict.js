"use strict";

module.exports = function(it) {
    const { pluginId, plugins ***REMOVED*** = it;

    let result = `ESLint couldn't determine the plugin "${pluginId***REMOVED***" uniquely.
`;

    for (const { filePath, importerName ***REMOVED*** of plugins) {
        result += `
- ${filePath***REMOVED*** (loaded in "${importerName***REMOVED***")`;
    ***REMOVED***

    result += `

Please remove the "plugins" setting from either config or remove either plugin installation.

If you still can't figure out the problem, please stop by https://eslint.org/chat/help to chat with the team.
`;

    return result;
***REMOVED***;

"use strict";

/* eslint-disable no-param-reassign*/
const TokenTranslator = require("./token-translator");
const { normalizeOptions ***REMOVED*** = require("./options");

const STATE = Symbol("espree's internal state");
const ESPRIMA_FINISH_NODE = Symbol("espree's esprimaFinishNode");


/**
 * Converts an Acorn comment to a Esprima comment.
 * @param {boolean***REMOVED*** block True if it's a block comment, false if not.
 * @param {string***REMOVED*** text The text of the comment.
 * @param {int***REMOVED*** start The index at which the comment starts.
 * @param {int***REMOVED*** end The index at which the comment ends.
 * @param {Location***REMOVED*** startLoc The location at which the comment starts.
 * @param {Location***REMOVED*** endLoc The location at which the comment ends.
 * @returns {Object***REMOVED*** The comment object.
 * @private
 */
function convertAcornCommentToEsprimaComment(block, text, start, end, startLoc, endLoc) {
    const comment = {
        type: block ? "Block" : "Line",
        value: text
    ***REMOVED***;

    if (typeof start === "number") {
        comment.start = start;
        comment.end = end;
        comment.range = [start, end];
    ***REMOVED***

    if (typeof startLoc === "object") {
        comment.loc = {
            start: startLoc,
            end: endLoc
        ***REMOVED***;
    ***REMOVED***

    return comment;
***REMOVED***

module.exports = () => Parser => {
    const tokTypes = Object.assign({***REMOVED***, Parser.acorn.tokTypes);

    if (Parser.acornJsx) {
        Object.assign(tokTypes, Parser.acornJsx.tokTypes);
    ***REMOVED***

    return class Espree extends Parser {
        constructor(opts, code) {
            if (typeof opts !== "object" || opts === null) {
                opts = {***REMOVED***;
            ***REMOVED***
            if (typeof code !== "string" && !(code instanceof String)) {
                code = String(code);
            ***REMOVED***

            const options = normalizeOptions(opts);
            const ecmaFeatures = options.ecmaFeatures || {***REMOVED***;
            const tokenTranslator =
                options.tokens === true
                    ? new TokenTranslator(tokTypes, code)
                    : null;

            // Initialize acorn parser.
            super({

                // TODO: use {...options***REMOVED*** when spread is supported(Node.js >= 8.3.0).
                ecmaVersion: options.ecmaVersion,
                sourceType: options.sourceType,
                ranges: options.ranges,
                locations: options.locations,

                // Truthy value is true for backward compatibility.
                allowReturnOutsideFunction: Boolean(ecmaFeatures.globalReturn),

                // Collect tokens
                onToken: token => {
                    if (tokenTranslator) {

                        // Use `tokens`, `ecmaVersion`, and `jsxAttrValueToken` in the state.
                        tokenTranslator.onToken(token, this[STATE]);
                    ***REMOVED***
                    if (token.type !== tokTypes.eof) {
                        this[STATE].lastToken = token;
                    ***REMOVED***
                ***REMOVED***,

                // Collect comments
                onComment: (block, text, start, end, startLoc, endLoc) => {
                    if (this[STATE].comments) {
                        const comment = convertAcornCommentToEsprimaComment(block, text, start, end, startLoc, endLoc);

                        this[STATE].comments.push(comment);
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***, code);

            // Initialize internal state.
            this[STATE] = {
                tokens: tokenTranslator ? [] : null,
                comments: options.comment === true ? [] : null,
                impliedStrict: ecmaFeatures.impliedStrict === true && this.options.ecmaVersion >= 5,
                ecmaVersion: this.options.ecmaVersion,
                jsxAttrValueToken: false,
                lastToken: null
            ***REMOVED***;
        ***REMOVED***

        tokenize() {
            do {
                this.next();
            ***REMOVED*** while (this.type !== tokTypes.eof);

            // Consume the final eof token
            this.next();

            const extra = this[STATE];
            const tokens = extra.tokens;

            if (extra.comments) {
                tokens.comments = extra.comments;
            ***REMOVED***

            return tokens;
        ***REMOVED***

        finishNode(...args) {
            const result = super.finishNode(...args);

            return this[ESPRIMA_FINISH_NODE](result);
        ***REMOVED***

        finishNodeAt(...args) {
            const result = super.finishNodeAt(...args);

            return this[ESPRIMA_FINISH_NODE](result);
        ***REMOVED***

        parse() {
            const extra = this[STATE];
            const program = super.parse();

            program.sourceType = this.options.sourceType;

            if (extra.comments) {
                program.comments = extra.comments;
            ***REMOVED***
            if (extra.tokens) {
                program.tokens = extra.tokens;
            ***REMOVED***

            /*
             * Adjust opening and closing position of program to match Esprima.
             * Acorn always starts programs at range 0 whereas Esprima starts at the
             * first AST node's start (the only real difference is when there's leading
             * whitespace or leading comments). Acorn also counts trailing whitespace
             * as part of the program whereas Esprima only counts up to the last token.
             */
            if (program.range) {
                program.range[0] = program.body.length ? program.body[0].range[0] : program.range[0];
                program.range[1] = extra.lastToken ? extra.lastToken.range[1] : program.range[1];
            ***REMOVED***
            if (program.loc) {
                program.loc.start = program.body.length ? program.body[0].loc.start : program.loc.start;
                program.loc.end = extra.lastToken ? extra.lastToken.loc.end : program.loc.end;
            ***REMOVED***

            return program;
        ***REMOVED***

        parseTopLevel(node) {
            if (this[STATE].impliedStrict) {
                this.strict = true;
            ***REMOVED***
            return super.parseTopLevel(node);
        ***REMOVED***

        /**
         * Overwrites the default raise method to throw Esprima-style errors.
         * @param {int***REMOVED*** pos The position of the error.
         * @param {string***REMOVED*** message The error message.
         * @throws {SyntaxError***REMOVED*** A syntax error.
         * @returns {void***REMOVED***
         */
        raise(pos, message) {
            const loc = Parser.acorn.getLineInfo(this.input, pos);
            const err = new SyntaxError(message);

            err.index = pos;
            err.lineNumber = loc.line;
            err.column = loc.column + 1; // acorn uses 0-based columns
            throw err;
        ***REMOVED***

        /**
         * Overwrites the default raise method to throw Esprima-style errors.
         * @param {int***REMOVED*** pos The position of the error.
         * @param {string***REMOVED*** message The error message.
         * @throws {SyntaxError***REMOVED*** A syntax error.
         * @returns {void***REMOVED***
         */
        raiseRecoverable(pos, message) {
            this.raise(pos, message);
        ***REMOVED***

        /**
         * Overwrites the default unexpected method to throw Esprima-style errors.
         * @param {int***REMOVED*** pos The position of the error.
         * @throws {SyntaxError***REMOVED*** A syntax error.
         * @returns {void***REMOVED***
         */
        unexpected(pos) {
            let message = "Unexpected token";

            if (pos !== null && pos !== void 0) {
                this.pos = pos;

                if (this.options.locations) {
                    while (this.pos < this.lineStart) {
                        this.lineStart = this.input.lastIndexOf("\n", this.lineStart - 2) + 1;
                        --this.curLine;
                    ***REMOVED***
                ***REMOVED***

                this.nextToken();
            ***REMOVED***

            if (this.end > this.start) {
                message += ` ${this.input.slice(this.start, this.end)***REMOVED***`;
            ***REMOVED***

            this.raise(this.start, message);
        ***REMOVED***

        /*
        * Esprima-FB represents JSX strings as tokens called "JSXText", but Acorn-JSX
        * uses regular tt.string without any distinction between this and regular JS
        * strings. As such, we intercept an attempt to read a JSX string and set a flag
        * on extra so that when tokens are converted, the next token will be switched
        * to JSXText via onToken.
        */
        jsx_readString(quote) { // eslint-disable-line camelcase
            const result = super.jsx_readString(quote);

            if (this.type === tokTypes.string) {
                this[STATE].jsxAttrValueToken = true;
            ***REMOVED***
            return result;
        ***REMOVED***

        /**
         * Performs last-minute Esprima-specific compatibility checks and fixes.
         * @param {ASTNode***REMOVED*** result The node to check.
         * @returns {ASTNode***REMOVED*** The finished node.
         */
        [ESPRIMA_FINISH_NODE](result) {

            // Acorn doesn't count the opening and closing backticks as part of templates
            // so we have to adjust ranges/locations appropriately.
            if (result.type === "TemplateElement") {

                // additional adjustment needed if ${ is the last token
                const terminalDollarBraceL = this.input.slice(result.end, result.end + 2) === "${";

                if (result.range) {
                    result.range[0]--;
                    result.range[1] += (terminalDollarBraceL ? 2 : 1);
                ***REMOVED***

                if (result.loc) {
                    result.loc.start.column--;
                    result.loc.end.column += (terminalDollarBraceL ? 2 : 1);
                ***REMOVED***
            ***REMOVED***

            if (result.type.indexOf("Function") > -1 && !result.generator) {
                result.generator = false;
            ***REMOVED***

            return result;
        ***REMOVED***
    ***REMOVED***;
***REMOVED***;

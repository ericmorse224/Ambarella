
import {nextToken, skipLineComment***REMOVED*** from "../tokenizer/index";
import {charCodes***REMOVED*** from "../util/charcodes";
import {input, state***REMOVED*** from "./base";
import {parseTopLevel***REMOVED*** from "./statement";

export function parseFile() {
  // If enabled, skip leading hashbang line.
  if (
    state.pos === 0 &&
    input.charCodeAt(0) === charCodes.numberSign &&
    input.charCodeAt(1) === charCodes.exclamationMark
  ) {
    skipLineComment(2);
  ***REMOVED***
  nextToken();
  return parseTopLevel();
***REMOVED***



import {augmentError, initParser, state***REMOVED*** from "./traverser/base";
import {parseFile***REMOVED*** from "./traverser/index";

export class File {
  
  

  constructor(tokens, scopes) {
    this.tokens = tokens;
    this.scopes = scopes;
  ***REMOVED***
***REMOVED***

export function parse(
  input,
  isJSXEnabled,
  isTypeScriptEnabled,
  isFlowEnabled,
) {
  if (isFlowEnabled && isTypeScriptEnabled) {
    throw new Error("Cannot combine flow and typescript plugins.");
  ***REMOVED***
  initParser(input, isJSXEnabled, isTypeScriptEnabled, isFlowEnabled);
  const result = parseFile();
  if (state.error) {
    throw augmentError(state.error);
  ***REMOVED***
  return result;
***REMOVED***

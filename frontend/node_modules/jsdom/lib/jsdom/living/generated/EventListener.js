"use strict";

const conversions = require("webidl-conversions");
const utils = require("./utils.js");

exports.convert = function convert(value, { context = "The provided value" ***REMOVED*** = {***REMOVED***) {
  if (!utils.isObject(value)) {
    throw new TypeError(`${context***REMOVED*** is not an object.`);
  ***REMOVED***

  function callTheUserObjectsOperation(event) {
    let thisArg = utils.tryWrapperForImpl(this);
    let O = value;
    let X = O;

    if (typeof O !== "function") {
      X = O["handleEvent"];
      if (typeof X !== "function") {
        throw new TypeError(`${context***REMOVED*** does not correctly implement EventListener.`);
      ***REMOVED***
      thisArg = O;
    ***REMOVED***

    event = utils.tryWrapperForImpl(event);

    let callResult = Reflect.apply(X, thisArg, [event]);
  ***REMOVED***

  callTheUserObjectsOperation[utils.wrapperSymbol] = value;
  callTheUserObjectsOperation.objectReference = value;

  return callTheUserObjectsOperation;
***REMOVED***;

exports.install = (globalObject, globalNames) => {***REMOVED***;

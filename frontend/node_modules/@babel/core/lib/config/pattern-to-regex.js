"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
***REMOVED***);
exports.default = pathToPattern;
function _path() {
  const data = require("path");
  _path = function () {
    return data;
  ***REMOVED***;
  return data;
***REMOVED***
const sep = `\\${_path().sep***REMOVED***`;
const endSep = `(?:${sep***REMOVED***|$)`;
const substitution = `[^${sep***REMOVED***]+`;
const starPat = `(?:${substitution***REMOVED***${sep***REMOVED***)`;
const starPatLast = `(?:${substitution***REMOVED***${endSep***REMOVED***)`;
const starStarPat = `${starPat***REMOVED****?`;
const starStarPatLast = `${starPat***REMOVED****?${starPatLast***REMOVED***?`;
function escapeRegExp(string) {
  return string.replace(/[|\\{***REMOVED***()[\]^$+*?.]/g, "\\$&");
***REMOVED***
function pathToPattern(pattern, dirname) {
  const parts = _path().resolve(dirname, pattern).split(_path().sep);
  return new RegExp(["^", ...parts.map((part, i) => {
    const last = i === parts.length - 1;
    if (part === "**") return last ? starStarPatLast : starStarPat;
    if (part === "*") return last ? starPatLast : starPat;
    if (part.indexOf("*.") === 0) {
      return substitution + escapeRegExp(part.slice(1)) + (last ? endSep : sep);
    ***REMOVED***
    return escapeRegExp(part) + (last ? endSep : sep);
  ***REMOVED***)].join(""));
***REMOVED***
0 && 0;

//# sourceMappingURL=pattern-to-regex.js.map

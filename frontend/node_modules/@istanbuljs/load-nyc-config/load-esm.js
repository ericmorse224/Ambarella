'use strict';

const {pathToFileURL***REMOVED*** = require('url');

module.exports = async filename => {
	const mod = await import(pathToFileURL(filename));
	if ('default' in mod === false) {
		throw new Error(`${filename***REMOVED*** has no default export`);
	***REMOVED***

	return mod.default;
***REMOVED***;

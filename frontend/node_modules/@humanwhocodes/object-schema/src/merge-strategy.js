/**
 * @filedescription Merge Strategy
 */

"use strict";

//-----------------------------------------------------------------------------
// Class
//-----------------------------------------------------------------------------

/**
 * Container class for several different merge strategies.
 */
class MergeStrategy {

    /**
     * Merges two keys by overwriting the first with the second.
     * @param {****REMOVED*** value1 The value from the first object key. 
     * @param {****REMOVED*** value2 The value from the second object key.
     * @returns {****REMOVED*** The second value.
     */
    static overwrite(value1, value2) {
        return value2;
    ***REMOVED***

    /**
     * Merges two keys by replacing the first with the second only if the
     * second is defined.
     * @param {****REMOVED*** value1 The value from the first object key. 
     * @param {****REMOVED*** value2 The value from the second object key.
     * @returns {****REMOVED*** The second value if it is defined.
     */
    static replace(value1, value2) {
        if (typeof value2 !== "undefined") {
            return value2;
        ***REMOVED***

        return value1;
    ***REMOVED***

    /**
     * Merges two properties by assigning properties from the second to the first.
     * @param {****REMOVED*** value1 The value from the first object key.
     * @param {****REMOVED*** value2 The value from the second object key.
     * @returns {****REMOVED*** A new object containing properties from both value1 and
     *      value2.
     */
    static assign(value1, value2) {
        return Object.assign({***REMOVED***, value1, value2);
    ***REMOVED***
***REMOVED***

exports.MergeStrategy = MergeStrategy;

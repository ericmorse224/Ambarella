'use strict';

var trimStart = require('../');
var test = require('tape');

var runTests = require('./tests');

test('as a function', function (t) {
	t.test('bad array/this value', function (st) {
		st['throws'](function () { trimStart(undefined, 'a'); ***REMOVED***, TypeError, 'undefined is not an object');
		st['throws'](function () { trimStart(null, 'a'); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(trimStart, t);

	t.end();
***REMOVED***);

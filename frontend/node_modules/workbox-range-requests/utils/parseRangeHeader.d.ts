import '../_version.js';
/**
 * @param {string***REMOVED*** rangeHeader A Range: header value.
 * @return {Object***REMOVED*** An object with `start` and `end` properties, reflecting
 * the parsed value of the Range: header. If either the `start` or `end` are
 * omitted, then `null` will be returned.
 *
 * @private
 */
declare function parseRangeHeader(rangeHeader: string): {
    start?: number;
    end?: number;
***REMOVED***;
export { parseRangeHeader ***REMOVED***;

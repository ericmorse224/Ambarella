/**
 * This function removes any uses of CSS variables used as an alpha channel
 *
 * This is required for selectors like `:visited` which do not allow
 * changes in opacity or external control using CSS variables.
 *
 * @param {import('postcss').Container***REMOVED*** container
 * @param {string[]***REMOVED*** toRemove
 */
export function removeAlphaVariables(container, toRemove) {
  container.walkDecls((decl) => {
    if (toRemove.includes(decl.prop)) {
      decl.remove()

      return
    ***REMOVED***

    for (let varName of toRemove) {
      if (decl.value.includes(`/ var(${varName***REMOVED***)`)) {
        decl.value = decl.value.replace(`/ var(${varName***REMOVED***)`, '')
      ***REMOVED*** else if (decl.value.includes(`/ var(${varName***REMOVED***, 1)`)) {
        decl.value = decl.value.replace(`/ var(${varName***REMOVED***, 1)`, '')
      ***REMOVED***
    ***REMOVED***
  ***REMOVED***)
***REMOVED***

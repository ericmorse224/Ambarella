'use strict';
const path = require('path');
const locatePath = require('locate-path');

module.exports = (filename, opts = {***REMOVED***) => {
	const startDir = path.resolve(opts.cwd || '');
	const {root***REMOVED*** = path.parse(startDir);

	const filenames = [].concat(filename);

	return new Promise(resolve => {
		(function find(dir) {
			locatePath(filenames, {cwd: dir***REMOVED***).then(file => {
				if (file) {
					resolve(path.join(dir, file));
				***REMOVED*** else if (dir === root) {
					resolve(null);
				***REMOVED*** else {
					find(path.dirname(dir));
				***REMOVED***
			***REMOVED***);
		***REMOVED***)(startDir);
	***REMOVED***);
***REMOVED***;

module.exports.sync = (filename, opts = {***REMOVED***) => {
	let dir = path.resolve(opts.cwd || '');
	const {root***REMOVED*** = path.parse(dir);

	const filenames = [].concat(filename);

	// eslint-disable-next-line no-constant-condition
	while (true) {
		const file = locatePath.sync(filenames, {cwd: dir***REMOVED***);

		if (file) {
			return path.join(dir, file);
		***REMOVED***

		if (dir === root) {
			return null;
		***REMOVED***

		dir = path.dirname(dir);
	***REMOVED***
***REMOVED***;

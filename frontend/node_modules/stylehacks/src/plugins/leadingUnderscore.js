'use strict';
const BasePlugin = require('../plugin');
const { IE_6 ***REMOVED*** = require('../dictionary/browsers');
const { PROPERTY ***REMOVED*** = require('../dictionary/identifiers');
const { DECL ***REMOVED*** = require('../dictionary/postcss');

/**
 * @param {string***REMOVED*** prop
 * @return {string***REMOVED***
 */
function vendorPrefix(prop) {
  let match = prop.match(/^(-\w+-)/);
  if (match) {
    return match[0];
  ***REMOVED***

  return '';
***REMOVED***

module.exports = class LeadingUnderscore extends BasePlugin {
  /** @param {import('postcss').Result=***REMOVED*** result */
  constructor(result) {
    super([IE_6], [DECL], result);
  ***REMOVED***

  /**
   * @param {import('postcss').Declaration***REMOVED*** decl
   * @return {void***REMOVED***
   */
  detect(decl) {
    const { before ***REMOVED*** = decl.raws;

    if (before && before.includes('_')) {
      this.push(decl, {
        identifier: PROPERTY,
        hack: `${before.trim()***REMOVED***${decl.prop***REMOVED***`,
      ***REMOVED***);
    ***REMOVED***

    if (
      decl.prop[0] === '-' &&
      decl.prop[1] !== '-' &&
      vendorPrefix(decl.prop) === ''
    ) {
      this.push(decl, {
        identifier: PROPERTY,
        hack: decl.prop,
      ***REMOVED***);
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

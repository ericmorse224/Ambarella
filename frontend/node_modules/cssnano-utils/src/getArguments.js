'use strict';
/**
 * Extracts the arguments of a CSS function or AtRule.
 *
 * @param {import('postcss-value-parser').ParsedValue | import('postcss-value-parser').FunctionNode***REMOVED*** node
 * @return {import('postcss-value-parser').Node[][]***REMOVED***
 */
module.exports = function getArguments(node) {
  /** @type {import('postcss-value-parser').Node[][]***REMOVED*** */
  const list = [[]];
  for (const child of node.nodes) {
    if (child.type !== 'div') {
      list[list.length - 1].push(child);
    ***REMOVED*** else {
      list.push([]);
    ***REMOVED***
  ***REMOVED***
  return list;
***REMOVED***;

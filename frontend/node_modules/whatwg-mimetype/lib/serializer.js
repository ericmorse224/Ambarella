"use strict";
const { solelyContainsHTTPTokenCodePoints ***REMOVED*** = require("./utils.js");

module.exports = mimeType => {
  let serialization = `${mimeType.type***REMOVED***/${mimeType.subtype***REMOVED***`;

  if (mimeType.parameters.size === 0) {
    return serialization;
  ***REMOVED***

  for (let [name, value] of mimeType.parameters) {
    serialization += ";";
    serialization += name;
    serialization += "=";

    if (!solelyContainsHTTPTokenCodePoints(value) || value.length === 0) {
      value = value.replace(/(["\\])/g, "\\$1");
      value = `"${value***REMOVED***"`;
    ***REMOVED***

    serialization += value;
  ***REMOVED***

  return serialization;
***REMOVED***;

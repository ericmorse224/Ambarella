"use strict";
const http = require("http");
const https = require("https");
const { parse: parseURLToNodeOptions ***REMOVED*** = require("url");
const HttpProxyAgent = require("http-proxy-agent");
const HttpsProxyAgent = require("https-proxy-agent");

module.exports = function agentFactory(proxy, rejectUnauthorized) {
  const agentOpts = { keepAlive: true, rejectUnauthorized ***REMOVED***;
  if (proxy) {
    const proxyOpts = { ...parseURLToNodeOptions(proxy), ...agentOpts ***REMOVED***;
    return { https: new HttpsProxyAgent(proxyOpts), http: new HttpProxyAgent(proxyOpts) ***REMOVED***;
  ***REMOVED***
  return { http: new http.Agent(agentOpts), https: new https.Agent(agentOpts) ***REMOVED***;
***REMOVED***;

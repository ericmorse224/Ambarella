import test from 'tape';
import { elementType ***REMOVED*** from 'jsx-ast-utils';

import isNonInteractiveRole from '../../../src/util/isNonInteractiveRole';
import {
  genElementSymbol,
  genInteractiveRoleElements,
  genNonInteractiveRoleElements,
***REMOVED*** from '../../../__mocks__/genInteractives';

test('isNonInteractiveRole', (t) => {
  t.equal(
    isNonInteractiveRole(undefined, []),
    false,
    'identifies JSX Components (no tagName) as non-interactive elements',
  );

  t.test('elements with a non-interactive role', (st) => {
    genNonInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;

      st.equal(
        isNonInteractiveRole(
          elementType(openingElement),
          attributes,
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.equal(
    isNonInteractiveRole('div', []),
    false,
    'does NOT identify elements without a role as non-interactive role elements',
  );

  t.test('elements with an interactive role', (st) => {
    genInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      const { attributes ***REMOVED*** = openingElement;

      st.equal(
        isNonInteractiveRole(
          elementType(openingElement),
          attributes,
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive role element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.end();
***REMOVED***);

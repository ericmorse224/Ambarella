import { YAMLSeq ***REMOVED*** from '../nodes/YAMLSeq.js';
import { resolveProps ***REMOVED*** from './resolve-props.js';
import { flowIndentCheck ***REMOVED*** from './util-flow-indent-check.js';

function resolveBlockSeq({ composeNode, composeEmptyNode ***REMOVED***, ctx, bs, onError, tag) {
    const NodeClass = tag?.nodeClass ?? YAMLSeq;
    const seq = new NodeClass(ctx.schema);
    if (ctx.atRoot)
        ctx.atRoot = false;
    if (ctx.atKey)
        ctx.atKey = false;
    let offset = bs.offset;
    let commentEnd = null;
    for (const { start, value ***REMOVED*** of bs.items) {
        const props = resolveProps(start, {
            indicator: 'seq-item-ind',
            next: value,
            offset,
            onError,
            parentIndent: bs.indent,
            startOnNewline: true
        ***REMOVED***);
        if (!props.found) {
            if (props.anchor || props.tag || value) {
                if (value && value.type === 'block-seq')
                    onError(props.end, 'BAD_INDENT', 'All sequence items must start at the same column');
                else
                    onError(offset, 'MISSING_CHAR', 'Sequence item without - indicator');
            ***REMOVED***
            else {
                commentEnd = props.end;
                if (props.comment)
                    seq.comment = props.comment;
                continue;
            ***REMOVED***
        ***REMOVED***
        const node = value
            ? composeNode(ctx, value, props, onError)
            : composeEmptyNode(ctx, props.end, start, null, props, onError);
        if (ctx.schema.compat)
            flowIndentCheck(bs.indent, value, onError);
        offset = node.range[2];
        seq.items.push(node);
    ***REMOVED***
    seq.range = [bs.offset, offset, commentEnd ?? offset];
    return seq;
***REMOVED***

export { resolveBlockSeq ***REMOVED***;

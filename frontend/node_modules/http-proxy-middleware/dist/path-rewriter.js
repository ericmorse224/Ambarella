"use strict";
Object.defineProperty(exports, "__esModule", { value: true ***REMOVED***);
exports.createPathRewriter = void 0;
const isPlainObj = require("is-plain-obj");
const errors_1 = require("./errors");
const logger_1 = require("./logger");
const logger = (0, logger_1.getInstance)();
/**
 * Create rewrite function, to cache parsed rewrite rules.
 *
 * @param {Object***REMOVED*** rewriteConfig
 * @return {Function***REMOVED*** Function to rewrite paths; This function should accept `path` (request.url) as parameter
 */
function createPathRewriter(rewriteConfig) {
    let rulesCache;
    if (!isValidRewriteConfig(rewriteConfig)) {
        return;
    ***REMOVED***
    if (typeof rewriteConfig === 'function') {
        const customRewriteFn = rewriteConfig;
        return customRewriteFn;
    ***REMOVED***
    else {
        rulesCache = parsePathRewriteRules(rewriteConfig);
        return rewritePath;
    ***REMOVED***
    function rewritePath(path) {
        let result = path;
        for (const rule of rulesCache) {
            if (rule.regex.test(path)) {
                result = result.replace(rule.regex, rule.value);
                logger.debug('[HPM] Rewriting path from "%s" to "%s"', path, result);
                break;
            ***REMOVED***
        ***REMOVED***
        return result;
    ***REMOVED***
***REMOVED***
exports.createPathRewriter = createPathRewriter;
function isValidRewriteConfig(rewriteConfig) {
    if (typeof rewriteConfig === 'function') {
        return true;
    ***REMOVED***
    else if (isPlainObj(rewriteConfig)) {
        return Object.keys(rewriteConfig).length !== 0;
    ***REMOVED***
    else if (rewriteConfig === undefined || rewriteConfig === null) {
        return false;
    ***REMOVED***
    else {
        throw new Error(errors_1.ERRORS.ERR_PATH_REWRITER_CONFIG);
    ***REMOVED***
***REMOVED***
function parsePathRewriteRules(rewriteConfig) {
    const rules = [];
    if (isPlainObj(rewriteConfig)) {
        for (const [key] of Object.entries(rewriteConfig)) {
            rules.push({
                regex: new RegExp(key),
                value: rewriteConfig[key],
            ***REMOVED***);
            logger.info('[HPM] Proxy rewrite rule created: "%s" ~> "%s"', key, rewriteConfig[key]);
        ***REMOVED***
    ***REMOVED***
    return rules;
***REMOVED***

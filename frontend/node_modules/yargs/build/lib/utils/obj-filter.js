import { objectKeys ***REMOVED*** from '../typings/common-types.js';
export function objFilter(original = {***REMOVED***, filter = () => true) {
    const obj = {***REMOVED***;
    objectKeys(original).forEach(key => {
        if (filter(key, original[key])) {
            obj[key] = original[key];
        ***REMOVED***
    ***REMOVED***);
    return obj;
***REMOVED***

"use strict";

const { findSlot, assignSlot, assignSlotable ***REMOVED*** = require("../helpers/shadow-dom");

// https://dom.spec.whatwg.org/#mixin-slotable
// https://dom.spec.whatwg.org/#light-tree-slotables
class SlotableMixinImpl {
  _initSlotableMixin() {
    this._slotableName = "";
  ***REMOVED***

  _attrModifiedSlotableMixin(name, value, oldValue) {
    if (name === "slot") {
      if (value === oldValue) {
        return;
      ***REMOVED***

      if (value === null && oldValue === "") {
        return;
      ***REMOVED***

      if (value === "" && oldValue === null) {
        return;
      ***REMOVED***

      if (value === null || value === "") {
        this._slotableName = "";
      ***REMOVED*** else {
        this._slotableName = value;
      ***REMOVED***


      if (this._assignedSlot) {
        assignSlotable(this._assignedSlot);
      ***REMOVED***

      assignSlot(this);
    ***REMOVED***
  ***REMOVED***

  get assignedSlot() {
    return findSlot(this, "open");
  ***REMOVED***
***REMOVED***

module.exports = {
  implementation: SlotableMixinImpl
***REMOVED***;

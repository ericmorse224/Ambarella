let assert = require('assert');
let exec = require('child_process').execSync;

const PROJECT_DIR = process.env.PROJECT_DIR;
const JAKE_CMD = `${PROJECT_DIR***REMOVED***/bin/cli.js`;

suite('selfDep', function () {

  this.timeout(7000);

  let origStderrWrite;

  setup(function () {
    origStderrWrite = process.stderr.write;
    process.stderr.write = function () {***REMOVED***;
  ***REMOVED***);

  teardown(function () {
    process.stderr.write = origStderrWrite;
  ***REMOVED***);

  test('self dep const', function () {
    try {
      exec(`${JAKE_CMD***REMOVED*** selfdepconst`);
    ***REMOVED***
    catch(e) {
      assert(e.message.indexOf('dependency of itself') > -1);
    ***REMOVED***
  ***REMOVED***);

  test('self dep dyn', function () {
    try {
      exec(`${JAKE_CMD***REMOVED*** selfdepdyn`);
    ***REMOVED***
    catch(e) {
      assert(e.message.indexOf('dependency of itself') > -1);
    ***REMOVED***
  ***REMOVED***);

***REMOVED***);



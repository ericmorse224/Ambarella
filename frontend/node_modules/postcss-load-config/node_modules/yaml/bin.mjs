#!/usr/bin/env node

import { UserError, cli, help ***REMOVED*** from './dist/cli.mjs'

cli(process.stdin, error => {
  if (error instanceof UserError) {
    if (error.code === UserError.ARGS) console.error(`${help***REMOVED***\n`)
    console.error(error.message)
    process.exitCode = error.code
  ***REMOVED*** else if (error) throw error
***REMOVED***)

// @ts-check
"use strict";

/** @typedef {import("webpack").Compilation***REMOVED*** Compilation */

/**
 * @type {{[sortmode: string] : (entryPointNames: Array<string>, compilation: Compilation, htmlWebpackPluginOptions: any) => Array<string> ***REMOVED******REMOVED***
 * This file contains different sort methods for the entry chunks names
 */
module.exports = {***REMOVED***;

/**
 * Performs identity mapping (no-sort).
 * @param  {Array<string>***REMOVED*** chunks the chunks to sort
 * @return {Array<string>***REMOVED*** The sorted chunks
 */
module.exports.none = (chunks) => chunks;

/**
 * Sort manually by the chunks
 * @param {string[]***REMOVED*** entryPointNames the chunks to sort
 * @param {Compilation***REMOVED*** compilation the webpack compilation
 * @param {any***REMOVED*** htmlWebpackPluginOptions the plugin options
 * @return {string[]***REMOVED*** The sorted chunks
 */
module.exports.manual = (
  entryPointNames,
  compilation,
  htmlWebpackPluginOptions,
) => {
  const chunks = htmlWebpackPluginOptions.chunks;
  if (!Array.isArray(chunks)) {
    return entryPointNames;
  ***REMOVED***
  // Remove none existing entries from
  // htmlWebpackPluginOptions.chunks
  return chunks.filter((entryPointName) => {
    return compilation.entrypoints.has(entryPointName);
  ***REMOVED***);
***REMOVED***;

/**
 * Defines the default sorter.
 */
module.exports.auto = module.exports.none;

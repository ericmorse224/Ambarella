'use strict';

var test = require('tape');
var hasToStringTag = require('has-tostringtag/shams')();
var hasOwn = require('hasown');

var setToStringTag = require('../');

test('setToStringTag', function (t) {
	t.equal(typeof setToStringTag, 'function', 'is a function');

	/** @type {{ [Symbol.toStringTag]?: typeof sentinel ***REMOVED******REMOVED*** */
	var obj = {***REMOVED***;
	var sentinel = {***REMOVED***;

	setToStringTag(obj, sentinel);

	t['throws'](
		// @ts-expect-error
		function () { setToStringTag(obj, sentinel, { force: 'yes' ***REMOVED***); ***REMOVED***,
		TypeError,
		'throws if options is not an object'
	);

	t.test('has Symbol.toStringTag', { skip: !hasToStringTag ***REMOVED***, function (st) {
		st.ok(hasOwn(obj, Symbol.toStringTag), 'has toStringTag property');

		st.equal(obj[Symbol.toStringTag], sentinel, 'toStringTag property is as expected');

		st.equal(String(obj), '[object Object]', 'toStringTag works');

		/** @type {{ [Symbol.toStringTag]?: string ***REMOVED******REMOVED*** */
		var tagged = {***REMOVED***;
		tagged[Symbol.toStringTag] = 'already tagged';
		st.equal(String(tagged), '[object already tagged]', 'toStringTag works');

		setToStringTag(tagged, 'new tag');
		st.equal(String(tagged), '[object already tagged]', 'toStringTag is unchanged');

		setToStringTag(tagged, 'new tag', { force: true ***REMOVED***);
		st.equal(String(tagged), '[object new tag]', 'toStringTag is changed with force: true');

		st.deepEqual(
			Object.getOwnPropertyDescriptor(tagged, Symbol.toStringTag),
			{
				configurable: true,
				enumerable: false,
				value: 'new tag',
				writable: false
			***REMOVED***,
			'has expected property descriptor'
		);

		setToStringTag(tagged, 'new tag', { force: true, nonConfigurable: true ***REMOVED***);
		st.deepEqual(
			Object.getOwnPropertyDescriptor(tagged, Symbol.toStringTag),
			{
				configurable: false,
				enumerable: false,
				value: 'new tag',
				writable: false
			***REMOVED***,
			'is nonconfigurable'
		);

		st.end();
	***REMOVED***);

	t.test('does not have Symbol.toStringTag', { skip: hasToStringTag ***REMOVED***, function (st) {
		var passed = true;
		for (var key in obj) { // eslint-disable-line no-restricted-syntax
			if (hasOwn(obj, key)) {
				st.fail('object has own key ' + key);
				passed = false;
			***REMOVED***
		***REMOVED***
		if (passed) {
			st.ok(true, 'object has no enumerable own keys');
		***REMOVED***

		st.end();
	***REMOVED***);

	t.end();
***REMOVED***);

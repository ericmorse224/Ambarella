/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

"use strict";

const SizeFormatHelpers = require("../SizeFormatHelpers");
const WebpackError = require("../WebpackError");

class MinMaxSizeWarning extends WebpackError {
	/**
	 * @param {string[] | undefined***REMOVED*** keys keys
	 * @param {number***REMOVED*** minSize minimum size
	 * @param {number***REMOVED*** maxSize maximum size
	 */
	constructor(keys, minSize, maxSize) {
		let keysMessage = "Fallback cache group";
		if (keys) {
			keysMessage =
				keys.length > 1
					? `Cache groups ${keys.sort().join(", ")***REMOVED***`
					: `Cache group ${keys[0]***REMOVED***`;
		***REMOVED***
		super(
			"SplitChunksPlugin\n" +
				`${keysMessage***REMOVED***\n` +
				`Configured minSize (${SizeFormatHelpers.formatSize(minSize)***REMOVED***) is ` +
				`bigger than maxSize (${SizeFormatHelpers.formatSize(maxSize)***REMOVED***).\n` +
				"This seem to be a invalid optimization.splitChunks configuration."
		);
	***REMOVED***
***REMOVED***

module.exports = MinMaxSizeWarning;

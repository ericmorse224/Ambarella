'use strict';

var define = require('define-properties');
var getPolyfill = require('./polyfill');

module.exports = function shimArrayPrototypeReduce() {
	var polyfill = getPolyfill();
	define(
		Array.prototype,
		{ reduce: polyfill ***REMOVED***,
		{ reduce: function () { return Array.prototype.reduce !== polyfill; ***REMOVED*** ***REMOVED***
	);
	return polyfill;
***REMOVED***;

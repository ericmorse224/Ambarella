'use strict'

const { stringify ***REMOVED*** = require('jsonfile/utils')
const { outputFile ***REMOVED*** = require('../output-file')

async function outputJson (file, data, options = {***REMOVED***) {
  const str = stringify(data, options)

  await outputFile(file, str, options)
***REMOVED***

module.exports = outputJson

import '../_version.js';
/**
 * Returns the timestamp model.
 *
 * @private
 */
declare class CacheTimestampsModel {
    private readonly _cacheName;
    private _db;
    /**
     *
     * @param {string***REMOVED*** cacheName
     *
     * @private
     */
    constructor(cacheName: string);
    /**
     * Performs an upgrade of indexedDB.
     *
     * @param {IDBPDatabase<CacheDbSchema>***REMOVED*** db
     *
     * @private
     */
    private _upgradeDb;
    /**
     * Performs an upgrade of indexedDB and deletes deprecated DBs.
     *
     * @param {IDBPDatabase<CacheDbSchema>***REMOVED*** db
     *
     * @private
     */
    private _upgradeDbAndDeleteOldDbs;
    /**
     * @param {string***REMOVED*** url
     * @param {number***REMOVED*** timestamp
     *
     * @private
     */
    setTimestamp(url: string, timestamp: number): Promise<void>;
    /**
     * Returns the timestamp stored for a given URL.
     *
     * @param {string***REMOVED*** url
     * @return {number | undefined***REMOVED***
     *
     * @private
     */
    getTimestamp(url: string): Promise<number | undefined>;
    /**
     * Iterates through all the entries in the object store (from newest to
     * oldest) and removes entries once either `maxCount` is reached or the
     * entry's timestamp is less than `minTimestamp`.
     *
     * @param {number***REMOVED*** minTimestamp
     * @param {number***REMOVED*** maxCount
     * @return {Array<string>***REMOVED***
     *
     * @private
     */
    expireEntries(minTimestamp: number, maxCount?: number): Promise<string[]>;
    /**
     * Takes a URL and returns an ID that will be unique in the object store.
     *
     * @param {string***REMOVED*** url
     * @return {string***REMOVED***
     *
     * @private
     */
    private _getId;
    /**
     * Returns an open connection to the database.
     *
     * @private
     */
    private getDb;
***REMOVED***
export { CacheTimestampsModel ***REMOVED***;

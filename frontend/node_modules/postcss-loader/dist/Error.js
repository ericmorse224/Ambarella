"use strict";

/**
 * **PostCSS Syntax Error**
 *
 * Loader wrapper for postcss syntax errors
 *
 * @class SyntaxError
 * @extends Error
 *
 * @param {Object***REMOVED*** err CssSyntaxError
 */
class SyntaxError extends Error {
  constructor(error) {
    super(error);
    const {
      line,
      column,
      reason,
      plugin,
      file
    ***REMOVED*** = error;
    this.name = "SyntaxError";
    this.message = `${this.name***REMOVED***\n\n`;

    if (typeof line !== "undefined") {
      this.message += `(${line***REMOVED***:${column***REMOVED***) `;
    ***REMOVED***

    this.message += plugin ? `${plugin***REMOVED***: ` : "";
    this.message += file ? `${file***REMOVED*** ` : "<css input> ";
    this.message += `${reason***REMOVED***`;
    const code = error.showSourceCode();

    if (code) {
      this.message += `\n\n${code***REMOVED***\n`;
    ***REMOVED***

    this.stack = false;
  ***REMOVED***

***REMOVED***

module.exports = SyntaxError;
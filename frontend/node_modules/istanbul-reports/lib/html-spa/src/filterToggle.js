const React = require('react');

function ToggleOption({ children, filter, activeFilters, setFilters ***REMOVED***) {
    return (
        <button
            className={
                'toggle__option ' + (activeFilters[filter] ? 'is-toggled' : '')
            ***REMOVED***
            onClick={() =>
                setFilters({
                    ...activeFilters,
                    [filter]: !activeFilters[filter]
                ***REMOVED***)
            ***REMOVED***
        >
            {children***REMOVED***
        </button>
    );
***REMOVED***

module.exports = function FilterToggle({ activeFilters, setFilters ***REMOVED***) {
    return (
        <div className="toggle">
            <div className="toggle__label">Filter:</div>
            <div className="toggle__options">
                <ToggleOption
                    filter="low"
                    activeFilters={activeFilters***REMOVED***
                    setFilters={setFilters***REMOVED***
                >
                    Low
                </ToggleOption>
                <ToggleOption
                    filter="medium"
                    activeFilters={activeFilters***REMOVED***
                    setFilters={setFilters***REMOVED***
                >
                    Medium
                </ToggleOption>
                <ToggleOption
                    filter="high"
                    activeFilters={activeFilters***REMOVED***
                    setFilters={setFilters***REMOVED***
                >
                    High
                </ToggleOption>
            </div>
        </div>
    );
***REMOVED***;

'use strict';
const getLastNode = require('./getLastNode.js');

/**
 * @param {import('postcss').Declaration[]***REMOVED*** props
 * @param {string[]***REMOVED*** properties
 * @return {import('postcss').Declaration[]***REMOVED***
 */
module.exports = function getRules(props, properties) {
  return properties
    .map((property) => {
      return getLastNode(props, property);
    ***REMOVED***)
    .filter(Boolean);
***REMOVED***;

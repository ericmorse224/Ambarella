'use strict';

// This is an example of using tokens to add a custom behaviour.
//
// Throw an error if an option is used more than once.

// 1. const { parseArgs ***REMOVED*** = require('node:util'); // from node
// 2. const { parseArgs ***REMOVED*** = require('@pkgjs/parseargs'); // from package
const { parseArgs ***REMOVED*** = require('..'); // in repo

const options = {
  ding: { type: 'boolean', short: 'd' ***REMOVED***,
  beep: { type: 'boolean', short: 'b' ***REMOVED***
***REMOVED***;
const { values, tokens ***REMOVED*** = parseArgs({ options, tokens: true ***REMOVED***);

const seenBefore = new Set();
tokens.forEach((token) => {
  if (token.kind !== 'option') return;
  if (seenBefore.has(token.name)) {
    throw new Error(`option '${token.name***REMOVED***' used multiple times`);
  ***REMOVED***
  seenBefore.add(token.name);
***REMOVED***);

console.log(values);

// Try the following:
//    node no-repeated-options --ding --beep
//    node no-repeated-options --beep -b
//    node no-repeated-options -ddd

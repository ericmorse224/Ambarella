'use strict';

var trim = require('../implementation');
var test = require('tape');
var hasStrictMode = require('has-strict-mode')();
var callBind = require('call-bind');

var runTests = require('./tests');

test('as a function', function (t) {
	t.test('bad array/this value', { skip: !hasStrictMode ***REMOVED***, function (st) {
		/* eslint no-useless-call: 0 */
		st['throws'](function () { trim.call(undefined, 'a'); ***REMOVED***, TypeError, 'undefined is not an object');
		st['throws'](function () { trim.call(null, 'a'); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(callBind(trim), t);

	t.end();
***REMOVED***);

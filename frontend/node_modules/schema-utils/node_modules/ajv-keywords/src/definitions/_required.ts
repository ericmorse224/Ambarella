import type {MacroKeywordDefinition***REMOVED*** from "ajv"
import type {GetDefinition***REMOVED*** from "./_types"

type RequiredKwd = "anyRequired" | "oneRequired"

export default function getRequiredDef(
  keyword: RequiredKwd
): GetDefinition<MacroKeywordDefinition> {
  return () => ({
    keyword,
    type: "object",
    schemaType: "array",
    macro(schema: string[]) {
      if (schema.length === 0) return true
      if (schema.length === 1) return {required: schema***REMOVED***
      const comb = keyword === "anyRequired" ? "anyOf" : "oneOf"
      return {[comb]: schema.map((p) => ({required: [p]***REMOVED***))***REMOVED***
    ***REMOVED***,
    metaSchema: {
      type: "array",
      items: {type: "string"***REMOVED***,
    ***REMOVED***,
  ***REMOVED***)
***REMOVED***

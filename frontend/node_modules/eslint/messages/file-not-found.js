"use strict";

module.exports = function(it) {
    const { pattern, globDisabled ***REMOVED*** = it;

    return `
No files matching the pattern "${pattern***REMOVED***"${globDisabled ? " (with disabling globs)" : ""***REMOVED*** were found.
Please check for typing mistakes in the pattern.
`.trimStart();
***REMOVED***;

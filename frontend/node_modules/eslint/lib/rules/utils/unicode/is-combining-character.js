/**
 * @author Toru Nagashima <https://github.com/mysticatea>
 */
"use strict";

/**
 * Check whether a given character is a combining mark or not.
 * @param {number***REMOVED*** codePoint The character code to check.
 * @returns {boolean***REMOVED*** `true` if the character belongs to the category, any of `Mc`, `Me`, and `Mn`.
 */
module.exports = function isCombiningCharacter(codePoint) {
    return /^[\p{Mc***REMOVED***\p{Me***REMOVED***\p{Mn***REMOVED***]$/u.test(String.fromCodePoint(codePoint));
***REMOVED***;

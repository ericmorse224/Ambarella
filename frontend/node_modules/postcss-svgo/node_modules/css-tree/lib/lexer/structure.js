var List = require('../common/List');
var hasOwnProperty = Object.prototype.hasOwnProperty;

function isValidNumber(value) {
    // Number.isInteger(value) && value >= 0
    return (
        typeof value === 'number' &&
        isFinite(value) &&
        Math.floor(value) === value &&
        value >= 0
    );
***REMOVED***

function isValidLocation(loc) {
    return (
        Boolean(loc) &&
        isValidNumber(loc.offset) &&
        isValidNumber(loc.line) &&
        isValidNumber(loc.column)
    );
***REMOVED***

function createNodeStructureChecker(type, fields) {
    return function checkNode(node, warn) {
        if (!node || node.constructor !== Object) {
            return warn(node, 'Type of node should be an Object');
        ***REMOVED***

        for (var key in node) {
            var valid = true;

            if (hasOwnProperty.call(node, key) === false) {
                continue;
            ***REMOVED***

            if (key === 'type') {
                if (node.type !== type) {
                    warn(node, 'Wrong node type `' + node.type + '`, expected `' + type + '`');
                ***REMOVED***
            ***REMOVED*** else if (key === 'loc') {
                if (node.loc === null) {
                    continue;
                ***REMOVED*** else if (node.loc && node.loc.constructor === Object) {
                    if (typeof node.loc.source !== 'string') {
                        key += '.source';
                    ***REMOVED*** else if (!isValidLocation(node.loc.start)) {
                        key += '.start';
                    ***REMOVED*** else if (!isValidLocation(node.loc.end)) {
                        key += '.end';
                    ***REMOVED*** else {
                        continue;
                    ***REMOVED***
                ***REMOVED***

                valid = false;
            ***REMOVED*** else if (fields.hasOwnProperty(key)) {
                for (var i = 0, valid = false; !valid && i < fields[key].length; i++) {
                    var fieldType = fields[key][i];

                    switch (fieldType) {
                        case String:
                            valid = typeof node[key] === 'string';
                            break;

                        case Boolean:
                            valid = typeof node[key] === 'boolean';
                            break;

                        case null:
                            valid = node[key] === null;
                            break;

                        default:
                            if (typeof fieldType === 'string') {
                                valid = node[key] && node[key].type === fieldType;
                            ***REMOVED*** else if (Array.isArray(fieldType)) {
                                valid = node[key] instanceof List;
                            ***REMOVED***
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED*** else {
                warn(node, 'Unknown field `' + key + '` for ' + type + ' node type');
            ***REMOVED***

            if (!valid) {
                warn(node, 'Bad value for `' + type + '.' + key + '`');
            ***REMOVED***
        ***REMOVED***

        for (var key in fields) {
            if (hasOwnProperty.call(fields, key) &&
                hasOwnProperty.call(node, key) === false) {
                warn(node, 'Field `' + type + '.' + key + '` is missed');
            ***REMOVED***
        ***REMOVED***
    ***REMOVED***;
***REMOVED***

function processStructure(name, nodeType) {
    var structure = nodeType.structure;
    var fields = {
        type: String,
        loc: true
    ***REMOVED***;
    var docs = {
        type: '"' + name + '"'
    ***REMOVED***;

    for (var key in structure) {
        if (hasOwnProperty.call(structure, key) === false) {
            continue;
        ***REMOVED***

        var docsTypes = [];
        var fieldTypes = fields[key] = Array.isArray(structure[key])
            ? structure[key].slice()
            : [structure[key]];

        for (var i = 0; i < fieldTypes.length; i++) {
            var fieldType = fieldTypes[i];
            if (fieldType === String || fieldType === Boolean) {
                docsTypes.push(fieldType.name);
            ***REMOVED*** else if (fieldType === null) {
                docsTypes.push('null');
            ***REMOVED*** else if (typeof fieldType === 'string') {
                docsTypes.push('<' + fieldType + '>');
            ***REMOVED*** else if (Array.isArray(fieldType)) {
                docsTypes.push('List'); // TODO: use type enum
            ***REMOVED*** else {
                throw new Error('Wrong value `' + fieldType + '` in `' + name + '.' + key + '` structure definition');
            ***REMOVED***
        ***REMOVED***

        docs[key] = docsTypes.join(' | ');
    ***REMOVED***

    return {
        docs: docs,
        check: createNodeStructureChecker(name, fields)
    ***REMOVED***;
***REMOVED***

module.exports = {
    getStructureFromConfig: function(config) {
        var structure = {***REMOVED***;

        if (config.node) {
            for (var name in config.node) {
                if (hasOwnProperty.call(config.node, name)) {
                    var nodeType = config.node[name];

                    if (nodeType.structure) {
                        structure[name] = processStructure(name, nodeType);
                    ***REMOVED*** else {
                        throw new Error('Missed `structure` field in `' + name + '` node type definition');
                    ***REMOVED***
                ***REMOVED***
            ***REMOVED***
        ***REMOVED***

        return structure;
    ***REMOVED***
***REMOVED***;

'use strict';

/**
 * @param {import('postcss-selector-parser').Selector***REMOVED*** selector
 * @param {number***REMOVED*** index
 * @param {string***REMOVED*** value
 * @return {boolean | undefined | ''***REMOVED***
 */
module.exports = function exists(selector, index, value) {
  const node = selector.at(index);

  return node && node.value && node.value.toLowerCase() === value;
***REMOVED***;

#!/usr/bin/env node
'use strict';

const {
  loadNycConfig
***REMOVED*** = require('@istanbuljs/load-nyc-config');

async function main() {
  const [cwd, nycrcPath] = process.argv.slice(2);
  console.log(JSON.stringify(await loadNycConfig({
    cwd,
    nycrcPath
  ***REMOVED***)));
***REMOVED***

main().catch(error => {
  console.log(JSON.stringify({
    'load-nyc-config-sync-error': error.message
  ***REMOVED***));
***REMOVED***);
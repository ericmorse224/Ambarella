import {ConditionalKeys***REMOVED*** from './conditional-keys';

/**
Pick keys from the shape that matches the given `Condition`.

This is useful when you want to create a new type from a specific subset of an existing type. For example, you might want to pick all the primitive properties from a class and form a new automatically derived type.

@example
```
import {Primitive, ConditionalPick***REMOVED*** from 'type-fest';

class Awesome {
	name: string;
	successes: number;
	failures: bigint;

	run() {***REMOVED***
***REMOVED***

type PickPrimitivesFromAwesome = ConditionalPick<Awesome, Primitive>;
//=> {name: string; successes: number; failures: bigint***REMOVED***
```

@example
```
import {ConditionalPick***REMOVED*** from 'type-fest';

interface Example {
	a: string;
	b: string | number;
	c: () => void;
	d: {***REMOVED***;
***REMOVED***

type StringKeysOnly = ConditionalPick<Example, string>;
//=> {a: string***REMOVED***
```
*/
export type ConditionalPick<Base, Condition> = Pick<
	Base,
	ConditionalKeys<Base, Condition>
>;

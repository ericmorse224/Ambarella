"use strict";

const { stringifyValueForError ***REMOVED*** = require("./shared");

module.exports = function({ ruleId, value ***REMOVED***) {
    return `
Configuration for rule "${ruleId***REMOVED***" is invalid. Each rule must have a severity ("off", 0, "warn", 1, "error", or 2) and may be followed by additional options for the rule.

You passed '${stringifyValueForError(value, 4)***REMOVED***', which doesn't contain a valid severity.

If you're attempting to configure rule options, perhaps you meant:

    "${ruleId***REMOVED***": ["error", ${stringifyValueForError(value, 8)***REMOVED***]

See https://eslint.org/docs/latest/use/configure/rules#using-configuration-files for configuring rules.
`.trimStart();
***REMOVED***;

'use strict'

var util = require('util')
var isNode = require('detect-node')

// Node.js 0.8, 0.10 and 0.12 support
Object.assign = (process.versions.modules >= 46 || !isNode)
  ? Object.assign // eslint-disable-next-line
  : util._extend

function QueueItem () {
  this.prev = null
  this.next = null
***REMOVED***
exports.QueueItem = QueueItem

function Queue () {
  QueueItem.call(this)

  this.prev = this
  this.next = this
***REMOVED***
util.inherits(Queue, QueueItem)
exports.Queue = Queue

Queue.prototype.insertTail = function insertTail (item) {
  item.prev = this.prev
  item.next = this
  item.prev.next = item
  item.next.prev = item
***REMOVED***

Queue.prototype.remove = function remove (item) {
  var next = item.next
  var prev = item.prev

  item.next = item
  item.prev = item
  next.prev = prev
  prev.next = next
***REMOVED***

Queue.prototype.head = function head () {
  return this.next
***REMOVED***

Queue.prototype.tail = function tail () {
  return this.prev
***REMOVED***

Queue.prototype.isEmpty = function isEmpty () {
  return this.next === this
***REMOVED***

Queue.prototype.isRoot = function isRoot (item) {
  return this === item
***REMOVED***

function LockStream (stream) {
  this.locked = false
  this.queue = []
  this.stream = stream
***REMOVED***
exports.LockStream = LockStream

LockStream.prototype.write = function write (chunks, callback) {
  var self = this

  // Do not let it interleave
  if (this.locked) {
    this.queue.push(function () {
      return self.write(chunks, callback)
    ***REMOVED***)
    return
  ***REMOVED***

  this.locked = true

  function done (err, chunks) {
    self.stream.removeListener('error', done)

    self.locked = false
    if (self.queue.length > 0) { self.queue.shift()() ***REMOVED***
    callback(err, chunks)
  ***REMOVED***

  this.stream.on('error', done)

  // Accumulate all output data
  var output = []
  function onData (chunk) {
    output.push(chunk)
  ***REMOVED***
  this.stream.on('data', onData)

  function next (err) {
    self.stream.removeListener('data', onData)
    if (err) {
      return done(err)
    ***REMOVED***

    done(null, output)
  ***REMOVED***

  for (var i = 0; i < chunks.length - 1; i++) { this.stream.write(chunks[i]) ***REMOVED***

  if (chunks.length > 0) {
    this.stream.write(chunks[i], next)
  ***REMOVED*** else { process.nextTick(next) ***REMOVED***

  if (this.stream.execute) {
    this.stream.execute(function (err) {
      if (err) { return done(err) ***REMOVED***
    ***REMOVED***)
  ***REMOVED***
***REMOVED***

// Just finds the place in array to insert
function binaryLookup (list, item, compare) {
  var start = 0
  var end = list.length

  while (start < end) {
    var pos = (start + end) >> 1
    var cmp = compare(item, list[pos])

    if (cmp === 0) {
      start = pos
      end = pos
      break
    ***REMOVED*** else if (cmp < 0) {
      end = pos
    ***REMOVED*** else {
      start = pos + 1
    ***REMOVED***
  ***REMOVED***

  return start
***REMOVED***
exports.binaryLookup = binaryLookup

function binaryInsert (list, item, compare) {
  var index = binaryLookup(list, item, compare)

  list.splice(index, 0, item)
***REMOVED***
exports.binaryInsert = binaryInsert

function binarySearch (list, item, compare) {
  var index = binaryLookup(list, item, compare)

  if (index >= list.length) {
    return -1
  ***REMOVED***

  if (compare(item, list[index]) === 0) {
    return index
  ***REMOVED***

  return -1
***REMOVED***
exports.binarySearch = binarySearch

function Timeout (object) {
  this.delay = 0
  this.timer = null
  this.object = object
***REMOVED***
exports.Timeout = Timeout

Timeout.prototype.set = function set (delay, callback) {
  this.delay = delay
  this.reset()
  if (!callback) { return ***REMOVED***

  if (this.delay === 0) {
    this.object.removeListener('timeout', callback)
  ***REMOVED*** else {
    this.object.once('timeout', callback)
  ***REMOVED***
***REMOVED***

Timeout.prototype.reset = function reset () {
  if (this.timer !== null) {
    clearTimeout(this.timer)
    this.timer = null
  ***REMOVED***

  if (this.delay === 0) { return ***REMOVED***

  var self = this
  this.timer = setTimeout(function () {
    self.timer = null
    self.object.emit('timeout')
  ***REMOVED***, this.delay)
***REMOVED***

const MARKER = "my-special-import";
module.exports = (config, outputOptions, omt) => {
  config.plugins = [
    omt(),
    {
      resolveId(id) {
        if (id !== MARKER) {
          return;
        ***REMOVED***
        return id;
      ***REMOVED***,
      load(id) {
        if (id !== MARKER) {
          return;
        ***REMOVED***
        const referenceId = this.emitFile({
          type: "asset",
          name: "my-asset.bin",
          source: "assetcontent"
        ***REMOVED***);
        return `export default import.meta.ROLLUP_FILE_URL_${referenceId***REMOVED***`;
      ***REMOVED***
    ***REMOVED***
  ];
***REMOVED***;

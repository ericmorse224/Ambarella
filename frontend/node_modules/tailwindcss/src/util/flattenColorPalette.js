const flattenColorPalette = (colors) =>
  Object.assign(
    {***REMOVED***,
    ...Object.entries(colors ?? {***REMOVED***).flatMap(([color, values]) =>
      typeof values == 'object'
        ? Object.entries(flattenColorPalette(values)).map(([number, hex]) => ({
            [color + (number === 'DEFAULT' ? '' : `-${number***REMOVED***`)]: hex,
          ***REMOVED***))
        : [{ [`${color***REMOVED***`]: values ***REMOVED***]
    )
  )

export default flattenColorPalette

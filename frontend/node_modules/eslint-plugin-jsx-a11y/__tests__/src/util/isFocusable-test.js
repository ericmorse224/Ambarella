import test from 'tape';
import { elementType ***REMOVED*** from 'jsx-ast-utils';

import isFocusable from '../../../src/util/isFocusable';
import {
  genElementSymbol,
  genInteractiveElements,
  genNonInteractiveElements,
***REMOVED*** from '../../../__mocks__/genInteractives';
import JSXAttributeMock from '../../../__mocks__/JSXAttributeMock';

function mergeTabIndex(index, attributes) {
  return [].concat(attributes, JSXAttributeMock('tabIndex', index));
***REMOVED***

test('isFocusable', (t) => {
  t.test('interactive elements', (st) => {
    genInteractiveElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isFocusable(
          elementType(openingElement),
          openingElement.attributes,
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(-1, openingElement.attributes),
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of -1 as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(0, openingElement.attributes),
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of 0 as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(1, openingElement.attributes),
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of 1 as a focusable element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('non-interactive elements', (st) => {
    genNonInteractiveElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isFocusable(
          elementType(openingElement),
          openingElement.attributes,
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(-1, openingElement.attributes),
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of -1 as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(0, openingElement.attributes),
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of 0 as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex(1, openingElement.attributes),
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of 1 as a focusable element`,
      );

      st.equal(
        isFocusable(
          elementType(openingElement),
          mergeTabIndex('bogus', openingElement.attributes),
        ),
        false,
        `does NOT identify \`${genElementSymbol(openingElement)***REMOVED***\` with tabIndex of 'bogus' as a focusable element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.end();
***REMOVED***);

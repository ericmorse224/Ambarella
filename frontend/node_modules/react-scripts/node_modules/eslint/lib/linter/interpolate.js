/**
 * @fileoverview Interpolate keys from an object into a string with {{ ***REMOVED******REMOVED*** markers.
 * @author Jed Fox
 */

"use strict";

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

module.exports = (text, data) => {
    if (!data) {
        return text;
    ***REMOVED***

    // Substitution content for any {{ ***REMOVED******REMOVED*** markers.
    return text.replace(/\{\{([^{***REMOVED***]+?)\***REMOVED***\***REMOVED***/gu, (fullMatch, termWithWhitespace) => {
        const term = termWithWhitespace.trim();

        if (term in data) {
            return data[term];
        ***REMOVED***

        // Preserve old behavior: If parameter name not provided, don't replace it.
        return fullMatch;
    ***REMOVED***);
***REMOVED***;

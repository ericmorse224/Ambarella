import type {CodeKeywordDefinition***REMOVED*** from "../../types"
import type {KeywordCxt***REMOVED*** from "../../compile/validate"
import {checkStrictMode***REMOVED*** from "../../compile/util"

const def: CodeKeywordDefinition = {
  keyword: ["maxContains", "minContains"],
  type: "array",
  schemaType: "number",
  code({keyword, parentSchema, it***REMOVED***: KeywordCxt) {
    if (parentSchema.contains === undefined) {
      checkStrictMode(it, `"${keyword***REMOVED***" without "contains" is ignored`)
    ***REMOVED***
  ***REMOVED***,
***REMOVED***

export default def

// Type definitions for commander 2.11
// Project: https://github.com/visionmedia/commander.js
// Definitions by: Alan Agius <https://github.com/alan-agius4>, Marcelo Dezem <https://github.com/mdezem>, vvakame <https://github.com/vvakame>, Jules Randolph <https://github.com/sveinburne>
// Definitions: https://github.com/DefinitelyTyped/DefinitelyTyped

declare namespace local {

  class Option {
    flags: string;
    required: boolean;
    optional: boolean;
    bool: boolean;
    short?: string;
    long: string;
    description: string;

    /**
     * Initialize a new `Option` with the given `flags` and `description`.
     *
     * @param {string***REMOVED*** flags
     * @param {string***REMOVED*** [description]
     */
    constructor(flags: string, description?: string);
  ***REMOVED***

  class Command extends NodeJS.EventEmitter {
    [key: string]: any;

    args: string[];

    /**
     * Initialize a new `Command`.
     *
     * @param {string***REMOVED*** [name]
     */
    constructor(name?: string);

    /**
     * Set the program version to `str`.
     *
     * This method auto-registers the "-V, --version" flag
     * which will print the version number when passed.
     *
     * @param {string***REMOVED*** str
     * @param {string***REMOVED*** [flags]
     * @returns {Command***REMOVED*** for chaining
     */
    version(str: string, flags?: string): Command;

    /**
     * Add command `name`.
     *
     * The `.action()` callback is invoked when the
     * command `name` is specified via __ARGV__,
     * and the remaining arguments are applied to the
     * function for access.
     *
     * When the `name` is "*" an un-matched command
     * will be passed as the first arg, followed by
     * the rest of __ARGV__ remaining.
     *
     * @example
     *      program
     *        .version('0.0.1')
     *        .option('-C, --chdir <path>', 'change the working directory')
     *        .option('-c, --config <path>', 'set config path. defaults to ./deploy.conf')
     *        .option('-T, --no-tests', 'ignore test hook')
     *
     *      program
     *        .command('setup')
     *        .description('run remote setup commands')
     *        .action(function() {
     *          console.log('setup');
     *        ***REMOVED***);
     *
     *      program
     *        .command('exec <cmd>')
     *        .description('run the given remote command')
     *        .action(function(cmd) {
     *          console.log('exec "%s"', cmd);
     *        ***REMOVED***);
     *
     *      program
     *        .command('teardown <dir> [otherDirs...]')
     *        .description('run teardown commands')
     *        .action(function(dir, otherDirs) {
     *          console.log('dir "%s"', dir);
     *          if (otherDirs) {
     *            otherDirs.forEach(function (oDir) {
     *              console.log('dir "%s"', oDir);
     *            ***REMOVED***);
     *          ***REMOVED***
     *        ***REMOVED***);
     *
     *      program
     *        .command('*')
     *        .description('deploy the given env')
     *        .action(function(env) {
     *          console.log('deploying "%s"', env);
     *        ***REMOVED***);
     *
     *      program.parse(process.argv);
     *
     * @param {string***REMOVED*** name
     * @param {string***REMOVED*** [desc] for git-style sub-commands
     * @param {CommandOptions***REMOVED*** [opts] command options
     * @returns {Command***REMOVED*** the new command
     */
    command(name: string, desc?: string, opts?: commander.CommandOptions): Command;

    /**
     * Define argument syntax for the top-level command.
     *
     * @param {string***REMOVED*** desc
     * @returns {Command***REMOVED*** for chaining
     */
    arguments(desc: string): Command;

    /**
     * Parse expected `args`.
     *
     * For example `["[type]"]` becomes `[{ required: false, name: 'type' ***REMOVED***]`.
     *
     * @param {string[]***REMOVED*** args
     * @returns {Command***REMOVED*** for chaining
     */
    parseExpectedArgs(args: string[]): Command;

    /**
     * Register callback `fn` for the command.
     *
     * @example
     *      program
     *        .command('help')
     *        .description('display verbose help')
     *        .action(function() {
     *           // output help here
     *        ***REMOVED***);
     *
     * @param {(...args: any[]) => void***REMOVED*** fn
     * @returns {Command***REMOVED*** for chaining
     */
    action(fn: (...args: any[]) => void): Command;

    /**
     * Define option with `flags`, `description` and optional
     * coercion `fn`.
     *
     * The `flags` string should contain both the short and long flags,
     * separated by comma, a pipe or space. The following are all valid
     * all will output this way when `--help` is used.
     *
     *    "-p, --pepper"
     *    "-p|--pepper"
     *    "-p --pepper"
     *
     * @example
     *     // simple boolean defaulting to false
     *     program.option('-p, --pepper', 'add pepper');
     *
     *     --pepper
     *     program.pepper
     *     // => Boolean
     *
     *     // simple boolean defaulting to true
     *     program.option('-C, --no-cheese', 'remove cheese');
     *
     *     program.cheese
     *     // => true
     *
     *     --no-cheese
     *     program.cheese
     *     // => false
     *
     *     // required argument
     *     program.option('-C, --chdir <path>', 'change the working directory');
     *
     *     --chdir /tmp
     *     program.chdir
     *     // => "/tmp"
     *
     *     // optional argument
     *     program.option('-c, --cheese [type]', 'add cheese [marble]');
     *
     * @param {string***REMOVED*** flags
     * @param {string***REMOVED*** [description]
     * @param {((arg1: any, arg2: any) => void) | RegExp***REMOVED*** [fn] function or default
     * @param {****REMOVED*** [defaultValue]
     * @returns {Command***REMOVED*** for chaining
     */
    option(flags: string, description?: string, fn?: ((arg1: any, arg2: any) => void) | RegExp, defaultValue?: any): Command;
    option(flags: string, description?: string, defaultValue?: any): Command;

    /**
     * Allow unknown options on the command line.
     *
     * @param {boolean***REMOVED*** [arg] if `true` or omitted, no error will be thrown for unknown options.
     * @returns {Command***REMOVED*** for chaining
     */
    allowUnknownOption(arg?: boolean): Command;

    /**
     * Parse `argv`, settings options and invoking commands when defined.
     *
     * @param {string[]***REMOVED*** argv
     * @returns {Command***REMOVED*** for chaining
     */
    parse(argv: string[]): Command;

    /**
     * Parse options from `argv` returning `argv` void of these options.
     *
     * @param {string[]***REMOVED*** argv
     * @returns {ParseOptionsResult***REMOVED***
     */
    parseOptions(argv: string[]): commander.ParseOptionsResult;

    /**
     * Return an object containing options as key-value pairs
     *
     * @returns {{[key: string]: any***REMOVED******REMOVED***
     */
    opts(): { [key: string]: any ***REMOVED***;

    /**
     * Set the description to `str`.
     *
     * @param {string***REMOVED*** str
     * @param {{[argName: string]: string***REMOVED******REMOVED*** argsDescription
     * @return {(Command | string)***REMOVED***
     */
    description(str: string, argsDescription?: {[argName: string]: string***REMOVED***): Command;
    description(): string;

    /**
     * Set an alias for the command.
     *
     * @param {string***REMOVED*** alias
     * @return {(Command | string)***REMOVED***
     */
    alias(alias: string): Command;
    alias(): string;

    /**
     * Set or get the command usage.
     *
     * @param {string***REMOVED*** str
     * @return {(Command | string)***REMOVED***
     */
    usage(str: string): Command;
    usage(): string;

    /**
     * Set the name of the command.
     *
     * @param {string***REMOVED*** str
     * @return {Command***REMOVED***
     */
    name(str: string): Command;

    /**
     * Get the name of the command.
     *
     * @return {string***REMOVED***
     */
    name(): string;

    /**
     * Output help information for this command.
     *
     * @param {(str: string) => string***REMOVED*** [cb]
     */
    outputHelp(cb?: (str: string) => string): void;

    /** Output help information and exit.
     *
     * @param {(str: string) => string***REMOVED*** [cb]
     */
    help(cb?: (str: string) => string): never;
  ***REMOVED***

***REMOVED***

declare namespace commander {

    type Command = local.Command

    type Option = local.Option

    interface CommandOptions {
        noHelp?: boolean;
        isDefault?: boolean;
    ***REMOVED***

    interface ParseOptionsResult {
        args: string[];
        unknown: string[];
    ***REMOVED***

    interface CommanderStatic extends Command {
        Command: typeof local.Command;
        Option: typeof local.Option;
        CommandOptions: CommandOptions;
        ParseOptionsResult: ParseOptionsResult;
    ***REMOVED***

***REMOVED***

declare const commander: commander.CommanderStatic;
export = commander;

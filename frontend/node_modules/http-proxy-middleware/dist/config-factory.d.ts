import { Filter, Options ***REMOVED*** from './types';
export declare type Config = {
    context: Filter;
    options: Options;
***REMOVED***;
export declare function createConfig(context: any, opts?: Options): Config;

'use strict';
var Queue = function () {
  this.head = null;
  this.tail = null;
***REMOVED***;

Queue.prototype = {
  add: function (item) {
    var entry = { item: item, next: null ***REMOVED***;
    var tail = this.tail;
    if (tail) tail.next = entry;
    else this.head = entry;
    this.tail = entry;
  ***REMOVED***,
  get: function () {
    var entry = this.head;
    if (entry) {
      var next = this.head = entry.next;
      if (next === null) this.tail = null;
      return entry.item;
    ***REMOVED***
  ***REMOVED***
***REMOVED***;

module.exports = Queue;

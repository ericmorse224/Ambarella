import crypto from 'crypto';

function md5(bytes) {
  if (Array.isArray(bytes)) {
    bytes = Buffer.from(bytes);
  ***REMOVED*** else if (typeof bytes === 'string') {
    bytes = Buffer.from(bytes, 'utf8');
  ***REMOVED***

  return crypto.createHash('md5').update(bytes).digest();
***REMOVED***

export default md5;
import test from 'tape';
import { elementType ***REMOVED*** from 'jsx-ast-utils';

import isNonInteractiveElement from '../../../src/util/isNonInteractiveElement';
import {
  genElementSymbol,
  genIndeterminantInteractiveElements,
  genInteractiveElements,
  genInteractiveRoleElements,
  genNonInteractiveElements,
  genNonInteractiveRoleElements,
***REMOVED*** from '../../../__mocks__/genInteractives';

test('isNonInteractiveElement', (t) => {
  t.equal(
    isNonInteractiveElement(undefined, []),
    false,
    'identifies JSX Components (no tagName) as non-interactive elements',
  );

  t.test('non-interactive elements', (st) => {
    genNonInteractiveElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isNonInteractiveElement(
          elementType(openingElement),
          openingElement.attributes,
        ),
        true,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('non-interactive role elements', (st) => {
    genNonInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isNonInteractiveElement(
          elementType(openingElement),
          openingElement.attributes,
        ),
        false,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('interactive elements', (st) => {
    genInteractiveElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isNonInteractiveElement(
          elementType(openingElement),
          openingElement.attributes,
        ),
        false,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('interactive role elements', (st) => {
    genInteractiveRoleElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isNonInteractiveElement(
          elementType(openingElement),
          openingElement.attributes,
        ),
        false,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.test('indeterminate elements', (st) => {
    genIndeterminantInteractiveElements().forEach(({ openingElement ***REMOVED***) => {
      st.equal(
        isNonInteractiveElement(
          elementType(openingElement),
          openingElement.attributes,
        ),
        false,
        `identifies \`${genElementSymbol(openingElement)***REMOVED***\` as a non-interactive element`,
      );
    ***REMOVED***);

    st.end();
  ***REMOVED***);

  t.end();
***REMOVED***);

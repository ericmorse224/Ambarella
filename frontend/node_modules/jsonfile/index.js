let _fs
try {
  _fs = require('graceful-fs')
***REMOVED*** catch (_) {
  _fs = require('fs')
***REMOVED***
const universalify = require('universalify')
const { stringify, stripBom ***REMOVED*** = require('./utils')

async function _readFile (file, options = {***REMOVED***) {
  if (typeof options === 'string') {
    options = { encoding: options ***REMOVED***
  ***REMOVED***

  const fs = options.fs || _fs

  const shouldThrow = 'throws' in options ? options.throws : true

  let data = await universalify.fromCallback(fs.readFile)(file, options)

  data = stripBom(data)

  let obj
  try {
    obj = JSON.parse(data, options ? options.reviver : null)
  ***REMOVED*** catch (err) {
    if (shouldThrow) {
      err.message = `${file***REMOVED***: ${err.message***REMOVED***`
      throw err
    ***REMOVED*** else {
      return null
    ***REMOVED***
  ***REMOVED***

  return obj
***REMOVED***

const readFile = universalify.fromPromise(_readFile)

function readFileSync (file, options = {***REMOVED***) {
  if (typeof options === 'string') {
    options = { encoding: options ***REMOVED***
  ***REMOVED***

  const fs = options.fs || _fs

  const shouldThrow = 'throws' in options ? options.throws : true

  try {
    let content = fs.readFileSync(file, options)
    content = stripBom(content)
    return JSON.parse(content, options.reviver)
  ***REMOVED*** catch (err) {
    if (shouldThrow) {
      err.message = `${file***REMOVED***: ${err.message***REMOVED***`
      throw err
    ***REMOVED*** else {
      return null
    ***REMOVED***
  ***REMOVED***
***REMOVED***

async function _writeFile (file, obj, options = {***REMOVED***) {
  const fs = options.fs || _fs

  const str = stringify(obj, options)

  await universalify.fromCallback(fs.writeFile)(file, str, options)
***REMOVED***

const writeFile = universalify.fromPromise(_writeFile)

function writeFileSync (file, obj, options = {***REMOVED***) {
  const fs = options.fs || _fs

  const str = stringify(obj, options)
  // not sure if fs.writeFileSync returns anything, but just in case
  return fs.writeFileSync(file, str, options)
***REMOVED***

const jsonfile = {
  readFile,
  readFileSync,
  writeFile,
  writeFileSync
***REMOVED***

module.exports = jsonfile

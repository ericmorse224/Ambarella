/**
 * @fileoverview A rule to disallow modifying variables of class declarations
 * @author Toru Nagashima
 */

"use strict";

const astUtils = require("./utils/ast-utils");

//------------------------------------------------------------------------------
// Rule Definition
//------------------------------------------------------------------------------

/** @type {import('../shared/types').Rule***REMOVED*** */
module.exports = {
    meta: {
        type: "problem",

        docs: {
            description: "Disallow reassigning class members",
            recommended: true,
            url: "https://eslint.org/docs/latest/rules/no-class-assign"
        ***REMOVED***,

        schema: [],

        messages: {
            class: "'{{name***REMOVED******REMOVED***' is a class."
        ***REMOVED***
    ***REMOVED***,

    create(context) {

        const sourceCode = context.sourceCode;

        /**
         * Finds and reports references that are non initializer and writable.
         * @param {Variable***REMOVED*** variable A variable to check.
         * @returns {void***REMOVED***
         */
        function checkVariable(variable) {
            astUtils.getModifyingReferences(variable.references).forEach(reference => {
                context.report({ node: reference.identifier, messageId: "class", data: { name: reference.identifier.name ***REMOVED*** ***REMOVED***);

            ***REMOVED***);
        ***REMOVED***

        /**
         * Finds and reports references that are non initializer and writable.
         * @param {ASTNode***REMOVED*** node A ClassDeclaration/ClassExpression node to check.
         * @returns {void***REMOVED***
         */
        function checkForClass(node) {
            sourceCode.getDeclaredVariables(node).forEach(checkVariable);
        ***REMOVED***

        return {
            ClassDeclaration: checkForClass,
            ClassExpression: checkForClass
        ***REMOVED***;

    ***REMOVED***
***REMOVED***;

'use strict';

module.exports = function quote(xs) {
	return xs.map(function (s) {
		if (s === '') {
			return '\'\'';
		***REMOVED***
		if (s && typeof s === 'object') {
			return s.op.replace(/(.)/g, '\\$1');
		***REMOVED***
		if ((/["\s]/).test(s) && !(/'/).test(s)) {
			return "'" + s.replace(/(['\\])/g, '\\$1') + "'";
		***REMOVED***
		if ((/["'\s]/).test(s)) {
			return '"' + s.replace(/(["\\$`!])/g, '\\$1') + '"';
		***REMOVED***
		return String(s).replace(/([A-Za-z]:)?([#!"$&'()*,:;<=>?@[\\\]^`{|***REMOVED***])/g, '$1\\$2');
	***REMOVED***).join(' ');
***REMOVED***;

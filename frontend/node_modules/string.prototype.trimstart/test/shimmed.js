'use strict';

require('../auto');

var runTests = require('./tests');

var test = require('tape');
var defineProperties = require('define-properties');
var callBind = require('call-bind');
var isEnumerable = Object.prototype.propertyIsEnumerable;
var functionsHaveNames = require('functions-have-names')();

test('shimmed', function (t) {
	t.equal(String.prototype.trimStart.length, 0, 'String#trimStart has a length of 0');
	t.test('Function name', { skip: !functionsHaveNames ***REMOVED***, function (st) {
		st.equal((/^(?:trimLeft|trimStart)$/).test(String.prototype.trimStart.name), true, 'String#trimStart has name "trimLeft" or "trimStart"');
		st.end();
	***REMOVED***);

	t.test('enumerability', { skip: !defineProperties.supportsDescriptors ***REMOVED***, function (et) {
		et.equal(false, isEnumerable.call(String.prototype, 'trimStart'), 'String#trimStart is not enumerable');
		et.end();
	***REMOVED***);

	var supportsStrictMode = (function () { return typeof this === 'undefined'; ***REMOVED***());

	t.test('bad string/this value', { skip: !supportsStrictMode ***REMOVED***, function (st) {
		st['throws'](function () { return String.prototype.trimStart.call(undefined, 'a'); ***REMOVED***, TypeError, 'undefined is not an object');
		st['throws'](function () { return String.prototype.trimStart.call(null, 'a'); ***REMOVED***, TypeError, 'null is not an object');
		st.end();
	***REMOVED***);

	runTests(callBind(String.prototype.trimStart), t);

	t.end();
***REMOVED***);

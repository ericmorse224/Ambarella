'use strict';

var define = require('define-properties');
var getPolyfill = require('./polyfill');

var $IteratorPrototype = require('../Iterator.prototype/implementation');

module.exports = function shimIteratorPrototypeFilter() {
	var polyfill = getPolyfill();

	define(
		$IteratorPrototype,
		{ filter: polyfill ***REMOVED***,
		{ filter: function () { return $IteratorPrototype.filter !== polyfill; ***REMOVED*** ***REMOVED***
	);

	return polyfill;
***REMOVED***;

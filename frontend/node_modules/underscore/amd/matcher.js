define(['./extendOwn', './isMatch'], function (extendOwn, isMatch) {

  // Returns a predicate for checking whether an object has a given set of
  // `key:value` pairs.
  function matcher(attrs) {
    attrs = extendOwn({***REMOVED***, attrs);
    return function(obj) {
      return isMatch(obj, attrs);
    ***REMOVED***;
  ***REMOVED***

  return matcher;

***REMOVED***);

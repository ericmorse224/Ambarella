/**
 * @fileoverview Traverser to traverse AST trees.
 * @author Nicholas C. Zakas
 * @author Toru Nagashima
 */
"use strict";

//------------------------------------------------------------------------------
// Requirements
//------------------------------------------------------------------------------

const vk = require("eslint-visitor-keys");
const debug = require("debug")("eslint:traverser");

//------------------------------------------------------------------------------
// Helpers
//------------------------------------------------------------------------------

/**
 * Do nothing.
 * @returns {void***REMOVED***
 */
function noop() {

    // do nothing.
***REMOVED***

/**
 * Check whether the given value is an ASTNode or not.
 * @param {any***REMOVED*** x The value to check.
 * @returns {boolean***REMOVED*** `true` if the value is an ASTNode.
 */
function isNode(x) {
    return x !== null && typeof x === "object" && typeof x.type === "string";
***REMOVED***

/**
 * Get the visitor keys of a given node.
 * @param {Object***REMOVED*** visitorKeys The map of visitor keys.
 * @param {ASTNode***REMOVED*** node The node to get their visitor keys.
 * @returns {string[]***REMOVED*** The visitor keys of the node.
 */
function getVisitorKeys(visitorKeys, node) {
    let keys = visitorKeys[node.type];

    if (!keys) {
        keys = vk.getKeys(node);
        debug("Unknown node type \"%s\": Estimated visitor keys %j", node.type, keys);
    ***REMOVED***

    return keys;
***REMOVED***

/**
 * The traverser class to traverse AST trees.
 */
class Traverser {
    constructor() {
        this._current = null;
        this._parents = [];
        this._skipped = false;
        this._broken = false;
        this._visitorKeys = null;
        this._enter = null;
        this._leave = null;
    ***REMOVED***

    /**
     * Gives current node.
     * @returns {ASTNode***REMOVED*** The current node.
     */
    current() {
        return this._current;
    ***REMOVED***

    /**
     * Gives a copy of the ancestor nodes.
     * @returns {ASTNode[]***REMOVED*** The ancestor nodes.
     */
    parents() {
        return this._parents.slice(0);
    ***REMOVED***

    /**
     * Break the current traversal.
     * @returns {void***REMOVED***
     */
    break() {
        this._broken = true;
    ***REMOVED***

    /**
     * Skip child nodes for the current traversal.
     * @returns {void***REMOVED***
     */
    skip() {
        this._skipped = true;
    ***REMOVED***

    /**
     * Traverse the given AST tree.
     * @param {ASTNode***REMOVED*** node The root node to traverse.
     * @param {Object***REMOVED*** options The option object.
     * @param {Object***REMOVED*** [options.visitorKeys=DEFAULT_VISITOR_KEYS] The keys of each node types to traverse child nodes. Default is `./default-visitor-keys.json`.
     * @param {Function***REMOVED*** [options.enter=noop] The callback function which is called on entering each node.
     * @param {Function***REMOVED*** [options.leave=noop] The callback function which is called on leaving each node.
     * @returns {void***REMOVED***
     */
    traverse(node, options) {
        this._current = null;
        this._parents = [];
        this._skipped = false;
        this._broken = false;
        this._visitorKeys = options.visitorKeys || vk.KEYS;
        this._enter = options.enter || noop;
        this._leave = options.leave || noop;
        this._traverse(node, null);
    ***REMOVED***

    /**
     * Traverse the given AST tree recursively.
     * @param {ASTNode***REMOVED*** node The current node.
     * @param {ASTNode|null***REMOVED*** parent The parent node.
     * @returns {void***REMOVED***
     * @private
     */
    _traverse(node, parent) {
        if (!isNode(node)) {
            return;
        ***REMOVED***

        this._current = node;
        this._skipped = false;
        this._enter(node, parent);

        if (!this._skipped && !this._broken) {
            const keys = getVisitorKeys(this._visitorKeys, node);

            if (keys.length >= 1) {
                this._parents.push(node);
                for (let i = 0; i < keys.length && !this._broken; ++i) {
                    const child = node[keys[i]];

                    if (Array.isArray(child)) {
                        for (let j = 0; j < child.length && !this._broken; ++j) {
                            this._traverse(child[j], node);
                        ***REMOVED***
                    ***REMOVED*** else {
                        this._traverse(child, node);
                    ***REMOVED***
                ***REMOVED***
                this._parents.pop();
            ***REMOVED***
        ***REMOVED***

        if (!this._broken) {
            this._leave(node, parent);
        ***REMOVED***

        this._current = parent;
    ***REMOVED***

    /**
     * Calculates the keys to use for traversal.
     * @param {ASTNode***REMOVED*** node The node to read keys from.
     * @returns {string[]***REMOVED*** An array of keys to visit on the node.
     * @private
     */
    static getKeys(node) {
        return vk.getKeys(node);
    ***REMOVED***

    /**
     * Traverse the given AST tree.
     * @param {ASTNode***REMOVED*** node The root node to traverse.
     * @param {Object***REMOVED*** options The option object.
     * @param {Object***REMOVED*** [options.visitorKeys=DEFAULT_VISITOR_KEYS] The keys of each node types to traverse child nodes. Default is `./default-visitor-keys.json`.
     * @param {Function***REMOVED*** [options.enter=noop] The callback function which is called on entering each node.
     * @param {Function***REMOVED*** [options.leave=noop] The callback function which is called on leaving each node.
     * @returns {void***REMOVED***
     */
    static traverse(node, options) {
        new Traverser().traverse(node, options);
    ***REMOVED***

    /**
     * The default visitor keys.
     * @type {Object***REMOVED***
     */
    static get DEFAULT_VISITOR_KEYS() {
        return vk.KEYS;
    ***REMOVED***
***REMOVED***

module.exports = Traverser;

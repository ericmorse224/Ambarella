'use strict';

var implementation = require('../implementation');
var callBind = require('call-bind');
var test = require('tape');
var hasStrictMode = require('has-strict-mode')();
var runTests = require('./tests');

test('as a function', function (t) {
	t.test('ArrayBuffer support', { skip: typeof ArrayBuffer === 'undefined' ***REMOVED***, function (st) {
		st.test('bad array/this value', { skip: !hasStrictMode ***REMOVED***, function (s2t) {
		/* eslint no-useless-call: 0 */
			s2t['throws'](function () { implementation.call(undefined); ***REMOVED***, TypeError, 'undefined is not an object');
			s2t['throws'](function () { implementation.call(null); ***REMOVED***, TypeError, 'null is not an object');
			s2t.end();
		***REMOVED***);

		runTests(callBind(implementation), st);

		st.end();
	***REMOVED***);

	t.test('no ArrayBuffer support', { skip: typeof ArrayBuffer !== 'undefined' ***REMOVED***, function (st) {
		st['throws'](
			function () { implementation.call({***REMOVED***); ***REMOVED***,
			SyntaxError,
			'ArrayBuffer is not supported'
		);
	***REMOVED***);

	t.end();
***REMOVED***);

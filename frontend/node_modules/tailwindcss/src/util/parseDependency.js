// @ts-check

/**
 * @typedef {{type: 'dependency', file: string***REMOVED*** | {type: 'dir-dependency', dir: string, glob: string***REMOVED******REMOVED*** Dependency
 */

/**
 *
 * @param {import('../lib/content.js').ContentPath***REMOVED*** contentPath
 * @returns {Dependency[]***REMOVED***
 */
export default function parseDependency(contentPath) {
  if (contentPath.ignore) {
    return []
  ***REMOVED***

  if (!contentPath.glob) {
    return [
      {
        type: 'dependency',
        file: contentPath.base,
      ***REMOVED***,
    ]
  ***REMOVED***

  if (process.env.ROLLUP_WATCH === 'true') {
    // rollup-plugin-postcss does not support dir-dependency messages
    // but directories can be watched in the same way as files
    return [
      {
        type: 'dependency',
        file: contentPath.base,
      ***REMOVED***,
    ]
  ***REMOVED***

  return [
    {
      type: 'dir-dependency',
      dir: contentPath.base,
      glob: contentPath.glob,
    ***REMOVED***,
  ]
***REMOVED***

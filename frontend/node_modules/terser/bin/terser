#!/usr/bin/env node

"use strict";

require("../tools/exit.cjs");

try {
    require("source-map-support").install();
***REMOVED*** catch (err) {***REMOVED***

const fs = require("fs");
const path = require("path");
const program = require("commander");

const packageJson = require("../package.json");
const { _run_cli: run_cli ***REMOVED*** = require("..");

run_cli({ program, packageJson, fs, path ***REMOVED***).catch((error) => {
    console.error(error);
    process.exitCode = 1;
***REMOVED***);

'use strict';
/**
 * @param {import('postcss').Rule***REMOVED*** rule
 * @param {import('postcss').Declaration***REMOVED*** decl
 * @param {Partial<import('postcss').DeclarationProps>=***REMOVED*** props
 * @return {import('postcss').Declaration***REMOVED***
 */
module.exports = function insertCloned(rule, decl, props) {
  const newNode = Object.assign(decl.clone(), props);

  rule.insertAfter(decl, newNode);

  return newNode;
***REMOVED***;

// parse out just the options we care about
const looseOption = Object.freeze({ loose: true ***REMOVED***)
const emptyOpts = Object.freeze({ ***REMOVED***)
const parseOptions = options => {
  if (!options) {
    return emptyOpts
  ***REMOVED***

  if (typeof options !== 'object') {
    return looseOption
  ***REMOVED***

  return options
***REMOVED***
module.exports = parseOptions

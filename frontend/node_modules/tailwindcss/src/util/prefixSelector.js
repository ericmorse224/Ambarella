import parser from 'postcss-selector-parser'

/**
 * @template {string | import('postcss-selector-parser').Root***REMOVED*** T
 *
 * Prefix all classes in the selector with the given prefix
 *
 * It can take either a string or a selector AST and will return the same type
 *
 * @param {string***REMOVED*** prefix
 * @param {T***REMOVED*** selector
 * @param {boolean***REMOVED*** prependNegative
 * @returns {T***REMOVED***
 */
export default function (prefix, selector, prependNegative = false) {
  if (prefix === '') {
    return selector
  ***REMOVED***

  /** @type {import('postcss-selector-parser').Root***REMOVED*** */
  let ast = typeof selector === 'string' ? parser().astSync(selector) : selector

  ast.walkClasses((classSelector) => {
    let baseClass = classSelector.value
    let shouldPlaceNegativeBeforePrefix = prependNegative && baseClass.startsWith('-')

    classSelector.value = shouldPlaceNegativeBeforePrefix
      ? `-${prefix***REMOVED***${baseClass.slice(1)***REMOVED***`
      : `${prefix***REMOVED***${baseClass***REMOVED***`
  ***REMOVED***)

  return typeof selector === 'string' ? ast.toString() : ast
***REMOVED***

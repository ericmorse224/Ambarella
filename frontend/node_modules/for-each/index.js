'use strict';

var isCallable = require('is-callable');

var toStr = Object.prototype.toString;
var hasOwnProperty = Object.prototype.hasOwnProperty;

/** @type {<This, A extends readonly unknown[]>(arr: A, iterator: (this: This | void, value: A[number], index: number, arr: A) => void, receiver: This | undefined) => void***REMOVED*** */
var forEachArray = function forEachArray(array, iterator, receiver) {
    for (var i = 0, len = array.length; i < len; i++) {
        if (hasOwnProperty.call(array, i)) {
            if (receiver == null) {
                iterator(array[i], i, array);
            ***REMOVED*** else {
                iterator.call(receiver, array[i], i, array);
            ***REMOVED***
        ***REMOVED***
    ***REMOVED***
***REMOVED***;

/** @type {<This, S extends string>(string: S, iterator: (this: This | void, value: S[number], index: number, string: S) => void, receiver: This | undefined) => void***REMOVED*** */
var forEachString = function forEachString(string, iterator, receiver) {
    for (var i = 0, len = string.length; i < len; i++) {
        // no such thing as a sparse string.
        if (receiver == null) {
            iterator(string.charAt(i), i, string);
        ***REMOVED*** else {
            iterator.call(receiver, string.charAt(i), i, string);
        ***REMOVED***
    ***REMOVED***
***REMOVED***;

/** @type {<This, O>(obj: O, iterator: (this: This | void, value: O[keyof O], index: keyof O, obj: O) => void, receiver: This | undefined) => void***REMOVED*** */
var forEachObject = function forEachObject(object, iterator, receiver) {
    for (var k in object) {
        if (hasOwnProperty.call(object, k)) {
            if (receiver == null) {
                iterator(object[k], k, object);
            ***REMOVED*** else {
                iterator.call(receiver, object[k], k, object);
            ***REMOVED***
        ***REMOVED***
    ***REMOVED***
***REMOVED***;

/** @type {(x: unknown) => x is readonly unknown[]***REMOVED*** */
function isArray(x) {
    return toStr.call(x) === '[object Array]';
***REMOVED***

/** @type {import('.')._internal***REMOVED*** */
module.exports = function forEach(list, iterator, thisArg) {
    if (!isCallable(iterator)) {
        throw new TypeError('iterator must be a function');
    ***REMOVED***

    var receiver;
    if (arguments.length >= 3) {
        receiver = thisArg;
    ***REMOVED***

    if (isArray(list)) {
        forEachArray(list, iterator, receiver);
    ***REMOVED*** else if (typeof list === 'string') {
        forEachString(list, iterator, receiver);
    ***REMOVED*** else {
        forEachObject(list, iterator, receiver);
    ***REMOVED***
***REMOVED***;

const doNothingValues = new Set([
	'inherit',
	'initial',
	'revert',
	'unset',
]);

/**
 * @type {import('postcss').PluginCreator***REMOVED***
 */
module.exports = ({preserve = false***REMOVED*** = {***REMOVED***) => ({
	postcssPlugin: 'postcss-opacity-percentage',
	Declaration: {
		opacity(decl) {
			if (!decl.value || decl.value.startsWith('var(') || !decl.value.endsWith('%') || doNothingValues.has(decl.value)) {
				return;
			***REMOVED***

			decl.cloneBefore({value: String(Number.parseFloat(decl.value) / 100)***REMOVED***);
			if (!preserve) {
				decl.remove();
			***REMOVED***
		***REMOVED***,
	***REMOVED***,
***REMOVED***);

module.exports.postcss = true;

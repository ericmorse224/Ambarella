var Ajv = require('ajv');
var ajv = new Ajv({allErrors: true***REMOVED***);

var schema = {
  "properties": {
    "foo": { "type": "string" ***REMOVED***,
    "bar": { "type": "number", "maximum": 3 ***REMOVED***
  ***REMOVED***
***REMOVED***;

var validate = ajv.compile(schema);

test({"foo": "abc", "bar": 2***REMOVED***);
test({"foo": 2, "bar": 4***REMOVED***);

function test(data) {
  var valid = validate(data);
  if (valid) console.log('Valid!');
  else console.log('Invalid: ' + ajv.errorsText(validate.errors));
***REMOVED***
var List = require('../common/List');

module.exports = function clone(node) {
    var result = {***REMOVED***;

    for (var key in node) {
        var value = node[key];

        if (value) {
            if (Array.isArray(value) || value instanceof List) {
                value = value.map(clone);
            ***REMOVED*** else if (value.constructor === Object) {
                value = clone(value);
            ***REMOVED***
        ***REMOVED***

        result[key] = value;
    ***REMOVED***

    return result;
***REMOVED***;

import type { Node ***REMOVED*** from 'estree';

declare function visit(
    node: Node,
    keys: { [k in Node['type']]?: (keyof Node)[] ***REMOVED***,
    visitorSpec: { [k in Node['type'] | `${Node['type']***REMOVED***:Exit`]?: Function ***REMOVED***
): void;

export default visit;

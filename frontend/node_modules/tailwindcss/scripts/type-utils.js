export function union(types) {
  return [...new Set(types)].join(' | ')
***REMOVED***

export function unionValues(values) {
  return union(values.map(forValue))
***REMOVED***

export function forKeys(value) {
  return union(Object.keys(value).map((key) => `'${key***REMOVED***'`))
***REMOVED***

export function forValue(value) {
  if (Array.isArray(value)) {
    return `(${unionValues(value)***REMOVED***)[]`
  ***REMOVED***

  if (typeof value === 'object') {
    return `Record<${forKeys(value)***REMOVED***, ${unionValues(Object.values(value))***REMOVED***>`
  ***REMOVED***

  if (typeof value === 'string') {
    return `string`
  ***REMOVED***

  return `any`
***REMOVED***
